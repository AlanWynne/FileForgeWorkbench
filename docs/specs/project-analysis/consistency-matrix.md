# Consistency Matrix

Cross-unit record of shared/public artifacts and their owning Logical_Unit
(Requirement 3). A **Conflict** is any row with more than one claimed owner or a
contradiction. Populated incrementally as each sub-project is analysed; finalized
in Wave 6.

Where `docs/specs/project-master/api-consistency-report.md` and
`docs/reviews/requirements-review/terminology-map.md` already establish an owner,
cite them in the Resolution column rather than re-deriving.

Status: EMPTY (re-baselined after CR-NR-057, commit `b537737`) -- populated per
sub-project during Waves 0-5, finalized in Wave 6.

---

## Public Types

| Type | Owner unit | Referencing units | Conflict? | Resolution |
|------|-----------|-------------------|-----------|------------|
| `WorkbenchApp` | platform-core | ff-desktop, (plugin ctx) | No | Sole owner ff-core |
| `ServiceRegistry` | platform-core | ff-plugin (PluginContext) | No | Sole owner ff-core |
| `EventBus` | platform-core | ff-desktop | No | Sole owner ff-core |
| `LifecyclePhase` / `ThreadContext` / `CoreError` | platform-core | ff-desktop | No | Sole owner ff-core |
| `ConfigProvider` (trait) | platform-core (defined) | ff-config (impl) | No | Trait in ff-core, impl in ff-config -- correct direction |
| 5-layer model (Foundation/Core/Editor/Feature/Shell) | platform-core (Req 4) | whole workspace | No | Authoritative dependency-direction reference; enforced by Cargo.toml. Req 4.1 crate names illustrative -- PA-DOC-001 |
| `ConfigValue` / `ConfigTable` / `ConfigLayer` / `ConfigHandle` / `ConfigError` | configuration-system | all consumer crates | No | Sole owner ff-config |
| `EffectiveValue` / `Provenance` / `SchemaEntry` | configuration-system | ff-desktop (Settings) | No | Sole owner ff-config |
| `PluginConfigHandle` | configuration-system | ff-plugin (PluginContext) | No | Sole owner ff-config |
| `AuditEntry` / `ExportScope` / `ImportTarget` / `ImportSummary` | configuration-system | ff-desktop | No | Sole owner ff-config (Req 16/17) |
| `ConfigProvider` (trait) | platform-core (defined) | ff-config (impl) | No | Correct direction (W0.1) |
| plugin namespace `[plugins.{name}]` | configuration-system (Req 8) + plugin-architecture (Req 2.7/7.5) | -- | No | Same scoping rule both specs |
| Settings_Menu / Settings_Namespace_View | configuration-system (Req 15) + menu-workspace | -- | Watch | Settings UI shared -- PA-SPLIT-001 folds Req 15 into menu-workspace |
| `LogLevel` / `LogConfig` / `LogRecord` / `PluginLogHandle` | logging-subsystem | all crates | No | Sole owner ff-logging |
| `log_trace!`/`log_debug!`/`log_info!`/`log_warn!`/`log_error!` (macros) + `log()`/`log_lazy()` | logging-subsystem | all crates | No | Sole owner ff-logging |
| logging.* config keys | logging-subsystem (consumer) + configuration-system (owns `logging` namespace) | -- | No | Two-phase init (CR-CH-015, B033); ff-logging inits before ff-config |
| `dev-logging` cargo feature + `BUILD_PROFILE_LEVEL` const (CR-NR-058 Req 13) | logging-subsystem | ALL crates (compile-time gate for their own TRACE/DEBUG) | Watch | UNIMPLEMENTED (PA-CR058). Compile-time gate; downstream crates key their dev diagnostics on the exported const |
| per-command instrumentation at `execute_command` (CR-NR-058 Req 11) | command-framework | all invocation sources (keyboard/menu/cmd-line/macro/plugin) | Watch | UNIMPLEMENTED (PA-INCOMPLETE-004). Uniform start/params/completion at DEBUG; leverages dev-logging gate |
| `Transaction` / `EditOperation` / `UndoStack` / `RedoStack` / `ScrapStack` / `SavePoint` | undo-redo-transactions | command-framework, document-model, edit-operations | No | Sole owner ff-undo-redo (Scintilla model) |
| `SelectionPosition` / `SelectionRange` / `Selection` (LOGICAL model) | edit-operations (SOLE owner) | caret-and-selection (renders), undo-redo (own snapshot) | No | RESOLVED W1.5: sole owner; no duplication |
| `EditorTransaction` (line-snapshot model) vs `Transaction`/`ScrapStack` (undo-redo) | edit-operations + undo-redo-transactions | ff-line-commands PRODUCES EditorTransaction | CONFLICT | PA-CONFLICT-002 CONFIRMED W1.5: two unbridged undo-unit models; EditorTransaction de-facto canonical; undo-redo model unwired (ff-line-commands never uses its ff-undo-redo dep) |
| CAPS / edit profile | edit-operations (Req 16-17) | configuration-system (persistence) | No | Sole owner ff-edit-operations |
| LineCommand / BlockCommand / PendingCommand / CompatibilityMatrix | line-commands | command-semantics (collection step, W3.1) | No | Sole owner ff-line-commands |
| undoable line-command output | line-commands PRODUCES edit-operations `EditorTransaction` | -- | No | W1.6 evidence for PA-CONFLICT-002; ff-undo-redo dep unused (PA-DEP-001) |
| X/XX exclusion visibility | line-commands (consumes) | exclude-show-filter + display-line-mapping (canonical DisplayLineMapping) | No | Confirmed W1.4/W1.6; exclude-show-filter authoritative for SHOW restore (W1.11) |
| `SelectionState` (undo snapshot) | undo-redo-transactions (Req 9) | -- | Watch | PA-WATCH-006: vs edit-operations Selection (W1.5) + caret-and-selection rendering (W1.3) |
| `RecoveryPayload` (serialize-only) | undo-redo-transactions | file-operations/background-io (perform I/O) | No | Crate serialises to bytes+CRC32; caller does the write -- GUI/IO-independent |
| `Viewport` / `CaretPolicy` / `ScrollMode` / `ViewportError` | viewport-and-scrolling | ff-desktop (renderer), editor session | No | Sole owner ff-viewport-scrolling |
| scroll commands (ScrollPageUp/Down etc.) | viewport-and-scrolling | command-framework | No | Non-undoable (Req 10.6) -- consistent with undo-redo Req 10 |
| `DisplayLineMapping` (canonical trait) | display-line-mapping (owner, Req 7.10) | ff-line-commands, ff-exclude-show-filter, ff-desktop | No | Sole owner ff-display-line-mapping; correctly consumed by 3 crates |
| `DisplayLineMapper` (viewport-local) vs `DisplayLineMapping` (canonical) | viewport-and-scrolling (DUPLICATE local) + display-line-mapping (owner) | -- | CONFLICT | PA-CONFLICT-001 CONFIRMED W1.4: viewport defines its own trait, never imports canonical; not bridged; contrary to viewport Req 11.1 |
| `ContractionState` / DocLine / DisplayLine / SubLine | display-line-mapping | consumers | No | Sole owner ff-display-line-mapping |
| Fold-level storage/computation | syntax-highlighting (owner: `Lexer::fold_text`+`FoldContext`+`fold_level_at`, Req 8/15) | display-line-mapping (CONSUMES via API; stores only visibility+expanded+collapsed-label, Req 10.7) | No | CONFIRMED W1.10: clean single-owner boundary. display-line-mapping's 18 `fold_*` refs are ALL `fold_text` = collapsed-label STRING field, NOT level computation. Matches Req 15.1 (query exclusively, never compute). W1.4 PA-WATCH RESOLVED. NB: `fold_text` spelling reused across crates for different things (method vs field) -- coincidental, no collision |
| `SyntaxHighlighter`/`Lexer`/`LexerRegistry`/`StyleBuffer`/`StyleContext`/`WordList`/`HighlightEngine`/`FoldData`/`FoldContext`/`SubStyleAllocator` | syntax-highlighting (`ff-syntax-highlighting`) | theme (slot resolution), display-line-mapping (fold API), viewport (ensure_styled_to), text-decorations (peer) | No | Sole owner; W1.10 -- no duplication. GUI-independent (0 egui/colour refs, Req 11/12.1 verified) |
| `StyleSlotIndex` (u8) as theme boundary | syntax-highlighting (produces) + theme-and-appearance (resolves to attributes) | viewport renderer | No | Engine emits abstract slots only; theme owns colour/font. Clean layering (Req 12) |
| HILITE command (Req 16) | syntax-highlighting (executes modes: ON/OFF/LOGIC/PAREN/FIND) + edit-operations (parses/stores, delegates -- Req 16.12) | command-framework, find-and-replace (HILITE FIND extends highlight-all) | Watch | PA-WATCH: HILITE split across edit-operations(parse/store) and syntax-highlighting(execute); confirm delegation seam at Wave 3/4 command wiring |
| Logical line-exclusion layer (`ExclusionEngine`, EXCLUDE/SHOW/RESET/X commands, blocks, placeholder text) | exclude-show-filter (`ff-exclude-show-filter`) | command-semantics (matrix), line-commands (X/Xn/XX), find-and-replace (scope iterators), viewport (placeholders) | No | Sole owner of exclusion SEMANTICS; W1.11 |
| `DisplayLineMapping` trait consumed for visibility storage | display-line-mapping (owner, W1.4) | exclude-show-filter (CONSUMES CANONICAL correctly, W1.11) | No | POSITIVE counter-example to PA-CONFLICT-001: exclude-show-filter imports `ff_display_line_mapping::DisplayLineMapping` and drives set_visible/get_visible/hidden_lines/show_all -- no local duplicate. Reinforces that VIEWPORT is the outlier that duplicates the trait |
| shared visibility bit: flat exclusion vs nested code-folding | display-line-mapping (arbitrates combined result) | exclude-show-filter (flat, W1.11) + syntax-highlighting/fold (nested levels, W1.10) | Watch | PA-WATCH-010: both write visibility into the same layer; confirm no double-toggle race when a line is both folded and excluded (Wave 4) |
| FIND/CHANGE EXCLUDED\|VISIBLE scope | exclude-show-filter (exposes `visible_lines_iter`/`excluded_lines_iter`) | find-and-replace (consumes) | No | Single owner, many readers -- consistent with W1.8 find-and-replace record |
| `WhitespaceSettings` + mode enums (WhitespaceVisibility/TabDrawMode/IndentGuideMode/EdgeMode/WrapVisualFlag/WrapVisualLocation/WrapIndentMode) + query result types | whitespace-and-guides (`ff-whitespace-guides`) | GUI shell (renderer), command-framework (toggles) | No | Sole owner; W1.12. GUI-independent (0 egui/winit/wgpu, Req 9 verified) |
| wrap sub-line count for wrap-marker computation | display-line-mapping (produces) -> GUI shell -> whitespace-and-guides (consumes as PARAMETER) | -- | No | CLEAN (W1.12): whitespace-guides has NO crate dep on display-line-mapping; `compute_wrap_markers(sub_line_count, ...)` takes the count as a param. "Consumer" relationship realized at caller layer, not a crate edge. Contrast PA-CONFLICT-001 (viewport duplicates the trait) |
| `editor.whitespace_*`/`editor.tab_draw_mode`/`editor.indent_guides`/`editor.edge_*`/`editor.wrap_*` config keys | whitespace-and-guides (consumer) + configuration-system (owns namespace) | -- | No | Sole reader; hot-reload; no key collision with other units |
| Toggle commands (ToggleWhitespace/ToggleIndentGuides/ToggleEdgeColumn) | whitespace-and-guides | command-framework, configuration-system (user-layer persist Req 8.4) | No | Sole owner ff-whitespace-guides |
| `IndentDecision`/`CommentContinuation`/`BlockExpansion` (indent decision data) | auto-indentation (`ff-auto-indent`) | edit-operations/shell (wraps into EditorTransaction) | No | Sole owner; W1.13. Returns plain data; caller applies via edit-operations API (Req 10.4) |
| auto-indent transaction wrapping | auto-indentation PRODUCES decision data -> caller wraps in edit-operations `EditorTransaction` | -- | No | CLEAN seam for PA-CONFLICT-002 (W1.13): ff-auto-indent has NO dep on ff-edit-operations/ff-undo (deps = ff-logging+regex only); decoupled by design (same pattern as sequence-numbers W1.9). Third Wave-1 model crate that stays out of PA-CONFLICT-002 via return-data injection |
| `edit.indent`/`edit.unindent` (Tab/Shift+Tab) | auto-indentation (computes) + command-framework (registers) | edit-operations (applies) | No | Consistent; caller registers, auto-indent computes indent delta |
| `editor.auto_indent`/`editor.indent_size`/`editor.tab_size`/`editor.use_tabs` + `[indent]`/`[comment]` TOML | auto-indentation (consumer) + configuration-system + language-service | -- | No | Read via param injection; no key collision |
| `WrapMode` (None/Word/Character) + `WrapBoundary` (Viewport/Column(n)) + WRAP command (`view.wrap`) | line-wrap-toggle (`ff-wrap`) | display-line-mapping (set_height, caller-bridged), viewport, menu/statusbar, session | No | Sole owner of wrap MODE state; W1.14. Non-undoable, not in history (Req 3.12/3.13). No coupling deps (thiserror+serde only) |
| `WrapIndentMode` (Fixed/Same/Indent/DeepIndent) | line-wrap-toggle (`ff-wrap` indent.rs) + whitespace-and-guides (`ff-whitespace-guides` wrap_indent_mode.rs) -- BOTH define it | -- | CONFLICT | PA-CONFLICT-004 (W1.14): duplicate unbridged enum in two crates; neither imports the other. Recommend whitespace-and-guides as sole owner (its Req 6-7 + line-wrap Req 10.7 "rendered using whitespace-guides infrastructure"); ff-wrap consumes |
| wrap visual flags (None/End/Start/Margin) | line-wrap-toggle (`ff-wrap::WrapVisualFlags` ENUM) + whitespace-and-guides (`ff-whitespace-guides::WrapVisualFlag(u8)` BITFIELD) | -- | CONFLICT | PA-CONFLICT-004 (W1.14): duplicated AND structurally divergent (enum vs bitfield newtype). Unify shape under one owner (whitespace-and-guides recommended) |
| wrap config keys (`[view.wrap]` vs `editor.wrap_visual_flags`/`editor.wrap_indent_mode`) | line-wrap-toggle (`[view.wrap]`) + whitespace-and-guides (`editor.wrap_*`) | configuration-system | CONFLICT | PA-CONFLICT-004 (W1.14): TWO config surfaces for the same wrap indent/flag settings. Unify key ownership with the type ownership |
| wrap-inactive -> no-markers gate | line-wrap-toggle (Wrap None -> set_height 1, Req 6.2/10.5) + whitespace-and-guides (sub_line_count<=1 -> None, Req 6.9) | -- | No | RESOLVED W1.14: the two specs agree; Wrap None collapses every line to height 1, so whitespace-guides' guard fires. W1.12 PA-WATCH cleared |
| `IndicatorStyle`/`Indicator`/`Decoration`/`DecorationList`/`RunStyles`/`LineMarker`/`DecorationRenderer` | text-decorations (`ff-text-decorations`) | find-and-replace (producer), viewport (renderer), theme, display-line-mapping | No | Sole owner; W1.15. `RunStyles` verified UNIQUE (no duplicate in any crate) -- contrast the wrap/CaseFolder conflicts |
| decoration storage vs syntax style buffer (`under` property + layer order) | text-decorations (Decoration/RunStyles) + syntax-highlighting (StyleBuffer) -- SEPARATE stores | -- | No | CONFIRMED clean PEER W1.15/W1.10: independent storage; `under` (Req 2.2) + layer order (Req 14.1) compose them; matches syntax-highlighting Req 15.3-15.5 |
| indicator number namespace (0-7 lexer, 8-31 container, 32-35 IME, 36-43 history) | text-decorations (Req 13) | syntax-highlighting (owns 0-7), plugins (8-31) | No | Clean namespace split; separate from syntax STYLE-SLOT u8 space (no collision) |
| search-match indicators (INDICATOR_SEARCH_CURRENT/ALL) | text-decorations (owns storage) | find-and-replace (fills via fill_range), HILITE FIND (syntax-highlighting Req 16.4) | No | Single owner, producer writes. Ties PA-WATCH-009 (HILITE FIND toggles these) |
| Dataset catalog metadata / DSN / GDG / PDS / `catalog` VfsProvider / StorageProvider / record codecs | dataset-catalog (`ff-dscatalog`) | file-tree-panel, fileforge-integration, ff-idcams (query), ff-dsalloc (CRUD) | No | Sole authority per ADR-001 (dataset-ownership-model). W2.1. STRONG split PA-SPLIT-008 would re-home codecs+storage |
| catalog CRUD primitives vs JCL-driven allocation | dataset-catalog (`ff-dscatalog`, low-level CRUD, ADR-001) | dataset-allocator (`ff-dsalloc`, JCL/DISP workflows -- INVOKES, does not duplicate) | Watch | PA-WATCH-011: verify ff-dsalloc invokes catalog CRUD (W2.3); pre-declared boundary Req 7 |
| `catalog.listcat`/`listds` (workbench-native) vs `idcams.listcat` (IDCAMS-faithful) | dataset-catalog (native tabular) + ff-idcams (formatted, CALLS catalog API) | -- | Watch | PA-WATCH-011: verify ff-idcams delegates to catalog query API (Wave 5); pre-declared Req 13 |
| `StorageProvider` trait + NativeFileProvider + SqliteRecordProvider + record codecs | dataset-catalog today (`ff-dscatalog` storage/ + codecs/) | -- | Watch | PA-SPLIT-008 proposes extracting to `ff-record-codec` (Req 17.1 mandates independence) + `ff-record-store`/`ff-vsam` (Req 18-24); ff-dscatalog then consumes |
| direct fs I/O in a VFS provider | dataset-catalog (`ff-dscatalog`, 49 fs calls) | -- | No | CLEAN (W2.1): this crate IS the terminal `catalog` VfsProvider + NativeFileProvider (Req 10/19.5); FFW-ARCH-001 governs CONSUMERS, not the provider. Contrast PA-WATCH-004 (workflow non-provider raw I/O) |
| `posix` VfsProvider (scheme `posix`) | ff-vfs (`ff-vfs/src/posix_provider.rs`, 664 -- CORRECT home) + ff-desktop (`ff-desktop/src/posix_provider.rs`, 404 -- DUPLICATE, `impl VfsProvider`, `#[allow(dead_code)]`) | virtual-catalog-manager UI | CONFLICT | PA-CONFLICT-005 (W2.2): two posix-scheme provider impls AND a VfsProvider living in the SHELL (layering violation vs FFW-ARCH-001 / VFS ownership W0.11). Delete the ff-desktop copy; VCM consumes the ff-vfs provider via the registry |
| Virtual Catalog Manager UI (Catalog Explorer Context, catalog-manager/dataset-alloc/POSIX dialogs, `Catalog_Registry`) | virtual-catalog-manager (in `ff-desktop`, no dedicated crate) | dataset-catalog (delegatee), file-tree-panel (embed), connector-local-fs (Native) | No | Sole owner (shell UI); W2.2. Delegates mainframe CRUD to ff-dscatalog (ADR-001) |
| catalog mount/registry persistence | virtual-catalog-manager (`[virtual_catalogs]`, all 3 types) + dataset-catalog (`[catalog].mounted_catalogs`, mainframe-only) | configuration-system | Watch | PA-WATCH-012 (W2.2): confirm these are not two competing persistence stores for the same mainframe mounts |
| `CatalogProvider`/`CatalogService` trait (catalog access seam) | dataset-allocator (`ff-dsalloc` defines the trait + MockCatalog) | shell wires production impl -> ff-dscatalog | No | CLEAN (W2.3): PA-WATCH-011 CRUD half resolved. ff-dsalloc delegates all catalog CRUD/resolution/GDG via trait (ADR-001 Req 4.7); no ff-dscatalog dep, no duplicate catalog storage |
| DSN naming validation (`DatasetName::parse` / `validate_dsn`) | dataset-catalog (ADR-001 Req 3.1 SOLE owner: `dsn.rs` + `validate_dsn`) + dataset-allocator (`ff-dsalloc/dsn.rs` OWN `DatasetName::parse`) | -- | CONFLICT | PA-CONFLICT-006 (W2.3): ff-dsalloc duplicates the DSN syntax validation ADR-001 assigns exclusively to ff-dscatalog. Route via CatalogProvider::validate_dsn; delete/justify the local validator. (DD-statement parsing IS legitimately the allocator's) |
| VSAM record ops (KSDS/ESDS/RRDS/LDS) | ADR-001 Req 5 owner = `ff-vsam-services` (trait-only stub today) BUT implementation lives in `ff-dscatalog/src/storage/` | -- | CONFLICT | PA-CONFLICT-007 (W2.4): VSAM impl MISPLACED -- ff-vsam-services exists with the `VsamService` trait (Req 16.7 interim OK) but the code (esds/rrds/isam/sqlite_record 843) is in ff-dscatalog. Migrate the impl into ff-vsam-services. Subsumes PA-SPLIT-008 VSAM half as a MIGRATION (target crate already exists) |
| `CatalogService`/`DynCatalogService`/`VsamService` trait contracts | dataset-ownership-model ADR-001: `ff-dataset-catalog/src/traits.rs` (CatalogService -- INTERFACE crate) + `ff-vsam-services/src/traits.rs` (VsamService) | ff-dsalloc, ff-idcams (consume via trait) | No | W2.4: ff-dataset-catalog is the SHARED-INTERFACE crate (PA-DEP-002 WITHDRAWN -- not a dead stub); ff-dscatalog is the impl |
| ADR-001 dependency-direction fitness function | dataset-ownership-model (`ff-governance-tests/tests/architecture_compliance.rs`, Req 18) | whole catalog cluster | Watch | PA-WATCH-013 (W2.4): the suite checks `ff-dataset-catalog` (interface) but NOT `ff-dscatalog` (impl) or ff-dsalloc -- impl crates could acquire a prohibited dep undetected. Extend coverage |
| record-structure model (`FieldDefinition`/`RecordStructure`/field-type enum) | fileforge-integration (`ff-forge`: `DataType`/`FieldDefinition`/`RecordStructure`, declared owner) + structure-catalog (`ff-structure-catalog`: OWN `FieldType`/`FieldDefinition`/`RecordStructure`, NO dep on ff-forge) | -- | CONFLICT | PA-CONFLICT-008 (W2.5): two unbridged record-structure models across crates the spec says should share; structure-catalog "extends fileforge-integration" in prose but redefines its types. Unify under ff-forge; structure-catalog adds only the catalog library/persistence/.ffs layer |
| `.ffs` structure file format + catalog persistence | structure-catalog (`ff-structure-catalog`, `ffs_format.rs`) | -- | No | Library/persistence concern legitimately structure-catalog's; the record DATA MODEL it persists should be ff-forge's (PA-CONFLICT-008). ff-forge `StructureFile` overlap folds into PA-CONFLICT-008 |
| `catalog.*` config root | structure-catalog (`catalog.locations`/`catalog.active_location`) + dataset-catalog (`[catalog].mounted_catalogs`/`default_hlq`) | configuration-system | Watch | PA-WATCH-014 (W2.5): two different domains (record-structure library vs dataset emulation) share the `catalog` config prefix; confirm no key collision |
| field-type model (3rd parallel enum) | ff-forge `DataType` (owner) / structure-catalog `FieldType` / record-selection-criteria `FieldDataType` (`ff-select`) | -- | CONFLICT | PA-CONFLICT-008 EXTENSION (W2.6): THREE parallel field-type enums across ff-forge / structure-catalog / ff-select, none sharing. ff-select has NO ff-forge dep so cannot delegate COMP-3 decode as its spec requires. Unify under ff-forge |
| Criteria_Store persistence | record-selection-criteria (`ff-select`, raw `std::fs`) -- spec says configuration-system-managed (Req 9.1) | configuration-system (declared, NOT a dep) | Watch | PA-WATCH-015 (W2.6): config mediation BYPASSED -- store spec'd as config-user-layer but implemented with raw std::fs + no ff-config dep; .criteria.json outside VFS. Route through ff-config/ff-vfs or document caller-mediated design |
| Criteria_Scope (FIND/CHANGE restriction) | record-selection-criteria (`ff-select` exposes filter) | find-and-replace (consumes as SearchScope) | No | Single-owner-many-readers; mirrors exclude-show-filter EXCLUDED/VISIBLE (W1.11). Confirm wiring at Wave 5 |
| Display_Artifact_Line (synthetic viewport line) | glossary-unified concept: navigation-commands (COLS/BNDS, W1.7) + exclude-show-filter (Placeholder, W1.11) + tabs-and-mask (TABS/MASK, `ff-tabmask`) -- each rolls its OWN | -- | Watch | PA-WATCH-016 (W2.7): no shared `DisplayArtifactLine` abstraction (grep 0). Concept unified in specs, replicated in code. Consolidation opportunity at Wave 4 (viewport/display-line-mapping owned). Not a hard conflict (each artifact is distinct content) |
| TABS/MASK session-state + commands | tabs-and-mask (`ff-tabmask`) | edit-operations (Tab-key/mask-insert), command-semantics (RESET clears), line-commands (I/In triggers mask) | No | Sole owner; W2.7. Tab-key split with auto-indentation (single-cursor advance vs selection Indent/Unindent, W1.13) via shared Tab_Stop session state |
| minimal-dep + injection-trait seam (ConfigProvider/DocumentContext) | tabs-and-mask (`ff-tabmask`, ff-logging sole dep) | shell wires ff-command/ff-config/ff-language-service/ff-edit-operations | No | CLEAN (W2.7): 3rd Wave-2 unit (after ff-select) using minimal-deps + injection traits; spec "depends on" prose overstates coupling (trait seam, not Cargo dep) |
| Command execution pipeline (parse/normalize/scope/validate/execute/report) + Command_Chain (Req 11) | command-semantics (`ff-command-semantics`) | find-and-replace, exclude-show-filter, navigation-commands, line-commands (all consume its parsing/scope) | No | Sole owner; W3.1. Routes all commands through command-framework registry/dispatch |
| `split_chain` chain parser (Req 11 / Task 28.6) | command-semantics (SHARED pure helper) | menu-workspace fastpath Chained_Path resolver (must reuse) | Watch | PA-WATCH-017 (W3.1): designed single-owner-many-callers; verify menu-workspace fastpath calls the shared helper, not a duplicate. Also UNBUILT (PA-INCOMPLETE-007) |
| CR-NR-057 command chaining | command-semantics (Req 11 chain parse/execute, UNBUILT PA-INCOMPLETE-007) + command-framework (Req 10 Context Navigation Stack, UNBUILT PA-INCOMPLETE-003) | -- | No | INTERDEPENDENT (W3.1): Req 11.5 says Chain_Separator drives only the nav-stack behaviour; the two halves = the CR-NR-057 chaining bundle (Phase DH). Honest [ ] tracking on both |
| HILITE delegation chain -- command-semantics EXCLUSION | -- | -- | No | W3.1 NARROWS PA-WATCH-009: command-semantics has 0 HILITE/CompatibilityMatrix refs; it is NOT in the HILITE chain (edit-ops->syntax->find). Compatibility matrix owned by line-commands (W1.6) |
| `CompletionProvider` trait + completion candidate sources | command-completion (`ff-completion` owns the trait + built-in providers) | plugins (register custom providers) | No | Sole owner; W3.2. Injects command-framework registry / VFS paths / lua-macro names / line-commands / keywords -- read-only consumption, ZERO duplication, 0 fs (VFS caller-supplied). Cleanest cross-unit story W2-3 |
| fuzzy subsequence matcher | command-completion (`ff-completion/src/matching/fuzzy.rs`: fuzzy_match->Option<result>+highlight) + command-palette (`ff-desktop/src/command_palette/fuzzy.rs`: fuzzy_match->bool + fuzzy_score) | -- | CONFLICT | PA-CONFLICT-009 (W3.3): two fuzzy engines for the two ISPF-discoverability features (autocomplete + Ctrl+Shift+P palette), already diverged in signature; ff-desktop has no ff-completion dep. Extract a shared `ff-fuzzy` (or expose ff-completion's) consumed by both |
| Command Palette (Ctrl+Shift+P overlay) | command-palette (in `ff-desktop/src/command_palette/`, no crate) | command-framework registry (reads) | No | Sole owner (shell UI); W3.3. Recent-commands = palette-local session state; execution dispatches through the framework |
| Command Configurator (user-defined commands) + commands.toml store | command-configurator (in `ff-desktop/src/command_config/`, no crate) | command-framework (Command_Target Req 8), shell-command (external exec), menu-workspace (binding) | No | Sole owner (shell UI); W3.4. External process execution DELEGATED to shell-command (Req 19); security inherited from shell.mode (no separate switch) |
| menu-workspace-pattern raw-TOML stores | menu-workspace (`menus/*.toml`, REFERENCE) + command-configurator (`commands.toml`) + ff-select (`.criteria.json`, PA-WATCH-015) | -- | Watch | PA-WATCH-019 CONFIRMED (W3.5): menu-workspace is the reference "menus/ pattern"; 3 user-data TOML stores each with own std::fs loader + hot-reload, bypassing ff-config layers. Assess a shared loader helper. Local config files, NOT VFS documents -- no FFW-ARCH-001 issue. NB menu-workspace DOES log its store errors (unlike ff-select) |
| CR-NR-057 command chaining (Context Navigation Stack + chained paths) -- 3 LEGS | command-semantics Req 11 split_chain/execute_chain (PA-INCOMPLETE-007) + command-framework Req 10 Context Navigation Stack (PA-INCOMPLETE-003) + menu-workspace Req 5 separator-aware Chained_Path (PA-INCOMPLETE-008) | -- | No | INTERDEPENDENT, all UNBUILT + honestly tracked (W3.1/W3.5). Separator (`.`STOP/`;`PUSH) drives the nav stack; shared `split_chain`/navigation-activation helper (PA-WATCH-017, tasks 28.6+DF.3) reused across command-line chains + menu fastpath. Unified Phase DH bundle |
| shared `split_chain` / navigation-activation helper | command-semantics (`split_chain`, to build) | menu-workspace fastpath Chained_Path resolver (to reuse, DF.3) | No | PA-WATCH-017 RESOLVED-INTO-tasks (W3.5): the shared-helper reuse is an EXPLICIT requirement (menu Req 5.5 + tasks 28.6/DF.3), currently unbuilt on both sides. Implement split_chain once; menu fastpath consumes it |
| Menu bar + multi-segment status bar (model) | menu-and-statusbar (`ff-menu`, GUI-independent, no egui) | shell (renders), command-framework (dispatch), ff-plugin (segments/menu ext) | No | Sole owner; W3.6. All menu actions route through command-framework Command_Target (consistent w/ menu-workspace + command-configurator menu family) |
| Reqs 17-19 (Tab Window Chrome / Detachable Tab Windows / ISPF Split-Screen) | menu-and-statusbar SPEC (but NO ff-menu impl) -> actually owned by layout-and-docking + multi-tab-editor (Wave 4) | -- | Watch | PA-WATCH-020 (W3.6): SPEC scope-creep -- CR-appended reqs misfiled in menu-and-statusbar; ff-menu has 0 detach/split code. At Wave 4 confirm impl lives in layout/tab units + relocate/cross-ref the requirements. Spec-hygiene, not code duplication |
| Function/PF key maps + label bar + command history + RETRIEVE | function-keys-and-history (`ff-keys`) | command-framework (dispatch, Command_Target), shell (label bar + overlay render) | No | Sole owner; W3.7. Keys map to Command_Targets (consistent w/ menu/command family) |
| END/RETURN-from-POM navigation | function-keys-and-history (Req 17, handler in ff-desktop shell) | menu-workspace POM/Context model + command-framework Context Navigation Stack (Req 10) | No | PA-INCOMPLETE-009 (W3.7): END/RETURN closing a Workspace vs exiting is nav-stack behaviour; coordinate the Req 17 handler revision with menu-workspace PA-INCOMPLETE-008 + PA-INCOMPLETE-003 |
| command-field overlays (history dropdown / RETRIEVE list / completion popup / palette) | function-keys (Req 10/19) + command-completion (W3.2) + command-palette (W3.3) | -- | Watch | PA-WATCH-021 (W3.7): 4 command-field overlay UIs; confirm shared positioning/keyboard-nav infra at Wave 4 shell (or intentionally separate). Not a conflict; UI-consistency |
| Session/workspace persistence (SessionState + WorkspaceDescriptor + restore) | startup-and-session (`ff-session`) | shell (restore), VCM/ff-dscatalog (tab kinds) | No | Sole owner; W3.9. 37 fs LEGITIMATE (terminal session-persistence layer). PA-SPLIT-011 candidate (session-state vs startup-lifecycle) |
| catalog persistence -- THREE LAYERS | ff-session (WHICH tabs open, descriptors) + VCM (`[virtual_catalogs]` registry) + ff-dscatalog (`[catalog].mounted_catalogs` mounts) | -- | Watch | PA-WATCH-012 REFINED (W3.9): complementary, one direction (session tab -> VCM registry -> ff-dscatalog mount). Confirm startup RESTORE ORDERING (Req 5 + Req 2) does not race/double-restore. Coordination, not a conflict |
| config-loading orchestration (startup init sequence) | startup-and-session (`ff-session` ORCHESTRATES) + configuration-system (`ff-config` OWNS) | -- | No | W3.9: clean -- ff-config owns the config model, ff-session owns the startup init SEQUENCE (two-phase-init pattern, W0.5) |
| Reqs 13/14/19/20 (POM / File-Explorer / TSO-lifecycle) persistence-vs-functionality | startup-and-session (PERSISTENCE/routing slice) vs VCM (File-Explorer W2.2) + menu-workspace (POM W3.5) + shell/cmd-semantics (TSO) | -- | Watch | PA-WATCH-023 (W3.9): ff-session owns the persistence of these workspace kinds (descriptors, Req 21); the functionality is owned elsewhere. Confirm no duplicate functionality. Same spec-scope-creep family as PA-WATCH-020 |
| `WorkspaceState` (named root-dir grouping + workspace file) | workspace-model (`ff-session/src/workspace.rs`) | command-framework (lifecycle cmds), ff-config (scoped settings), Command Palette + Global Search (root set) | No | Sole owner; W3.10. Distinct from SessionState (tabs/geometry). Both in ff-session -> reinforces PA-SPLIT-011 (3 concerns) |
| ff-desktop `shell/` coordinator (WorkbenchShell) | ff-desktop shell module (cross-cutting: dispatch/update/render/chrome) | all shell-wired units | No (size) | PA-STD-042 (W3.10): WORST 400-cap violations in codebase -- commands.rs 1255, update.rs 1011, mod.rs 922, render.rs 873, render_chrome.rs 580. Split per prescribed shell layout. Cross-cutting, not one unit's; pairs w/ VCM PA-STD-025 |
| workspace root set (unblock) | workspace-model (`WorkspaceState`) | command-palette (search scope), global-search (root set) | Watch | PA-WATCH-024 (W3.10): confirm Palette + Global Search actually consume the workspace root set (declared unblock). Wave 5 + palette |
| tab chrome / detach / split / floating windows / tab groups | layout-and-docking (`ff-layout`, OWNER -- Req 2/3/11, 607 refs) | shell (renders); menu-and-statusbar Reqs 17-19 + session Req 14 REFERENCE it | No | PA-WATCH-020 RESOLVED (W4.1): ff-layout is the real owner; menu-statusbar Reqs 17-19 are misfiled spec text (relocate here, no code move). "Detached Workspace" terminology consistent w/ VCM. GUI-independent (no egui) |
| layout serialization + personas | layout-and-docking (`ff-layout`, Req 5/6) | shell (persona switch) | No | Sole owner; W4.1. 10 fs LEGITIMATE (owns layout persistence). Local-store family (PA-WATCH-019 adjacent) |
| Editor tab collection + per-tab state (navigate/pin/MRU/DnD/close/split-view) | multi-tab-editor (`ff-tabs`) | ff-layout (split mechanics via TabGroupId), ff-session (persist contract), command-framework | No | Sole owner; W4.2. Split MECHANICS delegated to ff-layout; NOT ff-tabmask (TABS/MASK commands) |
| "Tab Window Chrome" (Tab_Header + Title_Line + Command_Line) | TRIPLE-SPECIFIED: multi-tab-editor Req 15 + menu-and-statusbar Req 17 + layout-and-docking Req 11 -- implemented ONCE in `ff-desktop/src/shell/render_chrome.rs` | shell renders | CONFLICT | PA-CONFLICT-010 (W4.2): one shell impl, three claiming specs, no owner. Designate ONE owner (recommend multi-tab Req 15 or a shell spec); others cross-ref. Extends/subsumes PA-WATCH-020. Spec-hygiene, no code move |
| file-tree MODEL | file-tree-panel (`ff-file-tree` crate: TreeNode/FileTreeState/filter/sort/keyboard -- ORPHANED, used by no crate) vs ff-desktop panels (`file_explorer_panel.rs` 935 + `files_panel.rs` 1195: own `FileExplorerPanelState`, 0 ff-file-tree refs) | -- | CONFLICT | PA-CONFLICT-011 (W4.3): entire tested model crate abandoned for a parallel shell reimplementation (worst duplication in analysis). Rewire shell onto ff-file-tree (preferred -- relieves PA-STD-025 caps) OR delete orphan. SAME panels as VCM (W2.2) |
| file-explorer/files panels (ff-desktop) -- CONVERGENCE POINT | file-tree-panel (PA-CONFLICT-011) + virtual-catalog-manager (W2.2 PA-STD-025 + PA-CONFLICT-005 posix) | -- | CONFLICT | Three findings converge on the SAME ff-desktop file-explorer/files panels: orphan-model duplication (011), severe cap violations 1195/935 (PA-STD-025), misplaced posix provider (005). Untangle as ONE file-explorer-panel refactor |
| `StyleSlotTable` + all colour groups (Syntax/Decoration/Indicator/Editor/Chrome/FileTree/TabBar/Ui) | theme-and-appearance (`ff-theme`, SOLE owner -- 0 dup) | syntax-highlighting (Req 12, style-slot resolve), text-decorations (Req 15, indicator/marker colours), whitespace-guides, all rendering | No | CLEAN single-owner exemplar; W4.4. Confirms W1.10/W1.12/W1.15 layering (engine emits abstract slots; ff-theme resolves). Counter-example to Wave-2 domain-type fragmentation. GUI-independent (no egui) |
| theme-value validation WARN | theme-and-appearance (`ff-theme`, Req 7.4/7.5) | text-decorations Req 15.8 DELEGATES its theme-value WARN here (W1.15) | No | PA-LOG-030 (W4.4): the mandated invalid-theme-value WARN belongs to ff-theme (currently unimplemented, dead dep); satisfies the text-decorations PA-LOG-011 theme-side delegation |
| high-contrast visual mode vs WCAG contrast verification | theme-and-appearance (`ff-theme` Req 5, high-contrast palette = producer) | accessibility (W4.9, WCAG contrast-ratio verify = verifier) | Watch | PA-WATCH-025 (W4.4): confirm the producer/verifier boundary at accessibility (W4.9) |
| per-editor display-only view state (zoom offset) | view-zoom (`ff-zoom`, ZoomState/ZoomSessionEntry) | ff-session (persist contract), command-framework (ZOOM), ff-menu (status), ff-theme (base font) | No | Sole owner; W4.5. Clean producer/consumer; 0 fs. Same display-only-per-instance pattern as line-wrap (W1.14) |
| hex view/edit + undo edit-actions | hex-display (`ff-hex`: ByteReader trait + returns undo edit-action data) | caller wraps in transaction; find-and-replace (FIND X'...'), ff-forge (structured raw-byte view) | No | CLEAN seam for PA-CONFLICT-002 (W4.6): trait-decoupled, returns old-byte data (like seqnum/auto-indent). ff-logging sole dep. 0 fs (ByteReader) |
| Notification channel (toasts + Event Log + NotificationQueue API) | notification-system (in `ff-desktop/src/notification/`, no crate) | ff-menu status bar (bell integration Req 4), all subsystems (emit events) | No | Sole owner (shell UI); W4.7. COMPLEMENTS status bar (transient msgs) + ff-logging (dev channel) -- distinct channels, no conflict. Toast UI + bell UNBUILT (PA-INCOMPLETE-011) |
| user-facing notification vs dev logging | notification-system (NotificationQueue, user-facing) + logging-subsystem (ff-logging, dev/debug) | emitting subsystems (both) | No | Complementary channels; a subsystem may both notify (user) and log (dev). Emitter should mirror Warn/Error notifications to ff-logging (PA-LOG-033) |
| Plugin Manager Context (list/enable/disable/configure) | plugin-manager-ui (in `ff-desktop/src/plugin_manager_panel.rs`, no crate) | ff-plugin PluginRegistry (drives lifecycle activate/deactivate), ff-session (disabled-plugin persist), ff-config ([plugins.{name}]) | No | Sole owner (shell UI); W4.8. Consumes ff-plugin (W0.13) correctly. Enable/disable ACTIONS + config display UNBUILT (PA-INCOMPLETE-012); registry API ready |
| WCAG contrast verification | accessibility Req 1 -> `ff-theme/src/contrast.rs` (verifier) + ff-theme Req 5 high-contrast palette (producer) | -- | No | PA-WATCH-025 RESOLVED CLEAN (W4.9): verifier + producer both in ff-theme -- single-crate; theme owns colours, contrast.rs validates AA 4.5:1/3.0:1 (ContrastWarning). No split/duplication |
| accessibility (cross-cutting) | distributed: ff-theme (contrast Req1), shell (keyboard Req2, focus Req3, reduced-motion Req5), egui/accesskit (screen reader Req4 -- UNBUILT) | -- | No | W4.9: cross-cutting a11y satisfied by owner crates (like CR-NR-058 logging). Screen-reader UNBUILT (PA-INCOMPLETE-013); full WCAG/SR conformance needs MANUAL assistive-tech testing |
| context-sensitive help (Help Panel + F1 + topic library + plugin help) | context-help (`ff-help`) | command-semantics (HELP cmd Req 7 routes through), ff-layout (docking), ff-plugin (plugin help), ff-config | No | Sole owner; W4.10. Owner(command-semantics dispatch)/executor(ff-help panel) split -- non-undoable, like HILITE + the menu/command family. Plugin help via extensibility (like completion/status-segments) |
| clipboard content model (ClipboardEntry stream/rectangular/multi_caret) | clipboard-operations (`ff-clipboard`) | caller applies paste as edit; command-framework (COPY), ff-vfs (file-insert) | No | CLEAN seam for PA-CONFLICT-002 (W4.11): 0 EditorTransaction/ScrapStack usages -- returns ClipboardEntry data, caller wraps. Content vs selection layer (clipboard segments vs edit-ops selection). 0 raw fs (file-insert via VFS) |
| ff-clipboard over-declared deps | clipboard-operations (declares ff-edit-operations + ff-undo-redo) | -- | Watch | PA-DEP-003 (W4.11): declared but UNUSED (0 EditorTransaction/ScrapStack) -- dead deps contradicting the clean-seam design; prune. Same over-declaration family as dead-ff-logging |
| idle scheduler | idle-processing (`ff-idle-processing`, ORPHAN -- used by no crate) vs syntax-highlighting inline `idle_styling.rs` (own IdleStylingConfig, same 10ms budget) | -- | CONFLICT | PA-CONFLICT-012 (W4.12): orphan standalone scheduler + inline consumer reimplementation. Spec's ONE-cooperative-scheduler premise (slices to syntax/wrap/fold/search) NOT realized -- consumers idle independently. Rewire consumers onto ff-idle-processing OR delete orphan. 2nd orphan-crate finding (w/ ff-file-tree PA-CONFLICT-011) |
| large-file render optimization | large-file-performance (`ff-large-file-performance`, ORPHAN -- used by no crate; shell render path has no inline equiv either) | -- | CONFLICT | PA-CONFLICT-013 (W4.13): THIRD orphan crate. Full toolkit (ChunkRenderer/LineLayoutCache/PositionCache/ScrollPredictor/InvalidationCoordinator/Surface) but ff-desktop deps 0 + 0 layout-cache refs. Spec 60fps>1M-line NOT realized. Wire render path onto it OR reconcile+delete. Large-file layout is the idle scheduler's (PA-CONFLICT-012) prime workload -- one integration wires both |
| external-modification detection | external-modification (`ff-external-mod`, ORPHAN -- used by no crate) | ff-vfs (watching/stat -- USED), document-model, notification-system (unwired), file-operations (placement TBD) | CONFLICT | PA-CONFLICT-014 (W4.14): FOURTH orphan crate + spec never resolved placement ("part of ff-file-operations OR standalone"). Detector (WatchRegistry/ChangeType/ExternalChange/BatchCoalescer/FocusGainedChecker) complete but not wired to shell/document lifecycle/notifications. EXEMPLARY VFS discipline (0 real fs). Resolve placement (keep standalone) + wire. Cross-ref W4.15 |
| ORPHAN-CRATE PATTERN (Wave 4) | ff-file-tree (PA-CONFLICT-011) + ff-idle-processing (PA-CONFLICT-012) + ff-large-file-performance (PA-CONFLICT-013) + ff-external-mod (PA-CONFLICT-014) -- all complete/tested, used by NO crate | -- | CONFLICT | Wave-4 pattern: FOUR well-built infra crates abandoned (file-tree + idle scheduler reimplemented inline; large-file-perf + external-mod neither wired nor reimplemented). external-mod placement was even left UNRESOLVED in its own spec. Recommend rewiring consumers/shell onto the crates (preferred over deletion -- all tested + well-decomposed). NOTE: ff-file-ops (W4.15) is NOT an orphan (consumed by ff-global-search) -- breaks the streak |
| file lifecycle + persistence | file-operations (`ff-file-ops`) | ff-vfs (all I/O), command-framework (dispatch), document-model (is_dirty READ), ff-global-search (consumer) | No | W4.15 CLEAN. Not an orphan (consumed by global-search). EXEMPLARY VFS discipline (0 real fs). CLEAN seam for PA-CONFLICT-002 (reads document.is_dirty, no transactions; Revert=gated reload). Confirms external-mod (PA-CONFLICT-014) built standalone (file-ops has 0 ff-external-mod dep) |
| ff-file-ops over-declared deps | file-operations (declares ff-undo-redo + ff-logging) | -- | Watch | PA-DEP-004 (W4.15): ff-undo-redo declared but UNUSED (Transaction 0) + ff-logging dead. Same over-declaration family as ff-clipboard PA-DEP-003 + the dead-ff-logging cluster; prune |
| JES job engine + SDSF panel framework | jes-emulator (`ff-jes`, FileForgePlugin) | ff-plugin (registers), ff-command, dataset-catalog, connectors; MISSING ff-vfs | CONFLICT | W5.1. PA-SPLIT-013: split engine (Reqs 1-15) vs SDSF (Reqs 16-18) -- file seam matches, 2 files over cap. PA-CONFLICT-015: job-queue persists via RAW std::fs (queue.rs) -- FIRST raw-fs violation, no ff-vfs dep. NOT an orphan (plugin, wired by name 19x). TCR exemplary (74) |
| VFS discipline (raw-fs violations) | jes-emulator (`ff-jes` queue.rs raw std::fs) VS exemplary external-mod + file-ops + idcams (0 real fs) | ff-vfs (the mandated layer, FFW-ARCH-001) | CONFLICT | PA-CONFLICT-015 (W5.1): first raw-fs VIOLATION found (JES queue). idcams (W5.2) is CLEAN (0 raw fs, uses injected IdcamsServices). Watch remaining Wave-5 emulators/tools for the same bypass |
| IDCAMS emulator engine | idcams-emulator (`ff-idcams`, ORPHAN -- 0 refs, no FileForgePlugin) | command-framework (Req 20.1 registration -- UNBUILT), ff-jes (EXEC PGM=IDCAMS batch step, Req 20.4 -- UNBUILT) | CONFLICT | PA-CONFLICT-016 (W5.2): orphan + missing DUAL integration the spec explicitly designs. ff-jes has 0 idcams refs -> the JES-runs-IDCAMS coupling is absent. Register with command framework + wire JES batch-step. Engine complete (execute_idcams/CommandExecutor/IdcamsServices) |
| emulator integration (JES <-> IDCAMS) | jes-emulator (`ff-jes`) + idcams-emulator (`ff-idcams`) | -- | CONFLICT | The two mainframe emulators do not reference each other (ff-jes 0 idcams refs). IDCAMS is designed to run as a JES batch step (EXEC PGM=IDCAMS) but the coupling is unbuilt. Part of PA-CONFLICT-016; wire together |
| JCL resolution / parsing | ff-dsalloc (dataset-allocator: 4-stage JCL symbolic resolver, lint codes, [jcl] config) + ff-jes/ffjcl.rs (FFJCL job-def AST parser) | jcl-resolver sub-project (EMPTY stub -- meant to own/unify JCL but never populated) | CONFLICT | PA-CONFLICT-017 (W5.3): JCL handling fragmented across two crates; empty jcl-resolver stub. Docs (readiness-summary) INACCURATELY claim scope folded into FFW-JES Req 11 (= Dataset Catalog Integration, not JCL) -- PA-TRACK-005. Distribute+delete stub+fix docs OR populate as unifying home |
| database IDE (DB access, panels) | database-tool (`ff-database-tool`, REGISTERED plugin) | ff-command/ff-vfs/ff-layout (Reqs 15/16/17 -- deps ABSENT, unbuilt); egui/sqlx/tokio (absent) | Watch | W5.4: PA-INCOMPLETE-014 -- spec is a full DB IDE (17 reqs) but impl is a foundation skeleton (thiserror+serde only). Integrations SPEC'd but unbuilt. NOT an orphan (wired 5x). When the DB file I/O lands, use ff-vfs (avoid the JES raw-fs bypass PA-CONFLICT-015) |
| toolchain plugin extension point | ff-toolchain-api (ToolchainPlugin trait + Diagnostic/BuildProfile/ToolchainState) | ff-gcc-toolchain + ff-rust-toolchain (plugins), ff-desktop (consumer, 99 refs) | No | W5.5 CLEAN + POSITIVE exemplar. Fully wired (counter-example to Wave-4/idcams orphans). MockToolchain testable seam (38 TCR rows). Mirrors JES JobProvider + connector provider patterns. Only test std::fs (OS compilers outside VFS -- not a bypass). Caps: gcc lib.rs 421 + rust lib.rs 462 (PA-SPLIT-014) |
| headless batch execution | batch-execution (ff-desktop batch/cli.rs + batch/runner.rs + fftest_cli.rs) | ff-command-semantics (parse/dispatch), ff-shell (output capture), ff-workflow (sequencing) | No | W5.6 CLEAN + POSITIVE LOGGING exemplar. No dedicated crate (headless mode of ff-desktop). REUSES the command pipeline (no dup). BatchRunner genuinely uses ff_logging (run-start/abort/per-cmd RC+duration/RC>=8 ERROR). One open task: --batch-log file sink (PA-INCOMPLETE-015) |
| ASA print-preview model | asa-report-preview (`ff-asa`) | command-framework (PREVIEW cmd, Req 3), document-model (strip/restore Req 7), shell/ff-layout (renders panel.rs model) | No | W5.7 CLEAN + WIRED (173 external refs). Clean-seam panel: NO egui dep -- panel.rs is a page-layout/shading DATA MODEL the shell draws (GUI-independent, like the clean crates). Printer profiles (IBM 1403/3800/4245) sole-owned. Minor: U+00D7/em-dash in display strings (PA-STD-061) |
| code-execution security gate (2 surfaces) | shell-command (shell.mode, PA-WATCH-018) + lua-macro-engine (`ff-lua` SecurityMode, engine.rs:84) | -- | No | W5.8 POSITIVE: BOTH untrusted-code-execution surfaces (external shell + Lua macros) enforce a mode gate with a SAFE default before running. Lua: Disabled/Prompt/TrustedOnly/Enabled, gated at execution. Consistent security posture -- exemplar pair |
| macro/FFCMD command chaining | lua-macro-engine (`ff-lua` Req 5.8-5.11, tasks 26.x OPEN) | command-semantics (shared chain executor Req 11.4), CR-NR-057 Phase DH bundle | CONFLICT (open) | W5.8: lua-macro FFCMD-sequence chaining is ANOTHER LEG of the CR-NR-057 bundle (w/ command-framework Req10, command-semantics Req11, menu-workspace Req5, function-keys Req17). Reuse the shared chain executor -- do NOT build a separate one. Fold into Phase DH (PA-INCOMPLETE-016) |
| language detection / definition | language-service (`ff-language-service`, ORPHAN) VS ff-syntax-highlighting (reimplements detection INLINE) | spec names syntax-highlighting as the consumer, but it has 0 dep on the crate | CONFLICT | PA-CONFLICT-018 (W5.9): FIFTH orphan. Foundation crate its STATED consumer ignores + duplicates inline. 2nd foundation crate syntax-highlighting ignores (also idle PA-CONFLICT-012). Rewire syntax-highlighting onto ff-language-service (delete inline detection) OR delete/absorb orphan |
| syntax-highlighting ignores TWO foundation crates | ff-syntax-highlighting (reimplements idle-styling AND language-detection inline) | ff-idle-processing (PA-CONFLICT-012 orphan) + ff-language-service (PA-CONFLICT-018 orphan) | CONFLICT | Cross-cut: syntax-highlighting was designed to consume BOTH ff-idle-processing (idle budget) and ff-language-service (detection) but reimplements both inline, leaving both orphaned. Rewiring both onto their foundation crates is one coherent effort (same consumer) |
| file-viewer framework vs point crates | custom-file-viewers (`ff-viewers` framework, ORPHAN) | ff-asa (wired ASA preview, 173 refs) + ff-hex (hex-display) -- DUPLICATED, not wrapped | CONFLICT | PA-CONFLICT-019 (W5.10): SIXTH orphan. ff-viewers ships own asa_report.rs/hex.rs/csv/image (NO dep on ff-asa/ff-hex) -> 2 ASA paths + 2 hex paths. Framework orphaned; point ff-asa wired. Adopt framework wrapping point crates OR delete orphan |
| PREVIEW command owner | custom-file-viewers (`ff-viewers` Req 3, 105 refs) + asa-report-preview (`ff-asa` Req 3) | -- | CONFLICT | PA-CONFLICT-019 (W5.10): TWO owners of the PREVIEW verb (viewer framework + ASA crate). Cousin of shell/configurator + TSO-verb ambiguities. Pick one owner; the framework should own PREVIEW and dispatch to registered viewers |
| line diff / merge engine | compare-and-merge (`ff-compare-merge`) | COMPARE command, theme (Req 5), clipboard (Req 15), document saved version (Req 14) | No | W5.11 CLEAN + WIRED (156 refs). Single diff/merge owner (Myers/Patience LCS + 3-way, no duplication). Clean-seam views (no egui dep). VFS-aware BY INTERFACE (URI CompareSource, 0 raw fs). Note: session.rs is misnamed (it IS the DiffEngine, 557 lines) -- PA-STD-066 |
| cross-file search + replace | global-search (`ff-global-search`) | ff-find-and-replace (match engine, USED -- good), ff-file-ops (DECLARED but UNUSED -- dead) | CONFLICT | W5.12: REUSES ff-find-and-replace (no dup -- positive). PA-CONFLICT-020: cross-file REPLACE writes RAW std::fs (replace.rs:60/91) bypassing ff-file-ops atomic/backup safe-write -- risky bulk mutation. PA-DEP-005: ff-file-ops dead dep -> CORRECTS W4.15 "consumed" claim (declaration-only) |
| ff-file-ops actual consumers (W4.15 correction) | file-operations (`ff-file-ops`) | global-search DECLARES but does NOT use it (raw std::fs instead) | Watch | W5.12 correction to W4.15: ff-file-ops' only declared consumer (global-search) is a DEAD dep (0 `ff_file_ops` refs). So the W4.15 "not an orphan (consumed by global-search)" conclusion was declaration-only. Routing global-search replace through ff-file-ops (PA-CONFLICT-020) would make it true |
| connector plugin framework | connector-extensibility (`ff-connector-extensibility`) | INTENDED implementers = DEFERRED remote connectors (network-fs/ftp-sftp/cloud/mainframe); local-fs implements ff-vfs directly by design | Watch (was CONFLICT) | PA-CONFLICT-021 (W5.13) REFINED by W5.15: NOT an abandoned orphan. network-fs spec states future connectors implement the connector-extensibility trait (VfsProvider + lifecycle + auth + capability); local (initial release) uses ff-vfs directly. So the framework is an INTENTIONAL base built ahead of its DEFERRED implementers. DOWNGRADE to LOW-MED/WATCH: keep it, note implementers are deferred, re-confirm API when first remote connector lands. Crate DOES log (positive) |
| deferred remote connectors (network/ftp/cloud/mainframe) | connector-network-fs + connector-ftp-sftp + connector-cloud + connector-mainframe (ALL spec-only, no crate, DEFERRED) | ff-connector-extensibility (trait they will implement), ff-vfs | No | W5.15-18: FOUR honestly-deferred future specs (design.md "DEFERRED", reqs+design but no tasks.md). Reveal the intended connector architecture (remote = extensibility trait; local = ff-vfs directly). PA-TRACK-006: mark deferred specs clearly. PA-STD-071: design.md mojibake STATUS lines |
| LOCAL mainframe emulation vs REMOTE connectivity | dataset-catalog + JES (ff-jes) + IDCAMS (ff-idcams) = local emulation (BUILT) | connector-mainframe = real z/OS connectivity (DEFERRED) | No | W5.18 POSITIVE: explicit, well-designed boundary -- emulate the mainframe LOCALLY (initial release) vs CONNECT to a real z/OS (future). Complementary, non-duplicative roles (contrast ff-viewers/ff-asa duplication W5.10). Exemplar of correct scope separation |
| EBCDIC conversion | ff-encoding (encoding-and-characters, 81 EBCDIC refs -- OWNER) VS ff-forge (own ebcdic.rs, 95 refs, 0 dep on ff-encoding) | -- | CONFLICT | PA-CONFLICT-022 (W5.15/row76): ff-forge reimplements EBCDIC instead of reusing ff-encoding. Reuse OR document flat-file-specific divergence + share low-level tables |
| ASA carriage control (THREE implementations) | ff-asa (wired ASA preview) + ff-viewers/asa_report.rs + ff-forge/asa.rs | -- | CONFLICT | PA-CONFLICT-019 (W5.10) + PA-CONFLICT-022 (W5.15/row76): ASA now implemented in THREE crates, none sharing. Consolidate onto ff-asa (the wired preview owner); others depend on it |
| UI-test framework | automated-dialog-testing (`ff-fftest`) | ff-desktop (automation IDs, 37 refs), batch fftest_cli (W5.6), governance tests | No | W5.19 CLEAN + POSITIVE. Egui-DECOUPLED (drives UI via AutomationId model, not egui internals). EXEMPLARY TCR (62). 7 fs = legitimate test artifacts (reports/screenshots), not VFS violation. Sole owner of FFTest; batch CLI is thin reuse. Cap: parser.rs 406 (PA-STD-073) |
| master narrative vs analysis | project-master (readiness-summary + sub-project-audit + validation docs) | project-analysis/ (checklist + registers -- the current findings) | CONFLICT | PA-TRACK-007 (W6.1): master narrative STALE -- 0 analysis refs in readiness/audit/validation (only tasks.md has the PA-W phases). Presents pre-analysis "all ready" picture, omits 6 orphans/database-tool false-complete/duplication/data-safety. Reconcile: master should point to project-analysis as source of truth for outstanding work |
| primary VFS provider (fs layer) | connector-local-fs (`ff-connector-local-fs` = LocalFsProvider) | ff-vfs VfsProvider trait (implements), ff-logging (used) | No | W5.14 CLEAN. Correct layering: centralises OS-fs (51 legit fs usages, async tokio) behind VfsProvider. WIRED (7 refs), logs (9). The LEGITIMATE home for raw fs -- contrast JES data-store bypass PA-CONFLICT-015. Cap: provider.rs 542 (PA-STD-069) |
| ORPHAN-CRATE cluster (whole-project) | 6 orphans: ff-file-tree, ff-idle-processing, ff-large-file-performance, ff-external-mod, ff-language-service, ff-viewers (+ idcams unwired-engine) | -- | CONFLICT | Running tally across Waves 4-5. Sub-flavors: (i) complete-but-unwired (file-tree/idle/large-file/external-mod); (ii) foundation-crate-ignored-by-consumer (language-service); (iii) framework-orphaned-while-point-solutions-win (viewers). NOTE: ff-connector-extensibility REMOVED from this list by W5.15 -- it is an INTENTIONAL base for DEFERRED remote connectors, not an abandoned orphan (PA-CONFLICT-021 downgraded). DOMINANT structural theme -> Wave-6 headline |
| shell.mode external-execution security gate | shell-command (`ff-shell` owns `shell.mode`, Req 2) | command-configurator (inherits the gate via delegation) | No | PA-WATCH-018 RESOLVED CLEAN (W3.8): first-class ENFORCED gate (disabled/prompt/enabled, safe prompt default, macro dual-gate Req 2.6/2.7) checked at engine entry BEFORE all 13 spawn sites; configurator's delegated exec inherits it. Exemplary security control |
| SHELL/TSO external-execution engine | shell-command (`ff-shell`: Command::new spawn, capture, stdin-pipe, terminal + emulator) | command-configurator (delegates external exec), command-framework (SHELL/TSO command) | No | Sole owner; W3.8. ACTUALLY LOGS (10 calls). Output to ff-document-model, progress via ff-workflow, Output_Panel via ff-layout |
| `TSO` verb | shell-command (`TSO` = SHELL alias, run OS command) + command-semantics (Req 9/10 emulated mainframe TSO P1/P2: OUTPUT/CANCEL/SEND/PROFILE/PRINTDS) | -- | Watch | PA-WATCH-022 (W3.8): two meanings of `TSO`; confirm the verb routing is disambiguated (which handler wins on `TSO ...`) |
| scroll-amount: commands (M/MAX/n) vs field (CSR/PAGE/HALF) | navigation-commands (Req 20 commands, DELEGATES) + viewport-and-scrolling (Req 14 field + state/clamping) | -- | No | PA-WATCH-008 RESOLVED W1.7: single source of truth; nav delegates viewport state; complementary layers |
| UP/DOWN/LEFT/RIGHT/TOP/BOTTOM/LOCATE/SORT/COLS/BOUNDS commands | navigation-commands | command-framework, viewport-and-scrolling (delegate) | No | Sole owner ff-navigation-commands |
| active Bounds state + query API (Req 5.15) | navigation-commands (owner) | line-commands (bounds-aware shift), find-and-replace (CHANGE/FIND), SORT | No | Single owner, multiple readers -- consistent |
| `FindEngine` / `FindRequest` / `FindResult` / `RegexEngine` / `FindState` | find-and-replace | command-semantics, exclude-show-filter, ff-desktop | No | Sole owner ff-find-and-replace |
| `CaseFolder` / `ICaseConverter` | encoding-and-characters (declared owner, Req 10) + find-and-replace (DUPLICATE own impl) | -- | CONFLICT | PA-CONFLICT-003: find-and-replace defines its own CaseFolder instead of consuming ff-encoding's via ICaseConverter; not bridged; contrary to its own cross-ref |
| `RegexEngine` (NFA) | find-and-replace | -- | No | Sole owner; PA-SPLIT-007 proposes extracting to `ff-regex` |
| `ColumnRange`/`SequenceFormat`/`DetectionResult`/`SeqNumState`/`SideTableEntry`/`SeqNumConfig`/`NumberEngine`/`SeqNumIndicator` | sequence-numbers (`ff-seqnum`) | ff-desktop (renderer/statusbar), viewport (overlay) | No | Sole owner; W1.9 -- no duplication found |
| `UndoRecorder` (trait) / `ColumnChange` | sequence-numbers (defines trait) | shell supplies concrete recorder | No | CLEAN seam for PA-CONFLICT-002 (W1.9): ff-seqnum does NOT bind to `Transaction` or `EditorTransaction`; concrete transaction model injected at runtime. Shell wiring must back it with the winning model |
| `sequence.unnum`/`sequence.number`/`sequence.number_show` (+ NUM/AUTONUM aliases) | sequence-numbers | command-framework (registry), command-semantics (matrix Req 14) | No | Sole owner ff-seqnum |
| `editor.sequence_numbers.*` config keys | sequence-numbers (consumer) + configuration-system (owns namespace) | -- | No | Layered override model; consistent with config-system ownership |
| Caret/selection VISUAL config (CaretStyle, SelectionColours, BlinkState) | caret-and-selection | ff-desktop (renderer), theme | No | Sole owner ff-caret-selection -- RENDERING only |
| `Selection` / `SelectionRange` / `SelectionPosition` (LOGICAL model) | edit-operations (owner, W1.5) | caret-and-selection (renders; does NOT duplicate -- W1.3 verified), undo-redo (own snapshot) | Watch | PA-WATCH-006: confirm undo-redo SelectionState references it at W1.5 |
| `CommandId` / `CommandParams` / `CommandResult` / `CommandRegistry` / `ExecutionContext` | command-framework | all crates | No | Sole owner ff-command |
| `ShortcutBinding` / `Chord` | command-framework | ff-desktop | No | Sole owner ff-command |
| `CommandTarget` (5 variants, incl. produces_visible_workspace) | command-framework | menu-workspace, command-configurator, startup-and-session, shell-command, lua-macro-engine | Watch | PA-WATCH-001: verify Target_Resolution + Req 10.10 predicate reuse |
| Context_Navigation_Stack + `navigation.stack_max_depth` (CR-NR-057 Req 10) | command-framework | startup-and-session, menu-workspace, function-keys-and-history; paired w/ command-semantics Req 11 | Watch | PA-INCOMPLETE-003 (unbuilt) + PA-WATCH-001; config key not yet registered |
| reserved `arg` param key + verb/arg split (Req 9) | command-framework | all input sources | Watch | Req 9 DEFINED but UNIMPLEMENTED (PA-INCOMPLETE-001) |
| `Document` / `DocumentHandle` / `TextBuffer` / `GapBuffer` / `LineIndex` | document-model | edit-operations, display-line-mapping, viewport, undo-redo, ff-desktop | No | Sole owner ff-document-model |
| `BytePosition` / `LineNumber` / `CharacterExtracted` / `DocumentWatcher` | document-model | edit/nav consumers | No | Sole owner ff-document-model |
| `LineEndMode` | document-model (owns) + encoding-and-characters (validates bytes) | -- | No | No duplicate ownership |
| VFS-only I/O (Req 4.8) | document-model (consumer) + virtual-file-system (owner) | -- | No | VERIFIED no std::fs/tokio::fs (FFW-ARCH-001) |
| `Vfs` / `VfsProvider` / `StorageProvider` / `ProviderRegistry` / `ResourceUri` / `VfsError` | virtual-file-system | ALL crates (FFW-ARCH-001) | No | Sole owner ff-vfs; only crate allowed direct std::fs |
| `WatchHandle` / `WatchEvent` / `VfsTransaction` | virtual-file-system | document-model, external-modification, dataset-catalog | No | Sole owner ff-vfs |
| workspace backup manifest | virtual-file-system (Req 12.2) + dataset-catalog (ff-dscatalog Req 26.3) | -- | Watch | PA-WATCH-003: verify shared-vs-distinct manifest type at Wave 2 |
| `FileForgePlugin` / `PluginContext` / `PluginError` / `Capability` / `PluginRegistry` / `PLUGIN_API_VERSION` | plugin-architecture | all plugin crates, shell | No | Sole owner ff-plugin |
| plugin namespace `[plugins.{name}]` | plugin-architecture (Req 2.7/7.5) + configuration-system (Req 8) | -- | No | Same scoping rule both specs |
| PluginLogHandle contract | plugin-architecture (uses) + logging-subsystem (Req 10 owns) | -- | No | Consistent `[plugin:name]` prefix |
| `Workflow` / `WorkflowDefinition` / `WorkflowRunner` / `WorkflowRegistry` / `Checkpoint` | workflow-engine | command-framework, plugins, shell | No | Sole owner ff-workflow |
| `CancellationToken` (workflow) | workflow-engine | steps, async I/O | No | Distinct from ff-background-io cancellation (W0.17) |
| checkpoint storage I/O | workflow-engine (`tokio::fs`) | virtual-file-system (would-be owner) | Watch | PA-WATCH-004: direct tokio::fs vs FFW-ARCH-001 -- owner decision |
| `IoError` / `ProgressState` / `IoTaskHandle` / `BackgroundIoService` / `RetryPolicy` | background-io | file-operations, document-model, shell | No | Sole owner ff-background-io (RetryPolicy defined but NOT wired -- PA-INCOMPLETE-002) |
| `IoCancellationToken` | background-io | callers | No | Distinct from ff-workflow CancellationToken |
| VFS-only I/O (Req 1.8/4.10/8.x) | background-io (consumer) + virtual-file-system (owner) | -- | No | VERIFIED upheld (only lib.rs doc-comment mentions std::fs) |
| `EncodingFamily` / `CharClassify` / `CharacterCategoryMap` / `CaseFolder` | encoding-and-characters | document-model, find, edit, nav | No | Sole owner ff-encoding |
| `ConversionResult` / `ConversionIssue` | encoding-and-characters | file-operations, document-model | No | Sole owner ff-encoding; lossy replacements returned to caller (Req 3.4) |
| LineEndMode interaction | encoding-and-characters (validates U+2028/2029/0085) + document-model (owns LineEndMode) | -- | No | ff-encoding validates bytes; document-model decides semantics -- no duplicate ownership |

## Command IDs

| Command_ID | Owner unit | Referencing units | Conflict? | Resolution |
|------------|-----------|-------------------|-----------|------------|
| _(none recorded yet)_ | | | | |

## Configuration Keys

| Config key | Owner unit | Referencing units | Conflict? | Resolution |
|------------|-----------|-------------------|-----------|------------|
| _(none recorded yet)_ | | | | |

## Panels / Contexts

| Panel/Context name | Owner unit | Referencing units | Conflict? | Resolution |
|--------------------|-----------|-------------------|-----------|------------|
| _(none recorded yet)_ | | | | |

## Glossary Terms

| Term | Owner unit | Definition source | Conflict? | Resolution |
|------|-----------|-------------------|-----------|------------|
| _(none recorded yet)_ | | | | |

## Dangling Cross-References

| From unit | Missing target | Correction |
|-----------|----------------|------------|
| _(none recorded yet)_ | | |
