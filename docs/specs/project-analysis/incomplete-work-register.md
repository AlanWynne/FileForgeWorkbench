# Incomplete Work Register

Consolidated, living backlog of every pending/incomplete task, orphaned
requirement, NOT-COVERED TCR row without a task, tracking-marker fix, and logging
gap found during the analysis (Requirement 4.3, 5.5).

This EXTENDS `docs/specs/ears-integration/incomplete-work-audit.md` (EI-3) rather
than replacing it. EI-3 established the pending phases and inconsistencies as of
that audit; this register carries them forward and adds new findings per
sub-project, then produces a dependency-ordered sequence in Wave 6.

Status: SEEDED from EI-3 (re-baselined after CR-NR-057, commit `b537737`);
populated per sub-project during Waves 0-5.

Item kinds: PENDING-TASK, ORPHAN-REQ (requirement with no task),
TCR-GAP (NOT COVERED row with no task), TRACKING-FIX (marker correction, no gate),
LOGGING-GAP (missing/inadequate logging, fix under code mode), SUPERSEDED.

---

## Seed rows carried forward from EI-3 (ears-integration/incomplete-work-audit.md)

| ID | Kind | Unit | Item | Status per EI-3 | Notes |
|----|------|------|------|-----------------|-------|
| IWR-001 | PENDING-TASK | dataset-catalog | BS.8-BS.15 (staged txns, integrity/backup, audit trail, security, hierarchy, editor integration, non-functional, design update) | Wave 3-4 pending | Re-verify against current tasks.md/TCR during Wave 2 analysis |
| IWR-002 | PENDING-TASK | virtual-catalog-manager | BU.2-BU.9 (SQLite catalog integration) | pending; needs BS.4 (done) + BS.8 | Re-verify during Wave 2 |
| IWR-003 | PENDING-TASK | dataset-catalog | BV.1 (CatalogLocation refactor) | pending; no deps | EI-3 says can start immediately; CR-NR-017 marked DONE in change-log -- reconcile during Wave 2 |
| IWR-004 | SUPERSEDED | virtual-catalog-manager | VCM Task 17 (dataset file creation on first open) | superseded by BU.7 | Confirm marker note present |
| IWR-005 | RESOLVED | virtual-file-system | ff-vfs Req 9-12 (StorageProvider, POSIX native, staged txns, backup/restore) had no tasks | RESOLVED (W0.11): tasks 13-16 exist and are `[x]`; backing code present; 21 TCR rows Req 9.1-12.5 PASS. EI-3 predated this work. | Verified Wave 0; no further action |
| IWR-006 | TRACKING-FIX | dataset-catalog | task 21 parent `[ ]` but subtasks 21.1-21.4 `[x]` | doc fix | Apply during Wave 2 |
| IWR-007 | TRACKING-FIX | project-master | BU.1 marker `[ ]` but BU.D1-BU.D4 `[x]` | doc fix | Reconcile during Wave 2/6 |
| IWR-008 | TRACKING-FIX | project-master / TCR | duplicate Phase BD `NOT COVERED` table in TCR.md | doc fix | Apply during Wave 4 (file-tree-panel) or 6 |
| IWR-009 | PENDING-TASK | compiler-toolchain-integration | Tasks beyond 5.1-5.3 (MockToolchain done; later tasks) | `[~]` in project-master | Re-verify during Wave 5 |

> Note: EI-3 predates several later phases (DB, DC-DE, CR-CH-015/016, CR-NR-054,
> CR-NR-057). Newer pending work (e.g. CR-NR-054 DF.7-DF.10 code, CR-NR-057
> command chaining / Context Navigation Stack, command-configurator Task 3
> desktop-adapter follow-ups, dockable Output_Panel view) will be added as their
> owning sub-projects are analysed in the relevant waves.

---

## New findings (added per sub-project during analysis)

| ID | Kind | Unit | Item | Proposed action | Wave |
|----|------|------|------|-----------------|------|
| PA-STD-001 | REFACTOR (400-line cap) | platform-core (`ff-core`) | `event_bus.rs` is 435 non-test lines, over the cap (lifecycle.rs 360 near). | Split event_bus.rs by concern (dispatch/subscribe; keep EventBus core). REFACTOR, no gate. | 6 (consolidation) |
| PA-DOC-001 | TRACKING-FIX | platform-core | Req 4.1 layer-membership lists use illustrative crate names (`ff-document`, `ff-edit`, `ff-undo`, `ff-viewport`, `ff-display-lines`, `ff-find`, `ff-nav`, `ff-exclude`) differing from actual crates (`ff-document-model`, `ff-edit-operations`, `ff-undo-redo`, `ff-viewport-scrolling`, `ff-display-line-mapping`, `ff-find-and-replace`, `ff-navigation-commands`, `ff-exclude-show-filter`). | Owner decision: reconcile to actual crate names OR annotate as illustrative. Doc-only, no gate. | 6 (consolidation) |
| PA-LOG-001 | LOGGING-GAP (ASCII, project-wide) | platform-core (`ff-core`) + project-wide | `.rs` sources contain non-ASCII in comments AND log-message string literals (em-dashes, box-drawing separators), violating rust-standards.md / documentation.md (Rust = plain ASCII only). ff-core: 322 matches incl. em-dashes in shutdown.rs:105 / lifecycle.rs:241/344 log strings (render in log file). ff-config: 641 matches. | Schedule ONE project-wide code-mode cleanup pass replacing non-ASCII in `.rs` with ASCII (`--`, `===`), EXCLUDING genuine Unicode test/data literals; do not fix per-unit. Low severity. | later (dedicated) |
| PA-TRACK-001 | TRACKING-FIX | configuration-system (`ff-config`) | Phase CQ tasks 30 (Req 16 Audit) and 31 (Req 17 Export/Import) remain `[ ]` (17 subtasks) although `audit.rs` (377) + `export_import.rs` (356) exist and TCR rows Req 16.1-16.6/17.x are PASS. | Owner action: check tasks 30.1-31.7. Bookkeeping only; no code, no gate. | 6 (consolidation) |
| PA-SPLIT-001 | SPLIT PROPOSAL | configuration-system | Spec meets 4/4 split criteria (365 req lines, 13 reqs, 2 crates, engine vs enterprise/UI bands). | Split SPEC into core (Req 1-9) + `configuration-enterprise` (Req 16-18); fold Settings UI (Req 15, 18.6) into `menu-workspace`. Crate split NOT recommended. Owner approval + own gate. | later (owner-gated) |
| PA-STD-002 | REFACTOR (400-line cap) | configuration-system (`ff-config`) | Six non-test files over the cap: `config_handle.rs` (809), `editorconfig/parser.rs` (626), `reload.rs` (510), `access.rs` (486), `init.rs` (446), `plugin_handle.rs` (432). | Split each by concern. REFACTOR, no gate. | 6 (consolidation) |
| PA-DOC-002 | TRACKING-FIX | configuration-system | requirements.md requirement numbering skips Req 10-14 (jumps 9 -> 15). | Confirm 10-14 allocated elsewhere; add a one-line note to the spec Introduction. Doc-only, no gate. | 6 (consolidation) |
| PA-TRACK-002 | TRACKING-FIX | logging-subsystem (`ff-logging` / `tools`) | Req 12 Logging Inventory Tool implemented + verified deterministic (tool `tools/python/logging_inventory.py` + report `docs/quality/logging-inventory.md` present; Property 12 holds), but tasks 23-24 remain `[ ]` and TCR Phase DD rows Req 12.1-12.5 remain NOT COVERED. | Owner action: check tasks 23.1-24.3; set Phase DD TCR rows to PASS. Bookkeeping, no code, no gate. | 6 (consolidation) |
| PA-LOG-002 | LOGGING DATA (project-wide) | all crates | `docs/quality/logging-inventory.md` reports 56/69 crates with ZERO log call sites and 792 silent-error candidates. | Authoritative input for the project-analysis logging audit (Req 5). Reference per-unit rather than re-scanning; owner decides which zero-log crates need instrumentation. | later (per-unit + owner-gated) |
| PA-STD-003 | REFACTOR (400-line cap) | logging-subsystem (`ff-logging`) | `init.rs` is 640 non-test lines, over the cap. | Split by concern (init_bootstrap / init_writer; keep init.rs entry points). REFACTOR, no gate. | 6 (consolidation) |
| PA-INCOMPLETE-001 | INCOMPLETE (real code gap, gate complete) | command-framework (`ff-command` + `ff-desktop`) | Req 9 Command Arguments (CR-NR-054) unimplemented: 6 Phase DF tasks open, TCR Phase DF red, no `parse_invocation`/`CommandInvocation`/reserved `arg` param. The `DOWN 8` / `MENU SETTINGS EDITOR` / `RETRIEVE LIST` key-forwarding feature. | Requirements gate complete. Owner: schedule Phase DF (TDD). | task-revision (ready to build) |
| PA-INCOMPLETE-003 | INCOMPLETE (NEW code gap, gate complete) | command-framework (`ff-command` + `ff-desktop`) | Req 10 Context Navigation Stack (CR-NR-057) unimplemented: Task 25 (25.1-25.7) open, no `ContextNavigationStack`/`navigation.stack_max_depth` code, TCR Phase DH Req 10.1-10.13 red. Includes Req 10.11 overflow WARN. Paired with command-semantics Req 11 (Command_Chain, W3.1). | Requirements gate complete (design + Task 25 + TCR rows). Owner: schedule Phase DH impl (TDD). | task-revision (ready to build) |
| PA-WATCH-001 | CONSISTENCY WATCH | command-framework (`Command_Target` + Context Navigation Stack) | Command_Target (Req 8) consumed by menu-workspace, command-configurator, startup-and-session, shell-command, lua-macro-engine. Req 10 Context Navigation Stack (CR-NR-057) consumed by startup-and-session, menu-workspace, function-keys-and-history; paired with command-semantics Req 11. | Verify Target_Resolution + nav-stack consistency when each consumer is analysed (Waves 3-5). No conflict now. | per-unit (Waves 3-5) |
| PA-WATCH-002 | FILE-SIZE WATCH | document-model (`ff-document-model`) | `document.rs` is 399 non-test lines -- exactly at the 400 cap; text_buffer.rs 354. | Split document.rs by concern (document_nav / document_scroll) on next touch. REFACTOR, no gate. | 6 (consolidation) / on next touch |
| PA-LOG-003 | LOGGING ENHANCEMENT (optional) | document-model (`ff-document-model`) | Zero log calls despite `ff-logging` dep; all errors surface via Result / LoadingProgress::Failed / watcher. Defensible for a pure library. | Optional: `log_warn!` on streaming-load failure (Req 4.6), `log_debug!` on read-only modify-attempt (Req 7.4). Owner-gated; not a blocker. | later (owner-gated) |
| PA-LOG-004 | LOGGING-GAP + Req 3.3 partial violation (code fix) | virtual-file-system (`ff-vfs`) | No `ff-logging` dependency; uses `eprintln!` at registry.rs:77, subsystem.rs:87/105 (stderr -- violates logging-subsystem Req 7.4/7.5). Req 3.3's mandated WARN on duplicate scheme registration NOT emitted (registry.rs:71). PERSISTS from prior pass. | Add `ff-logging` dep; replace 3 `eprintln!` with `log_info!`; add `log_warn!` on DuplicateScheme path. Follow ff-plugin/ff-core exemplar. Code-mode, no gate. HIGH value. | task-revision (code fix, high value) |
| PA-WATCH-003 | CONSISTENCY WATCH | virtual-file-system / dataset-catalog | Req 12 workspace-backup manifest (ff-vfs Req 12.2) overlaps dataset-catalog backup manifest (ff-dscatalog Req 26.3). | Verify shared-vs-distinct manifest type at Wave 2 (dataset-catalog). | Wave 2 |
| PA-SPLIT-002 | SPLIT PROPOSAL (weak) | virtual-file-system (`ff-vfs`) | Borderline: Req 9-12 physical-storage seam; posix_provider.rs 444 / workspace.rs 410 over cap; Req 9 permits a future `ff-storage-provider` crate. | NOT recommended now; revisit if storage layer grows. Apply 400-cap refactor to posix_provider.rs / workspace.rs. Owner-gated for crate split. | 6 (consolidation) / owner-gated |
| PA-STD-004 | REFACTOR (400-line cap) | plugin-architecture (`ff-plugin`) | `registry.rs` is 698 non-test lines, well over the cap. | Split by concern (registry_discovery / registry_lifecycle; keep registry.rs coordinator). REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-REF-001 | POSITIVE REFERENCE (no action) | plugin-architecture (`ff-plugin`) + platform-core (`ff-core`) | Exemplar foundation-crate logging pattern: declares `ff-logging`, all mandated ERROR/WARN paths present. | Cite as template when fixing PA-LOG-004 (ff-vfs) and evaluating zero-log crates (PA-LOG-002). No defect. | reference only |
| PA-LOG-005 | LOGGING-GAP + Req 7.6 partial violation (code fix) | workflow-engine (`ff-workflow`) | `checkpoint.rs` under-logs: load_checkpoint returns error on deserialize/schema failure without ERROR log or removing the invalid checkpoint (Req 7.6 mandates both); scan_resumable swallows unreadable files (`Err(_) => continue` :145); cleanup_expired discards remove failures (`let _ = ...` :195). Req 5.6 rollback ERROR present. PERSISTS. | (a) load_checkpoint: `log_error!` + remove invalid checkpoint; (b) scan_resumable: `log_warn!`; (c) cleanup_expired: log remove result. Code-mode, no gate. | task-revision (code fix) |
| PA-WATCH-004 | CONSISTENCY WATCH (FFW-ARCH-001) | workflow-engine (`ff-workflow`) | `checkpoint.rs` uses `tokio::fs` directly (9 sites) rather than ff-vfs. Req 7.3 omits VFS so not a stated violation, but inconsistent with document-model Req 4.8 / virtual-file-system Req 1.1; not a documented init-order exception. PERSISTS. | Owner decision: route checkpoint I/O through VFS OR document an explicit FFW-ARCH-001 exception. | later (owner-gated) |
| PA-STD-005 | REFACTOR (400-line cap) | workflow-engine (`ff-workflow`) | `runner.rs` (483) and `definition.rs` (463) over the cap. | Split by concern (runner_rollback/runner_step; definition_builder/definition_validate). REFACTOR, no gate. | 6 (consolidation) |
| PA-INCOMPLETE-002 | INCOMPLETE + tracking discrepancy + false-positive tests (HIGH) | background-io (`ff-background-io`) | Req 6.6 (load-error ERROR), 6.7 (retry), 6.8 (resume), 6.9 (retry WARN) NOT in `load.rs::execute_load` -- reads chunks in a plain loop, returns on first error, never invokes `RetryPolicy`. Tasks 9.1-9.4/9.9/13.7 `[x]`; claimed retry integration test (13.7) absent; property test only checks backoff arithmetic. PERSISTS. | Implement retry loop + resume + ERROR/WARN logging in execute_load (wire RetryPolicy from config); add real mock-VFS integration test; then re-mark tasks. Code-mode, no gate. HIGH. | task-revision (HIGH) |
| PA-TCR-001 | TCR-GAP | background-io (`ff-background-io`) | Crate has ZERO rows in TCR.md; none of Req 1-8 tracked despite tasks marked complete. PERSISTS. | Add TCR rows for Req 1-8 once PA-INCOMPLETE-002 fixed; Req 6.6-6.9 rows stay NOT COVERED/FAIL until then. | 6 (consolidation) / after PA-INCOMPLETE-002 |
| PA-SPLIT-003 | SPLIT PROPOSAL (borderline) | encoding-and-characters (`ff-encoding`) | 3/5 criteria: 14 reqs (>12), 316 req lines, 3 separable bands (encoding-I/O; character-classification; DBCS/grapheme/case-fold). | Split SPEC into `encoding-and-characters` (Req 1-5, 11, 14) + new `character-classification` (Req 6, 7, 12, 13). Crate split NOT recommended. Owner approval + own gate. | later (owner-gated) |
| PA-STD-006 | REFACTOR (400-line cap) | encoding-and-characters (`ff-encoding`) | `convert.rs` is 536 non-test lines, over the cap (category_map 357 / registry 337 near). | Split convert.rs by direction (convert_load / convert_save + convert_tables). REFACTOR, no gate. | 6 (consolidation) |
| PA-WATCH-005 | CONSISTENCY WATCH (low) | encoding-and-characters (`ff-encoding`) | Req 7.7 / 10.8 require Unicode-data tables (category map, case folding) generated at build time from the Unicode Character Database. | Verify a generation script / build.rs exists and records the Unicode version so tables do not silently drift. Low priority. | 6 (consolidation) |
| PA-INCOMPLETE-004 | INCOMPLETE (NEW code gap, gate complete, CR-NR-058) | command-framework (`ff-command`) | Req 11 Uniform Command Execution Instrumentation (CR-NR-058) unimplemented: no start/completion instrumentation wrapper at the `execute_command` dispatch boundary, no `redact_and_bound`, no Phase DI log records. Task 26 (26.1-26.x) open; TCR Phase DI rows NOT COVERED. Third command-framework gap (with Req 9 / Req 10). | Requirements gate complete. Owner: schedule Phase DI impl (TDD). HIGH LEVERAGE -- instruments every command invocation project-wide (start + params + outcome) at DEBUG in dev builds. Depends on Req 13 dev-logging gate. | task-revision (HIGH -- dev/debug logging) |
| PA-SPLIT-004 | SPLIT PROPOSAL | undo-redo-transactions (`ff-undo-redo`) | 19 reqs, 553 req lines, 3+ responsibilities. | SPEC split (core Req 1-7,9-11,14,16-18 vs recovery/tentative/container Req 8,12,13; command integration Req 15,19 -> command-semantics/function-keys). Crate split NOT recommended. Owner approval + own gate. | later (owner-gated) |
| PA-LOG-006 | LOGGING-GAP + Req 3.5 partial violation (code fix) | undo-redo-transactions (`ff-undo-redo`) | Orphaned-transaction force-close (transaction.rs:137 `force_close` + manager.rs caller) commits silently; Req 3.5 mandates a WARNING via the logging subsystem. Req 1.6 config-clamp WARN present (config.rs). | Add `log_warn!` naming the force-closed transaction at the force-close site. Code-mode, no gate. | task-revision (code fix) |
| PA-STD-007 | REFACTOR (400-line cap) | undo-redo-transactions (`ff-undo-redo`) | `manager.rs` is 600 non-test lines, over the cap. | Split by concern (manager_exec / manager_txn; keep manager.rs coordinator). REFACTOR, no gate. | 6 (consolidation) |
| PA-DOC-003 | TRACKING-FIX | undo-redo-transactions | Spec Introduction + Req 18.1 say "`ff-undo` crate"; actual crate is `ff-undo-redo`. | Reconcile the spec to the real crate name. Doc-only, no gate. | 6 (consolidation) |
| PA-CONFLICT-002 | CONSISTENCY CONFLICT (architectural, carried) | edit-operations / undo-redo-transactions | undo-redo defines `Transaction`/`EditOperation`/`ScrapStack` (Scintilla model); edit-operations defines `EditorTransaction` (line-snapshot model). Verify at W1.5 whether bridged or a genuine duplication (prior pass found them unbridged). | Resolve at edit-operations (W1.5): owner decision to unify OR declare one canonical + document the boundary. | Wave 1 (W1.5) / owner-gated |
| PA-WATCH-006 | RESOLVED | undo-redo-transactions / caret-and-selection / edit-operations | W1.5 RESOLVED: edit-operations is SOLE owner of the logical selection model (SelectionPosition/SelectionRange/Selection); caret-and-selection consumes it (no duplication, W1.3); undo-redo stores its own decoupled SelectionState snapshot (does not import edit-operations). Selection-model ownership CLEAN. | Transaction-model concern carried as PA-CONFLICT-002. | resolved W1.5 |
| PA-CONFLICT-002 | CONSISTENCY CONFLICT (architectural, CONFIRMED W1.5) | edit-operations / undo-redo-transactions | TWO independent unbridged undo-unit models: edit-operations `EditorTransaction` (LineSnapshot, FFE-MVP-3) vs undo-redo `Transaction`+`EditOperation`+`ScrapStack` (Scintilla). No dependency/reference either way. transaction.rs:4 claims delegation to ff-undo-redo but it is NOT wired. EVIDENCE: ff-line-commands produces `EditorTransaction` for all undoable commands and never uses its `ff-undo-redo` dep (grep 0) -- so EditorTransaction is de-facto canonical; undo-redo's model is unwired. | Owner decision: declare `EditorTransaction` canonical + reconcile/retire undo-redo's parallel model OR wire undo-redo to consume EditorTransaction. Confirm shell undo dispatch at Wave 4. | task-revision / owner-gated; confirm Wave 4 |
| PA-SPLIT-005 | SPLIT PROPOSAL | edit-operations (`ff-edit-operations`) | 17 reqs, 553 req lines, 3+ responsibilities. Files well-sized (none over cap). | SPEC split (text-mutation Req 1-5; selection/multi-caret/rectangular Req 6-9; profile/CAPS Req 16-17). Crate split lower value. Owner approval + own gate. | later (owner-gated) |
| PA-DEP-001 | UNUSED DEPENDENCY (REFACTOR) | line-commands (`ff-line-commands`) | Cargo.toml declares `ff-undo-redo` but no source references it (grep 0); all undoable ops produce edit-operations `EditorTransaction`. Re-confirmed W1.6. | Remove the `ff-undo-redo` dependency (or wire it if undo-redo becomes canonical per PA-CONFLICT-002). No gate. | 6 (consolidation) |
| PA-STD-010 | REFACTOR (400-line cap) | line-commands (`ff-line-commands`) | `resolution.rs` is 422 non-test lines, over the cap (command.rs 354 near). | Split resolution.rs by concern (resolution_block / resolution_target; keep dispatch). REFACTOR, no gate. | 6 (consolidation) |
| PA-CONFLICT-001 | CONSISTENCY CONFLICT (cross-unit, CONFIRMED at W1.4) | viewport-and-scrolling / display-line-mapping | CONFIRMED W1.4: canonical `DisplayLineMapping` trait is defined in ff-display-line-mapping (Req 7.10) and consumed by ff-line-commands + ff-exclude-show-filter + ff-desktop. BUT ff-viewport-scrolling defines its OWN `DisplayLineMapper` trait (+ IdentityMapper) and never imports the canonical one -- contrary to viewport Req 11.1 ("a DisplayLineMapper from the display-line-mapping crate"). Two differently-named, unbridged traits for one concept. | Owner decision: consume the canonical `DisplayLineMapping` in viewport (drop the duplicate) OR document the thin viewport-facing trait as intentional + add an adapter impl. Doc + possible small code change. | task-revision / owner-gated |
| PA-STD-009 | REFACTOR (400-line cap) | display-line-mapping (`ff-display-line-mapping`) | `contraction_state.rs` is 614 non-test lines, over the cap. | Split by concern (contraction_visibility/fold/wrap; keep contraction_state.rs core + trait impl). REFACTOR, no gate. | 6 (consolidation) |
| PA-STD-008 | REFACTOR (400-line cap) | viewport-and-scrolling (`ff-viewport-scrolling`) | `viewport.rs` is 572 lines, all non-test (tests external). | Split by concern (viewport_scroll / viewport_scrollbar; keep viewport.rs state+coordination). REFACTOR, no gate. Fix for the size pressure -- not a split. | 6 (consolidation) |
| PA-DOC-004 | TRACKING-FIX | viewport-and-scrolling | Spec says `ff-viewport-and-scrolling`; actual crate is `ff-viewport-scrolling`. | Reconcile the spec. Doc-only, no gate. | 6 (consolidation) |
| PA-LOG-007 | LOGGING ENHANCEMENT (optional) | viewport-and-scrolling (`ff-viewport-scrolling`) | Zero log calls despite `ff-logging` dep; defensible for a pure model. `ViewportError::InvalidConfig`/`SnapshotOutOfBounds` describe recovery actions. | Optional: `log_warn!` (or `log_debug!` under CR-NR-058 dev-logging) at those recovery points. Owner-gated; not a blocker. | later (owner-gated) |
| PA-WATCH-008 | RESOLVED (no conflict) | viewport-and-scrolling / navigation-commands | W1.7 RESOLVED: single source of truth. navigation-commands owns the scroll COMMANDS (UP/DOWN/LEFT/RIGHT + M/MAX/n arg, Req 3 + Req 20) and DELEGATES viewport STATE/clamping to `ff_viewport_scrolling::{CursorModel, ViewportModel}` (verified: scroll_to_line/max_top_line/visible_count calls). viewport Req 14 (CSR/PAGE/HALF field) and nav Req 20 (M/MAX/n command arg) are complementary layers, not duplicates. | No action. | resolved W1.7 |
| PA-INCOMPLETE-005 | INCOMPLETE (code gap, gate complete, CR-NR-054) | navigation-commands (`ff-navigation-commands`) | Req 20 Scroll Amount Arguments unimplemented: 5 Phase DF tasks open (DF.1-DF.5), no `parse_scroll_amount`/`ScrollAmount` in code. M/MAX/n for UP/DOWN/LEFT/RIGHT. Ships with command-framework Req 9 (PA-INCOMPLETE-001) as the Phase DF bundle. | Requirements gate complete. Owner: schedule Phase DF (TDD). | task-revision (ready to build) |
| PA-CONFLICT-003 | CONSISTENCY CONFLICT (duplicate CaseFolder) | find-and-replace / encoding-and-characters | `ff-find-and-replace` defines its own `CaseFolder` (case_folder.rs:25), does NOT depend on ff-encoding (grep 0), never imports `ff_encoding`/`ICaseConverter` -- despite ff-encoding (Req 10) being the declared sole owner of CaseFolder + ICaseConverter, and find-and-replace's own cross-ref stating dependence on encoding-and-characters. Two unbridged CaseFolder implementations. | Owner decision: consume `ff-encoding::CaseFolder` via the `ICaseConverter` trait (delete the local one) OR document the duplication as intentional. Code change + owner decision. | task-revision / owner-gated |
| PA-SPLIT-007 | SPLIT PROPOSAL | find-and-replace (`ff-find-and-replace`) | 20 reqs, 437 lines, 4/5 criteria; NFA RegexEngine (Req 12 + regex.rs 993) is a self-contained separable component. | Extract the NFA RegexEngine into a `ff-regex` crate; find-and-replace consumes it. Owner approval + own gate. | later (owner-gated) |
| PA-STD-011 | REFACTOR (400-line cap) | find-and-replace (`ff-find-and-replace`) | `regex.rs` (993) and `engine.rs` (838) far over the cap -- largest offenders found so far. | Split by concern (regex: compile/execute/classes; engine: find/change/state). REFACTOR, no gate (regex split may fold into PA-SPLIT-007). | 6 (consolidation) |
| PA-TCR-002 | TCR-GAP (coverage enumeration) | find-and-replace (`ff-find-and-replace`) | Only 1 TCR row for 20 reqs; the crate IS tested (property_tests.rs + unit tests) but TCR does not enumerate the criteria. | Add per-requirement TCR rows citing the existing tests. No code. | 6 (consolidation) |
| PA-SPLIT-006 | SPLIT PROPOSAL | navigation-commands (`ff-navigation-commands`) | 20 reqs, 624 req lines, 3+ responsibilities. SORT (undoable doc mutation) + COLS/BOUNDS (display/session-state) are separable seams from the caret/viewport navigation core. | SPEC split: keep nav core (LOCATE + UP/DOWN/etc + paragraph/word/word-part/vertical/doc nav + Req 20); move SORT to `sort-command` (or edit-operations); COLS/BOUNDS to `column-tools` (or fold BOUNDS into edit-operations). Crate split lower value. Owner-gated. | later (owner-gated) |
| PA-CR058 | REQUIREMENT ADDED (impacts W0.5 + W0.7) + LOGGING-AUDIT REFRAME | logging-subsystem (`ff-logging` Req 13) + command-framework (`ff-command` Req 11) + project-wide | CR-NR-058 (Phase DI) added build-profile compile-time logging gate (logging Req 13: `dev-logging` cargo feature, TRACE/DEBUG stripped in release, `BUILD_PROFILE_LEVEL` const) + uniform per-command start/params/completion instrumentation (command Req 11). UPDATE (commit `4bf6fbc`): Req 13 IMPLEMENTATION now DONE + verified (feature default, const re-exported from lib.rs, cfg-split macros; both profiles compile, 21 doctests pass, verify.ps1 clean) -- but Req 13 dedicated tests (Task 25.6/25.7) NOT written and TCR Phase DI Req 13 rows still NOT COVERED. Req 11 (command instrumentation) still UNIMPLEMENTED (PA-INCOMPLETE-004). | Req 13: write tasks 25.6/25.7 tests + set TCR Req 13 rows PASS. Then implement Req 11 (dispatch instrumentation) on the dev-logging gate. Canonical vehicle for the dev/debug logging; RE-SCOPE PA-LOG-003/PA-LOG-005 + PA-LOG-002 instrumentation to use the `dev-logging` convention. | task-revision (HIGH -- Req 13 tests remain; Req 11 remains) |
| PA-STD-013 | REFACTOR (ASCII source violation) | sequence-numbers (`ff-seqnum`) | 60 non-ASCII bytes across 16 `.rs` files: em-dash (U+2014), en-dash (U+2013), right-arrow (U+2192), and box-drawing separators (U+2500 range) in doc comments and section banners. documentation.md mandates STRICT ASCII in `.rs` (box-drawing allowed in `.md` ONLY, not `.rs`). Genuine violation. | Replace `--`/`-`/`->`; convert `// ___ Section ___` banners to `// === Section ===`. REFACTOR, no gate, no behaviour change. | 6 (consolidation) |
| PA-STD-012 | REFACTOR WATCH (400-line cap) | sequence-numbers (`ff-seqnum`) | `number_cmd.rs` = 355 non-test lines (largest; approaching cap). No file EXCEEDS 400 non-test yet, so no split forced. | If Req 6/7 NUMBER parsing grows, split argument-parsing from execution. WATCH only. | later |
| PA-TCR-003 | TCR-GAP (coverage enumeration) | sequence-numbers (`ff-seqnum`) | Only 3 TCR rows (1 generic + 2 Phase BY alias) for 14 reqs / ~90 criteria; the crate IS tested (integration + property + in-file unit tests) but TCR does not enumerate the criteria. Same pattern as PA-TCR-002. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-LOG-006 | LOGGING-GAP (mandated WARN + dev-logging) | sequence-numbers (`ff-seqnum`) | Zero ff-logging usage (defensible for a pure model). BUT Req 1.4 (malformed column range) and Req 2.8 (threshold out of range) SPECIFY "emit a WARN-level log record" -- code clamps/ignores correctly but emits no log. Also natural home for CR-NR-058 dev-logging (UNNUM/NUMBER/NUMBER SHOW start/params/completion at DEBUG). | Wire ff-logging: emit the two mandated WARNs; add dev-logging command instrumentation once logging Req 13 (PA-CR058) lands. | task-revision (LOW; dev-logging owner/Req-13 gated) |
| PA-STD-014 | REFACTOR (400-line cap EXCEEDED) | syntax-highlighting (`ff-syntax-highlighting`) | `engine/highlight_engine.rs` = 462 non-test lines, OVER the 400 hard cap. `hilite.rs` = 387 (WATCH, near cap). | Split `highlight_engine.rs` by concern (buffer mgmt / incremental re-highlight / demand-driven ensure_styled_to / lexer lifecycle). REFACTOR, no gate, no behaviour change. | 6 (consolidation) |
| PA-STD-015 | REFACTOR (ASCII source violation) | syntax-highlighting (`ff-syntax-highlighting`) | 17 non-ASCII bytes in `.rs`: em/en dashes in doc comments (e.g. lib.rs "0-255", "GUI-independent -- it"). documentation.md mandates strict ASCII in `.rs`. | Replace with `--`/`-`. REFACTOR, no gate. | 6 (consolidation) |
| PA-TCR-004 | TCR-GAP (coverage enumeration) | syntax-highlighting (`ff-syntax-highlighting`) | TCR enumerates ONLY Req 16 (HILITE, Phase CF rows 16.1-16.5). Reqs 1-15 (the entire engine) have NO TCR rows despite property_tests.rs + per-module unit tests existing. RECORDING gap, not test gap. | Add per-requirement TCR rows for Req 1-15 citing existing tests. No code. | 6 (consolidation) |
| PA-LOG-007 | LOGGING-GAP (mandated DEBUG + dev-logging) | syntax-highlighting (`ff-syntax-highlighting`) | Zero ff-logging usage (defensible for a pure engine). BUT Req 10.7 SPECIFIES "log a DEBUG-level message" for unknown lexer property keys -- code stores forward-compatibly but emits no log. Also natural CR-NR-058 dev-logging home (incremental-rehighlight + idle-styling trace at DEBUG). | Wire ff-logging: emit the Req 10.7 DEBUG record; add dev-logging trace points once logging Req 13 (PA-CR058) lands. | task-revision (LOW; dev-logging owner/Req-13 gated) |
| PA-WATCH-009 | WATCH (HILITE command delegation seam) | syntax-highlighting + edit-operations | HILITE (Req 16) is split: edit-operations parses/stores the command (Req 16.12, "delegates") and syntax-highlighting executes the modes (ON/OFF/LOGIC/PAREN/FIND). HILITE FIND also extends find-and-replace highlight-all. Three-crate command. | Confirm the parse->execute delegation wiring is correct at Wave 3/4 (command-semantics / shell command dispatch). No action now. | Wave 3/4 |
| PA-STD-016 | REFACTOR (400-line cap EXCEEDED) | exclude-show-filter (`ff-exclude-show-filter`) | `exclusion_engine.rs` = 535 non-test lines, well OVER the 400 hard cap. `registration.rs` = 287 (WATCH). | Split `exclusion_engine.rs` by concern (command dispatch / query+iterators / block+placeholder / X-XX line-command resolution). REFACTOR, no gate, no behaviour change. | 6 (consolidation) |
| PA-STD-017 | REFACTOR (ASCII source violation incl. runtime string) | exclude-show-filter (`ff-exclude-show-filter`) | 67 non-ASCII bytes in `.rs`: box-drawing banners + em/en dashes in comments (prohibited in `.rs`), PLUS `error.rs:18` puts an en-dash (U+2013) inside a RUNTIME error string ("invalid line range {start}-{end}") -- a non-ASCII char in user-facing output is a genuine defect. | Replace all with ASCII `-`/`--`; convert banners to `// === ... ===`. REFACTOR, no gate. | 6 (consolidation) |
| PA-TCR-005 | TCR-GAP (TOTAL absence) | exclude-show-filter (`ff-exclude-show-filter`) | ZERO TCR rows for the crate (grep = 0) across 10 reqs / ~70 criteria, despite in-file tests existing. Most complete TCR absence in Wave 1. RECORDING gap, not test gap. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-LOG-008 | LOGGING-GAP (dead dep -> dev-logging) | exclude-show-filter (`ff-exclude-show-filter`) | `ff-logging` declared as a Cargo dep (Cargo.toml:12) but NEVER imported/used (0 macro calls, 0 `use`). DEAD DEPENDENCY. Status-message reqs (2.8/3.7/4.7/5.7) are status-area returns, not logs. | Drop the unused dep OR (preferred) wire dev-logging command instrumentation for EXCLUDE/SHOW/RESET under the `dev-logging` gate once logging Req 13 (PA-CR058) lands. | task-revision (LOW; dev-logging owner/Req-13 gated) |
| PA-WATCH-010 | WATCH (visibility coexistence) | exclude-show-filter + display-line-mapping + syntax-highlighting/fold | Flat exclusion (exclude-show-filter) and nested code-folding (fold levels from syntax-highlighting) both write the SAME visibility bit in display-line-mapping. | Confirm no double-toggle race / correct combined visible/hidden result when a line is both folded and excluded. Verify at Wave 4 viewport/shell integration. No action now. | Wave 4 |
| PA-STD-018 | REFACTOR (ASCII source violation incl. runtime strings) | whitespace-and-guides (`ff-whitespace-guides`) | 39 non-ASCII bytes in `.rs`; 9 in non-comment code, of which 7 are em-dashes (U+2014) INSIDE runtime `#[error("...")]` strings in `error.rs` (lines 12-59, e.g. "invalid whitespace mode '{value}' -- using default"). Non-ASCII in user-facing output is a genuine defect. Remainder: em/en dashes + arrows (Req 8 "A -> B -> C" cycle text) + box-drawing banners in doc comments. | Replace all with ASCII `--`/`-`/`->`; convert banners to `// === ... ===`. REFACTOR, no gate. | 6 (consolidation) |
| PA-TCR-006 | TCR-GAP (TOTAL absence) | whitespace-and-guides (`ff-whitespace-guides`) | ZERO TCR rows for the crate (grep = 0) across 9 reqs / ~60 criteria, despite well-annotated in-file tests (`// Validates: Requirement X.Y`). Second consecutive total absence (after PA-TCR-005). RECORDING gap, not test gap. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-LOG-009 | LOGGING-GAP (dead dep -> dev-logging) | whitespace-and-guides (`ff-whitespace-guides`) | `ff-logging` declared as a Cargo dep (Cargo.toml:12) but NEVER imported/used (0 macro calls, 0 `use`). DEAD DEPENDENCY (same pattern as PA-LOG-008). The `error.rs` config-error variants ("invalid ... using default") are the natural WARN site on config-value coercion but emit no log. | Drop the unused dep OR wire ff-logging to emit a WARN on config coercion + dev-logging on toggle-command state changes, under the `dev-logging` gate once logging Req 13 (PA-CR058) lands. | task-revision (LOW; dev-logging owner/Req-13 gated) |
| PA-INCOMPLETE-006 | INCOMPLETE (mandated logging stubbed) -- CR-NR-058 relevant | auto-indentation (`ff-auto-indent`) | Two reqs MANDATE logging, NEITHER implemented while tasks read 137/137 `[x]`: Req 9.7 (invalid regex -> WARN) exists only as a COMMENTED-OUT call at `patterns.rs:47` (`// ff_logging::log(LogLevel::Warn, ...)`); Req 10.7 (per-decision DEBUG record with ref line + matched pattern + resulting level) has NO code at all. False-positive tracking (same class as PA-INCOMPLETE-002). This is the clearest Wave-1 case of CR-NR-058 dev/debug logging already written as an acceptance criterion. | Uncomment/wire the Req 9.7 WARN; implement the Req 10.7 per-decision DEBUG in the decision/service path, GATED behind `dev-logging` (logging Req 13 / PA-CR058) so it is release-stripped. Re-open the wrongly-`[x]` tasks. | task-revision (HIGH; dev-logging owner/Req-13 aware) |
| PA-STD-019 | REFACTOR (ASCII source violation incl. runtime string) | auto-indentation (`ff-auto-indent`) | 99 non-ASCII bytes in `.rs` (15 non-comment). `error.rs:12` has an em-dash inside a runtime error string ("invalid mode '{value}' -- expected ..."). Others: `->` arrows in test comments (indent_cmd.rs), em/en dashes + box-drawing banners in doc comments. | Replace with `--`/`-`/`->`; convert banners to `// === ... ===`. REFACTOR, no gate. | 6 (consolidation) |
| PA-TCR-007 | TCR-GAP (coverage enumeration) | auto-indentation (`ff-auto-indent`) | 1 TCR row for 10 reqs / ~60 criteria; in-file tests with `// Validates:` exist. Thin. Also needs Req 9.7/10.7 rows once PA-INCOMPLETE-006 lands. | Add per-requirement TCR rows citing existing tests; add 9.7/10.7 after the logging fix. No code (until then). | 6 (consolidation) |
| PA-CONFLICT-004 | CONSISTENCY CONFLICT (duplicate types) | line-wrap-toggle (`ff-wrap`) / whitespace-and-guides (`ff-whitespace-guides`) | `WrapIndentMode` (Fixed/Same/Indent/DeepIndent) defined in BOTH `ff-wrap::indent.rs:16` AND `ff-whitespace-guides::wrap_indent_mode.rs:10`; wrap visual flags defined in both AND structurally divergent (`ff-wrap::WrapVisualFlags` ENUM vs `ff-whitespace-guides::WrapVisualFlag(u8)` BITFIELD). Neither crate imports the other (ff-wrap deps = thiserror+serde only). Two unbridged parallel definitions of the same Scintilla concept + two config surfaces (`[view.wrap]` vs `editor.wrap_*`). Same anti-pattern class as PA-CONFLICT-001/003. | Owner decision: make whitespace-and-guides the SOLE owner of `WrapIndentMode` + wrap-visual-flag type (its Req 6-7 + line-wrap Req 10.7 "rendered using whitespace-guides infrastructure" point that way); ff-wrap consumes. Reconcile enum-vs-bitfield shape and unify config keys. Code change + owner decision. | task-revision / owner-gated (HIGH) |
| PA-LOG-010 | LOGGING-GAP (mandated warning, no mechanism) | line-wrap-toggle (`ff-wrap`) | FOUR reqs MANDATE a config warning via the logging-subsystem -- Req 4.7 (wrap_column range), Req 5.8 (indent_amount range), Req 11.3 (bad persisted mode), Req 12.2 ("emit a configuration warning via the logging-subsystem"). Crate has NO ff-logging dep and emits 0 logs. It DOES return warning DATA (config.rs builds "out of range ... using default" messages) but never logs it. | Add ff-logging + emit the mandated warnings here, OR make the config-loading CALLER's logging responsibility explicit in the spec. Plus dev-logging for WRAP command state changes (CR-NR-058). | task-revision (MEDIUM; dev-logging owner/Req-13 aware) |
| PA-STD-020 | REFACTOR (ASCII source violation incl. runtime strings) | line-wrap-toggle (`ff-wrap`) | 68 non-ASCII bytes in `.rs` (14 non-comment). MANY are em/en-dashes inside RUNTIME warning + error strings in `config.rs` (lines 99/115/133/147/153/172, e.g. "out of range (0-10000) -- using default") and `error.rs` (14/22). Non-ASCII in user-facing output is a genuine defect. Remainder: doc-comment banners/dashes. | Replace with `--`/`-`; convert banners to `// === ... ===`. REFACTOR, no gate. | 6 (consolidation) |
| PA-TCR-008 | TCR-GAP (coverage enumeration) | line-wrap-toggle (`ff-wrap`) | 1 TCR row for 13 reqs / ~80 criteria; in-file tests exist. Thin. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-STD-021 | REFACTOR (400-line cap EXCEEDED) | text-decorations (`ff-text-decorations`) | `run_styles.rs` = 410 non-test lines, just OVER the 400 hard cap. It is the RLE core (RunStyles + fill_range/insert_space/delete_range/value_at/start_run/end_run). | Split by concern: container/mutation vs range-query+iteration API. REFACTOR, no gate, no behaviour change. | 6 (consolidation) |
| PA-STD-022 | REFACTOR (ASCII source violation incl. runtime strings) | text-decorations (`ff-text-decorations`) | 71 non-ASCII bytes in `.rs` (3 non-comment). `error.rs:26,30` have en-dashes inside runtime `#[error]` strings ("lexer range (0-7)", "numbers (8-31)"). Remainder: doc-comment em/en dashes + box-drawing banners + a `->` in a test comment (run_styles.rs:467). | Replace with `--`/`-`/`->`; convert banners to `// === ... ===`. REFACTOR, no gate. | 6 (consolidation) |
| PA-TCR-009 | TCR-GAP (TOTAL absence) | text-decorations (`ff-text-decorations`) | ZERO TCR rows for the crate (grep = 0) across 15 reqs / ~130 criteria, despite in-file tests. Third total absence in Wave 1 (after PA-TCR-005, PA-TCR-006). RECORDING gap, not test gap. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-LOG-011 | LOGGING-GAP (dead dep + mandated WARN) | text-decorations (`ff-text-decorations`) | `ff-logging` declared as a Cargo dep (Cargo.toml:10) but 0 uses (DEAD, like PA-LOG-008/009). AND Req 15.8 MANDATES "logging a warning" when a theme value is invalid (alpha/stroke_width/style out of range) -- validation+fallback appear present but no log emitted. | Wire ff-logging to emit the Req 15.8 WARN on invalid theme value; add dev-logging edit-sync/allocation-exhaustion trace under the `dev-logging` gate once logging Req 13 (PA-CR058) lands. | task-revision (LOW-MED; dev-logging owner/Req-13 aware) |
| PA-DOC-005 | DOC ASCII violation (pre-existing, surfaced W1.16) | project-master (`docs/specs/project-master/tasks.md`) | 67 prohibited non-ASCII chars in EXISTING task sections (not the PA-W0/PA-W1 blocks, which are clean): arrows (U+2192), ellipsis (U+2026), en-dashes in task ranges (e.g. "22.1-22.8", "Tasks 1.1-1.9"), em-dashes in Phase 18/19/22/W lines. documentation.md flags these for `docs/`. Left untouched per non-destructive analysis rule -- recorded only. | One doc-cleanup pass replacing arrows->`->`, ellipsis->`...`, en/em-dashes->`-`/`--` in the pre-existing task lines. Doc-only, no gate. Fold into the PA-W0.23/PA-W1.9 ASCII sweep. | 6 (consolidation) |
| PA-SPLIT-008 | SPLIT PROPOSAL (4-of-4) | dataset-catalog (`ff-dscatalog`) | 31 reqs / 816 lines (largest unit). FOUR-FIVE separable concerns, two spec-declared: (1) catalog metadata+lifecycle Req 1-15,25-31; (2) record codecs Req 16-17 -- Req 17.1 MANDATES "separate module (or crate) independent of any storage provider"; (3) StorageProvider + Native/SqliteRecord providers + UUID layout Req 18-20; (4) VSAM KSDS/RRDS/ESDS/ISAM Req 21-24. `src/codecs/` and `src/storage/` already physically separate. | Owner-gated SPEC + crate split: keep ff-dscatalog = catalog meta + VFS provider; extract `ff-record-codec` (Req 16-17, highest-confidence) + `ff-record-store`/`ff-vsam` (Req 18-24); ff-dscatalog consumes behind StorageProvider (Req 19). Coordinate with dataset-allocator (W2.3) + dataset-ownership-model ADR-001 (W2.4). Own gate. | later (owner-gated) |
| PA-STD-023 | REFACTOR (400-line cap, MANY files) | dataset-catalog (`ff-dscatalog`) | NINE files over the 400 non-test cap: `storage/sqlite_record.rs` 843 (worst in analysis so far), `catalog.rs` 609, `storage/esds.rs` 590, `transactions.rs` 503, `integrity.rs` 501, `storage/vfs_provider.rs` 482, `gdg.rs` 459, `dsn.rs` 454 (schema.rs 331 near). | Much resolves via PA-SPLIT-008 (sqlite_record/esds move to record-store crate); residual concern-splits for catalog.rs/transactions.rs/integrity.rs/gdg.rs/dsn.rs. REFACTOR, no gate; do alongside the split. | 6 (consolidation) / with split |
| PA-STD-024 | REFACTOR (ASCII source violation incl. runtime UI strings) | dataset-catalog (`ff-dscatalog`) | 34 non-ASCII bytes (10 non-comment). The non-comment set is ellipsis (U+2026) inside `context_menu.rs` menu-item LABELS ("Mount Catalog...", "New Dataset...", "Rename...", etc.) -- runtime user-facing strings, a genuine defect. Plus doc-comment dashes. | Replace ellipsis with `...`; fix doc-comment dashes. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-012 | LOGGING-GAP (dead dep on I/O/DB/transactional subsystem) -- CR-NR-058 | dataset-catalog (`ff-dscatalog`) | `ff-logging` declared (Cargo.toml:13) but 0 uses. DEAD -- but MORE serious than the Wave-1 pure-model dead deps: this is a disk-touching (49 fs calls), SQLite-backed, TRANSACTIONAL subsystem (staged txns Req 25, integrity/backup Req 26, reconciliation Req 27, GDG roll-off Req 9.3, mount validation Req 5.5, import integrity Req 6.7, codec diagnostic Req 16.7, "corrupt database" NFR) whose error/recovery paths log NOTHING. | Wire ff-logging: ERROR on I/O + SQLite failures, WARN on roll-off/reconcile/import-mismatch, dev-logging DEBUG on command start/params/result. Highest-value Wave-2 logging target. Depends on Phase PA-W0.1 gate. | task-revision (MEDIUM-HIGH; dev-logging owner/Req-13 aware) |
| PA-DEP-002 | RECLASSIFIED W2.4 (doc-only, NOT removal) | dataset-catalog / `ff-dataset-catalog` | W2.1 flagged `crates/ff-dataset-catalog` (lib.rs + traits.rs) as a dead stub to remove. W2.4 CORRECTS this: it is the INTENTIONAL shared-interface crate holding the ADR-001 `CatalogService`/`DynCatalogService` traits (Req 7.6 "defined in ff-dataset-catalog OR a shared interface crate"); `ff-dscatalog` is the implementation. Do NOT remove. | DOC-only: clarify ff-dataset-catalog = interface crate, ff-dscatalog = implementation; correct the dataset-catalog spec's misleading "ff-dataset-catalog is wrong, read ff-dscatalog" note (the interface crate legitimately uses that name). | 6 (consolidation) -- doc-only |
| PA-WATCH-011 | WATCH (ADR-001 ownership boundaries) -- CRUD half RESOLVED W2.3 | dataset-catalog / dataset-allocator (`ff-dsalloc`) / ff-idcams | ADR-001 makes ff-dscatalog the sole authority for catalog metadata/CRUD. CRUD DELEGATION half RESOLVED CLEAN (W2.3): ff-dsalloc defines a `CatalogProvider` trait (catalog_bridge.rs), has NO ff-dscatalog dep, ships only MockCatalog, delegates all catalog CRUD/resolution/GDG through the trait per ADR-001 Req 4.3-4.7 -- CANNOT duplicate (no catalog storage code). REMAINING: ff-idcams listcat delegates to catalog query API (Req 13) -- verify at Wave 5. NB DSN-validation is NOT clean (PA-CONFLICT-006). | ff-idcams listcat delegation: verify at Wave 5. CRUD half: no action (clean). | Wave 5 (idcams only) |
| PA-CONFLICT-005 | CONSISTENCY CONFLICT (duplicate + misplaced provider) | virtual-catalog-manager (`ff-desktop`) / virtual-file-system (`ff-vfs`) | Req 7 mandates a `posix` VfsProvider. TWO impls exist: `ff-vfs/src/posix_provider.rs` (664, correct home) AND `ff-desktop/src/posix_provider.rs` (404) which genuinely `impl VfsProvider for PosixProvider` (scheme `posix`, line 133), wraps `ff_connector_local_fs::LocalFsProvider`, and carries `#![allow(dead_code)]` "wired in Tasks 4-10". A VfsProvider in the SHELL violates FFW-ARCH-001 + VFS ownership (W0.11); also a straight duplicate (PA-CONFLICT-001/003/004 class). | Owner decision: DELETE the ff-desktop posix_provider; keep the ff-vfs one; VCM UI consumes it via the VFS registry. Layering fix + de-duplication. Code change + owner decision. | task-revision / owner-gated (HIGH) |
| PA-STD-025 | REFACTOR (400-line cap, SEVERE -- worst in analysis) | virtual-catalog-manager (`ff-desktop`) | ff-desktop VCM UI files far over the cap: `files_panel.rs` 1195 (~3x), `file_explorer_panel.rs` 935 (~2.3x), `catalog_manager_dialog.rs` 645, `dataset_alloc_dialog.rs` 419. rust-standards.md prescribes shell-panel splits (`_state`/`_render`/`_commands`/`_dialogs`). | Split per the prescribed ff-desktop shell layout. REFACTOR, no gate. Highest line-count cap violations found. | 6 (consolidation) |
| PA-STD-026 | REFACTOR (ASCII source violation -- BOM/mojibake) | virtual-catalog-manager (`ff-desktop`) | Heavy non-ASCII in VCM files (catalog_manager_dialog 92, dataset_alloc_dialog 71, posix_provider 30, catalog_registry 23). `ff-desktop/src/posix_provider.rs` contains MOJIBAKE (U+00E2 BOM/UTF-8-artifact sequences): "Requirement 7.1-7.7" and box-drawing banners appear as corrupted bytes -- worse than plain em-dashes per documentation.md. | Replace with ASCII; strip BOM artifacts; convert banners to `// === ... ===`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-013 | LOGGING-GAP (destructive ops unlogged) -- CR-NR-058 | virtual-catalog-manager (`ff-desktop`) | 0 ff-logging calls in catalog_manager_dialog / dataset_alloc_dialog / posix_provider / catalog_registry. These perform catalog create/delete (Req 4.5 RECURSIVE delete of backing files), dataset allocate/delete, and POSIX file I/O -- destructive/operational paths with no logging. | Wire ff-logging: WARN/ERROR on catalog+file delete, allocate/delete, POSIX provider errors; dev-logging on dialog-confirm command dispatch. Consolidate with ff-dscatalog PA-LOG-012 (same catalog domain). Depends on Phase PA-W0.1 gate. | task-revision (MEDIUM; dev-logging owner/Req-13 aware) |
| PA-WATCH-012 | WATCH REFINED W3.9 (3-layer catalog persistence, coordination) | virtual-catalog-manager / dataset-catalog / startup-and-session | VCM persists all catalogs under `[virtual_catalogs]` (Req 2.1); ff-dscatalog persists mainframe mounts under `[catalog].mounted_catalogs` (W2.1 Req 14). Possible competing source of truth for the same mainframe catalog mounts. | Confirm the two are not competing persistence stores (VCM registry should own registration; ff-dscatalog owns mount runtime state). Verify at W2.x/Wave-3 startup-session. No action now. | Wave 3 (startup-session) |
| PA-CONFLICT-006 | CONSISTENCY CONFLICT (duplicate DSN validation vs ADR-001) | dataset-allocator (`ff-dsalloc`) / dataset-catalog (`ff-dscatalog`) | ff-dsalloc `dsn.rs` defines its own `DatasetName::parse()` enforcing z/OS DSN syntax (qualifier rules, uppercase, length). ADR-001 Req 3.1 assigns "dataset naming validation (DSN syntax, qualifier rules, HLQ management)" EXCLUSIVELY to ff-dscatalog (which exposes `validate_dsn`, Req 3.4) and Req 4.3 says the allocator obtains metadata exclusively from catalog services. Duplicate validator = governance conflict (PA-CONFLICT-003/004/005 class). NB: DD-statement parsing IS legitimately the allocator's; only DSN-string syntax validation is the catalog's. | Owner decision: add `validate_dsn` to the CatalogProvider trait and route validation through ff-dscatalog; delete ff-dsalloc's local validator OR document it as a permitted pre-parse convenience that MUST match catalog rules. Consolidate DSN parsers under ff-dscatalog. Code + owner decision. | task-revision / owner-gated (MEDIUM) |
| PA-STD-027 | REFACTOR (400-line cap) | dataset-allocator (`ff-dsalloc`) | `pipeline.rs` = 412 non-test lines, just over the cap (parser.rs 392, allocation.rs 359, operands.rs 317 near). | Split pipeline orchestration (parse->substitute->resolve->validate) by stage. REFACTOR, no gate. | 6 (consolidation) |
| PA-STD-028 | REFACTOR (ASCII source violation incl. runtime strings) | dataset-allocator (`ff-dsalloc`) | 79 non-ASCII bytes (7 non-comment). em-dashes inside runtime `#[error]`/message strings: error.rs (57/104), gdg_resolver.rs (148/170/187); a `->` arrow in panel.rs:175 runtime format string ("{} -> {}"). Non-ASCII in user-facing output is a genuine defect. Rest: doc-comment dashes. | Replace with `--`/`-`/`->`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-014 | LOGGING-GAP (spec-vs-impl dep drift) | dataset-allocator (`ff-dsalloc`) | Spec Introduction lists "`ff-logging` -- for structured diagnostics" as a dependency, but the crate has NO ff-logging dep and 0 log calls. Diagnostics are returned as `LintDiagnostic` DATA (reasonable for a pure resolver). Mismatch between declared and actual dependencies. | Reconcile: add ff-logging + dev-logging on the RESOLVE/allocation pipeline (CR-NR-058) OR correct the spec dependency list. Hard operational logging less critical than ff-dscatalog (PA-LOG-012). | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-TCR-010 | TCR-GAP (coverage enumeration) | dataset-allocator (`ff-dsalloc`) | 1 TCR row for 16 reqs / ~120 criteria; in-file tests with `// Validates:` exist. Thin (contrast sibling ff-dscatalog 99 rows). | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-CONFLICT-007 | ADR-001 COMPLIANCE GAP (VSAM impl misplaced) | dataset-ownership-model / dataset-catalog (`ff-dscatalog`) / `ff-vsam-services` | ADR-001 Req 5.1 makes ff-vsam-services the sole owner of VSAM behaviour (KSDS/ESDS/RRDS/LDS); Req 3.2 says ff-dataset-catalog SHALL NOT own VSAM. Actual: `ff-vsam-services` is a trait-only stub (lib.rs 27, traits.rs 370 -- Req 16.7 permits the interim stub) while the VSAM IMPLEMENTATION lives in `ff-dscatalog/src/storage/` (esds.rs 590, rrds.rs, isam.rs, sqlite_record.rs 843, native.rs). VSAM record logic is in the wrong crate. | Owner-gated: migrate the storage/ VSAM impl from ff-dscatalog into ff-vsam-services behind the existing `VsamService` trait; ff-dscatalog then depends on ff-vsam-services (Req 7.1 direction catalog->vsam->storage). SUBSUMES the VSAM half of PA-SPLIT-008 (target crate already exists -- migration, not new extraction). Code + owner decision. | later (owner-gated) |
| PA-WATCH-013 | WATCH (fitness-function coverage gap) | dataset-ownership-model (`ff-governance-tests`) | `architecture_compliance.rs` (Req 18) asserts prohibited deps for `ff-dataset-catalog` (the INTERFACE crate) but has 0 references to `ff-dscatalog` (the IMPLEMENTATION crate holding all code + the ff-vfs/ff-command/ff-config/ff-logging deps) or ff-dsalloc. The impl crates could acquire a prohibited dep (ff-dsalloc/ff-idcams) undetected. | Extend architecture_compliance.rs to also assert ff-dscatalog and ff-dsalloc prohibited deps. Low-risk test addition. | 6 (consolidation) |
| PA-CONFLICT-008 | CONSISTENCY CONFLICT (duplicate record-structure model) | structure-catalog (`ff-structure-catalog`) / fileforge-integration (`ff-forge`) | structure-catalog defines its OWN `FieldType` (field.rs), `FieldDefinition` (field.rs), `RecordStructure` (model.rs) and has NO Cargo dep on ff-forge -- despite the spec saying it "extends fileforge-integration (which owns record parsing, field extraction)". ff-forge defines parallel `DataType`/`FieldDefinition`/`RecordStructure`/`StructureFile`. Two unbridged record-structure models across crates that should share (PA-CONFLICT-003/004/006 class). | Owner-gated: unify the record-structure DATA MODEL under ff-forge (fileforge-integration); structure-catalog depends on it and adds only the catalog LIBRARY/persistence/`.ffs`/UI layer. Reconcile FieldType(catalog) vs DataType(forge) + the two StructureFile/.ffs models. Confirm exact divergence at fileforge-integration (Wave 5). Code + owner decision. | later (owner-gated) |
| PA-LOG-015 | LOGGING-GAP (dead dep + mandated WARN/INFO absent) | structure-catalog (`ff-structure-catalog`) | `ff-logging` declared but 0 uses (DEAD). FOUR mandated logs unimplemented: Req 1.5 (INFO on catalog-dir auto-create), Req 1.6 (WARN on inaccessible location -> skip), Req 2.5 (WARN on invalid TOML -> exclude), Req 2.6 (WARN on schema-validation failure -> exclude). These are the catalog-load error paths. | Wire ff-logging for the Req 1.5/1.6/2.5/2.6 records; add dev-logging on CATALOG/APPLY STRUCTURE command dispatch. Consolidate with catalog-domain PA-LOG-012/013. Depends on Phase PA-W0.1 gate. | task-revision (MEDIUM; dev-logging owner/Req-13 aware) |
| PA-STD-029 | REFACTOR (ASCII source violation -- comment-only) | structure-catalog (`ff-structure-catalog`) | 211 non-ASCII bytes in `.rs`: 206 em-dashes in doc comments (`//! ... -- ...`), 5 box-drawing. ALL in comments (0 in runtime strings -- better than W2.1/W2.2). Still a documentation.md violation (em-dash + box-drawing prohibited in `.rs`). | Replace em-dashes with `--`, box-drawing banners with `// === ... ===`. REFACTOR, no gate. | 6 (consolidation) |
| PA-TCR-011 | TCR-GAP (TOTAL absence) | structure-catalog (`ff-structure-catalog`) | ZERO TCR rows for the crate (grep = 0) across 15 reqs / ~110 criteria, despite well-annotated in-file tests. Another total absence (after PA-TCR-005/006/009). RECORDING gap, not test gap. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-WATCH-014 | WATCH (shared config prefix) | structure-catalog / dataset-catalog | structure-catalog uses `catalog.locations`/`catalog.active_location`; dataset-catalog uses `[catalog].mounted_catalogs`/`default_hlq`. Two different domains (record-structure library vs dataset emulation) share the `catalog` config root. | Confirm no key collision under the `catalog` prefix; consider distinct prefixes (e.g., `structure_catalog.*` vs `catalog.*`). Verify at Wave 3 (configuration/startup). No action now. | Wave 3 |
| PA-WATCH-015 | WATCH (config mediation bypassed + minimal deps) | record-selection-criteria (`ff-select`) | Req 9.1 says the Criteria_Store is "managed through the configuration system's user layer" but ff-select has NO ff-config dep (only ff-logging) and does raw `std::fs` in location.rs + persistence.rs for criteria_store.toml + `.criteria.json`. Config mediation bypassed (analogous to workflow PA-WATCH-004). Cross-refs also name command-framework + configuration-system but neither is a Cargo dep. | Route the Criteria_Store through ff-config's user layer (add ff-config dep); route `.criteria.json` catalog access through ff-vfs per FFW-ARCH-001; OR document the caller-mediated design and correct the cross-refs. Contained (2 fs sites). Verify at Wave 3 (config/startup). | task-revision (MEDIUM) |
| PA-LOG-016 | LOGGING-GAP (dead sole-dep + mandated WARN absent) | record-selection-criteria (`ff-select`) | `ff-logging` is the crate's ONLY dependency yet has 0 uses (DEAD). Req 9.9 mandates "emit a warning" on corrupt Criteria_Store; config.rs coercion messages (49-85 "must be a string -- using default") and error.rs parse/io/corrupt errors are built but never logged. File I/O present. | Wire ff-logging for the Req 9.9 WARN + config-coercion warnings + `.criteria.json` load failures (Req 9.7) + dev-logging on CRITERIA command dispatch. Depends on Phase PA-W0.1 gate. | task-revision (MEDIUM; dev-logging owner/Req-13 aware) |
| PA-STD-030 | REFACTOR (ASCII source violation incl. runtime strings) | record-selection-criteria (`ff-select`) | 54 non-ASCII bytes (13 non-comment). em-dashes inside runtime config-warning + `#[error]` strings: config.rs (49/59/69/85), error.rs (40/58/67/78). Non-ASCII in user-facing output is a genuine defect. Rest: doc-comment dashes. | Replace with `--`/`-`. REFACTOR, no gate. | 6 (consolidation) |
| PA-TCR-012 | TCR-GAP (coverage enumeration) | record-selection-criteria (`ff-select`) | 1 TCR row for 14 reqs / ~90 criteria; in-file tests exist. Thin. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-WATCH-016 | WATCH (pattern replication, consolidation opportunity) | tabs-and-mask / navigation-commands / exclude-show-filter | The glossary defines `Display_Artifact_Line` as ONE unified concept spanning COLS_Line, BNDS_Line (navigation-commands W1.7), Placeholder_Line (exclude-show-filter W1.11), TABS_Line, MASK_Line (ff-tabmask). In code there is NO shared `DisplayArtifactLine` trait/type (grep 0) -- each subsystem reimplements the synthetic-display-line concept independently. Not a hard duplicate-type conflict (each artifact is distinct content) but a consolidation opportunity. | Consider a shared synthetic-display-line abstraction (viewport-and-scrolling or display-line-mapping owned) unifying COLS/BNDS/TABS/MASK/placeholder rendering. Wave 4. Not blocking. | Wave 4 |
| PA-STD-031 | REFACTOR (ASCII source violation incl. runtime strings) | tabs-and-mask (`ff-tabmask`) | 33 non-ASCII bytes (3 non-comment). em-dashes inside runtime `#[error]` strings: error.rs (15/43/49). Non-ASCII in user-facing output is a genuine defect. Rest: doc-comment dashes. | Replace with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-017 | LOGGING-GAP (dead sole-dep) | tabs-and-mask (`ff-tabmask`) | `ff-logging` is the sole Cargo dep yet has 0 uses (DEAD, same as ff-select). Pure session-state/display model so zero-log largely defensible; config-value validation (invalid `editor.default_tab_stops`) is the one natural WARN site. | Drop the dead dep OR wire a config-coercion WARN + dev-logging on TABS/MASK command dispatch. Lower priority than disk/DB catalog crates. | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-TCR-013 | TCR-GAP (coverage enumeration) | tabs-and-mask (`ff-tabmask`) | 1 TCR row for 18 reqs / ~110 criteria; in-file tests exist. Thin. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-INCOMPLETE-007 | INCOMPLETE (CR-NR-057 Req 11 UNBUILT, honest tracking) | command-semantics (`ff-command-semantics`) | 173/181 tasks done, 8 OPEN -- all Task 28 (`chain.rs`) implementing Requirement 11 (Command Chain Parsing + Sequential Execution, CR-NR-057): split_chain (`.`/`;` quote/hex-aware, 28.1-28.2), commands.max_chain_length config (28.3), CommandEngine::execute_chain (left-to-right fail-stop, forward separator to nav stack, 28.4-28.5), menu-workspace fastpath shared-helper wiring (28.6), TCR (28.7). Req 11 (8 criteria) entirely unbuilt. HONEST `[ ]` tracking (NOT a false-positive like PA-INCOMPLETE-002/006). | Implement Task 28 (chain.rs + execute_chain + max_chain_length + tests + TCR). Coordinate with PA-INCOMPLETE-003 (Context Navigation Stack, cmd-framework Req 10 = Phase DH; Req 11.5 depends on it) and menu-workspace fastpath (28.6, W3.5). Fold into the CR-NR-057 Phase DH chaining bundle. | task-revision (HIGH; CR-NR-057) |
| PA-STD-032 | REFACTOR (400-line cap) | command-semantics (`ff-command-semantics`) | `tso.rs` = 447 non-test lines, over the cap (Req 9/10 TSO P1/P2 commands: OUTPUT/CANCEL/SEND/PROFILE/PRINTDS + FTSO operands). Only file over cap. | Split by TSO command group. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-018 | LOGGING-GAP (command pipeline dev-logging) -- CR-NR-058 | command-semantics (`ff-command-semantics`) | `ff-logging` declared but 0 uses (DEAD). This is THE command execution pipeline -- every command flows parse->scope->execute->status. The uniform per-command instrumentation belongs at the command-framework execute_command boundary (PA-W0.2) which this dispatches through, so the bulk is covered automatically once PA-W0.2 lands. | After PA-W0.2, add command-semantics-specific dev-logging: scope-resolution decisions, Req 11 chain step-by-step, error-path WARN, under the `dev-logging` gate. Tightly coupled to CR-NR-058 command instrumentation. | task-revision (MEDIUM; dev-logging owner/Req-13 aware) |
| PA-WATCH-017 | WATCH (shared chain-parser helper) | command-semantics / menu-workspace | Req 11 + Task 28.6 require `split_chain` to be a SHARED pure helper used by BOTH command-semantics (command-line chains) and menu-workspace (fastpath Chained_Path resolver). Designed to avoid a duplicate chain parser. | Verify the menu-workspace fastpath calls ff-command-semantics::split_chain, not a duplicate. Confirm at W3.5 (menu-workspace). Currently UNBUILT (PA-INCOMPLETE-007). | W3.5 (menu-workspace) |
| PA-STD-033 | REFACTOR (400-line cap) | command-completion (`ff-completion`) | `engine.rs` = 478 non-test lines, over the cap (Completion_Engine: candidate generation + filtering + ranking + selection state). Only file over cap. | Split by concern (candidate aggregation / filter+rank / selection-state). REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-019 | LOGGING-GAP (dead dep + mandated provider-failure log) | command-completion (`ff-completion`) | `ff-logging` declared but 0 uses (DEAD). THREE mandated logs unimplemented: Req 6.5/9.5 (invalid `completion.*` config -> fall back + WARN), Req 10.5 (a CompletionProvider that fails/panics -> Completion_Engine SHALL catch AND log). Req 10.5 is substantive -- a failing plugin provider must be diagnosable. | Wire ff-logging for the Req 10.5 provider-failure catch (substantive) + Req 6.5/9.5 config-coercion WARN; dev-logging on trigger/candidate-generation. Depends on Phase PA-W0.1 gate. | task-revision (LOW-MED; dev-logging owner/Req-13 aware) |
| PA-STD-034 | REFACTOR (ASCII source violation -- comment-only) | command-completion (`ff-completion`) | 60 non-ASCII bytes in `.rs`, ALL in comments (box-drawing banners + em/en dashes; 0 in runtime strings -- clean like structure-catalog). documentation.md violation. | Replace em-dashes with `--`, box-drawing with `// === ... ===`. REFACTOR, no gate. | 6 (consolidation) |
| PA-TCR-014 | TCR-GAP (coverage enumeration) | command-completion (`ff-completion`) | 1 TCR row for 10 reqs / ~90 criteria; in-file tests exist. Thin (contrast sibling command-semantics 39 rows). | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-CONFLICT-009 | CONSISTENCY CONFLICT (duplicate fuzzy matcher) | command-palette (`ff-desktop/src/command_palette/fuzzy.rs`) / command-completion (`ff-completion/src/matching/fuzzy.rs`) | Two fuzzy subsequence matchers doing the same job for the two closely-related ISPF-discoverability features. Palette: `fuzzy_match(query,target)->bool` + `fuzzy_score->i32`. Completion: `fuzzy_match(query,candidate,case_sensitive)->Option<FuzzyMatchResult>` + `highlight_positions`. Already DIVERGED in signature. ff-desktop has NO ff-completion dep, so the palette cannot reuse it. Same duplicate-logic anti-pattern as PA-CONFLICT-003/004/006/008. | Owner-gated: extract a single fuzzy-match engine (a small `ff-fuzzy` crate OR expose ff-completion's matcher) consumed by BOTH command-completion and command-palette so ranking/highlight is identical. Both impls small/self-contained (low risk). | later (owner-gated; LOW-MED) |
| PA-TCR-015 | TCR-GAP (total absence, small surface) | command-palette (`ff-desktop/src/command_palette/`) | ZERO TCR rows across 5 reqs; in-file tests exist (fuzzy match/score). Small surface. | Add TCR rows for the 5 palette reqs citing existing tests. No code. | 6 (consolidation) -- LOW |
| PA-LOG-020 | LOGGING-GAP (optional dev-logging) | command-palette (`ff-desktop/src/command_palette/`) | 0 log calls; no mandated logging (pure UI overlay). Command invocations from the palette dispatch through the command framework so PA-W0.2 per-command instrumentation covers them automatically. | Optional dev-logging on palette open/select under the `dev-logging` gate. Not required. | task-revision (LOW; optional) |
| PA-TRACK-003 | TRACKING GAP (bookkeeping, not feature) | command-configurator (`ff-desktop/src/command_config/`) | 24/26 tasks done, 2 OPEN -- both pure bookkeeping: 6.2 (update TCR rows to correct status) + 6.3 (mark project-master tasks complete). The implementation + tests are DONE (17 TCR rows exist; all 4 reqs covered). Same class as PA-TRACK-001/002 (stale checkboxes). | Verify code/tests complete; check off 6.2/6.3 + set TCR rows to real status. No feature work. | 6 (consolidation) -- LOW |
| PA-WATCH-018 | WATCH RESOLVED CLEAN (W3.8) -- external-exec security gate | command-configurator / shell-command | Command Configurator Req 3 external command execution is DELEGATED to shell-command (Req 19 external exec engine); the spec says "all external execution is gated by the existing `shell.mode` configuration ... no separate switch". | Confirm the `shell.mode` gate actually covers external commands launched via the configurator (Detached Started_Task / Captured_Run). Verify at shell-command (W3.8). | W3.8 (shell-command) |
| PA-WATCH-019 | WATCH (menu-workspace-pattern raw-TOML stores) | command-configurator / ff-select / menu-workspace | `store.rs` uses raw `std::fs` (read/write/metadata/create_dir_all) + own mtime hot-reload for `commands.toml`, bypassing ff-config layers -- framed as the "menus/ directory pattern". Same raw-file-store approach as ff-select `.criteria.json` (PA-WATCH-015) and menu-workspace `menus/`. NB: local user-config files, not workbench documents, so NOT an FFW-ARCH-001 VFS issue. | Assess whether the menu-workspace-pattern stores (commands.toml / menus/ / criteria) should share a common loader/hot-reload helper and/or route through ff-config, OR are intentionally direct. Confirm the shared pattern at menu-workspace (W3.5). | W3.5 (menu-workspace) -- LOW |
| PA-LOG-021 | LOGGING-GAP (store-event dev-logging) | command-configurator (`ff-desktop/src/command_config/`) | 0 log calls. Req 1.4 (skip duplicate id), 1.6 (invalid TOML/missing field), 1.7 (hot-reload) describe skip/reload events that a WARN would aid debugging (not explicitly mandated as "SHALL log"). External EXECUTION logging belongs to shell-command (W3.8), not here. | Dev-logging on Command_Store parse errors / skipped-duplicate / hot-reload under the `dev-logging` gate. Low priority (store errors surfaced to user). | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-INCOMPLETE-008 | INCOMPLETE (CR-NR-057 chained navigation UNBUILT, honest tracking) | menu-workspace (`ff-desktop/src/menu_workspace/`) | 75/90 tasks, 15 OPEN. Substantive unbuilt work = CR-NR-057 separator-aware Chained_Path (Task 21.1-21.4 + DF.1-DF.5): base `=0.Themes` fastpath IS built (resolve_chained_path->String, CR-NR-045), but the CR-NR-057 upgrade is not -- ordered `Vec<PathStep>` with STOP(`.`)/PUSH(`;`) separators, Navigation_Origin (POM for leading `=`, current Workspace otherwise), PathStep dispatch pushing intermediate Contexts, `MENU <name> <key>` chained form, and the SHARED navigation-activation helper (Req 5.5 / DF.3 = PA-WATCH-017). Plus menu persistence (1.2/1.3) + bookkeeping (8.1/8.2/21.5). Honest `[ ]`. | Implement the CR-NR-057 separator-aware Chained_Path (Task 21 + DF.1-5) as the 3rd leg of the command-chaining bundle, coordinated with PA-INCOMPLETE-007 (cmd-semantics Req 11 split_chain/execute_chain) + PA-INCOMPLETE-003 (cmd-framework Req 10 Context Navigation Stack) = unified Phase DH. Finish menu persistence (tie to W3.9). | task-revision (HIGH; CR-NR-057) |
| PA-STD-035 | REFACTOR (400-line cap) | menu-workspace (`ff-desktop/src/primary_option_menu.rs`) | `primary_option_menu.rs` = 537 non-test lines, over the cap (the POM instance of the menu pattern). menu_workspace/ module files all under cap (loader 311). | Split the POM by concern (options model / render / dispatch). REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-022 | LOGGING-GAP (dev-logging on dispatch) -- CR-NR-058 | menu-workspace (`ff-desktop/src/menu_workspace/`) | Already logs menu-load/hot-reload errors (4 ff-logging calls -- NOT a dead dep, one of few W2-3 shell units that logs). Gap: no dev-logging on menu option dispatch / chained-path resolution. | Add dev-logging on option dispatch + (post-build) chained-path PathStep resolution under the `dev-logging` gate. Existing menu-load logging is a good baseline. | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-TRACK-004 | TRACKING GAP (bookkeeping) | menu-workspace (`ff-desktop/src/menu_workspace/`) | Among the 15 open tasks, several are tracking-only: 1.3 (persistence round-trip tests), 8.1 (TCR CR-NR-045 rows), 8.2 (project-master Phase CU-impl), 21.5 (TCR CR-NR-057 Req 5 rows). Distinct from the PA-INCOMPLETE-008 feature work. | Verify + check off the tracking-only tasks; set TCR rows to real status. No feature work (the feature tasks are PA-INCOMPLETE-008). | 6 (consolidation) -- LOW |
| PA-WATCH-020 | RESOLVED W4.1 (spec scope-creep -- relocate reqs) | menu-and-statusbar (`ff-menu`) / layout-and-docking (`ff-layout` = OWNER) | Reqs 17 (Tab Window Chrome), 18 (Detachable Tab Windows), 19 (ISPF Navigation: SCROLL field, Fastpath, Split Screen, List Panel LOCATE) are in the menu-and-statusbar spec but ff-menu has NO implementation (grep: 0 detach/split/floating). They overlap layout-and-docking (detach/split), multi-tab-editor (tab chrome), and navigation/menu-workspace (fastpath/SCROLL). CR-appended reqs landed in the wrong spec. Tasks tracked `[x]` here despite no ff-menu code (impl presumably in Wave-4 crates). | At Wave 4 (layout-and-docking + multi-tab-editor) confirm Reqs 17-19 are implemented THERE; move/cross-reference these requirements to the owning specs to avoid double-ownership. Spec-hygiene, not code duplication. | Wave 4 (MEDIUM) |
| PA-STD-036 | REFACTOR (ASCII source violation, runtime string) | menu-and-statusbar (`ff-menu`) | 17 non-ASCII bytes (1 non-comment): em-dash in a runtime `#[error]` string (error.rs:21, "must be 1-64 ASCII"). Non-ASCII in user-facing output. Rest doc-comment dashes. | Replace with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-023 | LOGGING-GAP (dead dep + recent-files WARN) | menu-and-statusbar (`ff-menu`) | `ff-logging` declared but 0 uses (DEAD). recent_files.rs I/O errors returned as MenuError::RecentFilesIoError, not logged. Menu actions dispatch through command-framework so PA-W0.2 covers menu-command execution. | Wire a WARN on recent-files read/write failure + resolve the dead dep; optional menu-open dev-logging. | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-TCR-016 | TCR-GAP (coverage enumeration) | menu-and-statusbar (`ff-menu`) | 2 TCR rows for 16 reqs / ~130 criteria; in-file tests exist. Thin. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-INCOMPLETE-009 | INCOMPLETE (Req 17 + Req 19 partial, honest tracking) | function-keys-and-history (`ff-keys`) | 247/271 tasks, 24 OPEN. Req 17 (END/RETURN Navigation, Tasks 33-34): revise END/RETURN handlers in ff-desktop/src/shell/commands.rs so a POM CLOSES the Workspace (or file.exit if only one) instead of exiting -- CR-NR-057 POM-nav-model adjacent; substantial existing code (91 refs). Req 19 (RETRIEVE Argument-Driven Recall + Browser, DF.1-6): recall_list/recall_by_number API, not-recorded guard, ff-desktop History_List overlay -- PARTIAL (retrieve.rs already has RetrieveResult::ShowList + LIST tests; open work = helper API shape + overlay UI). Plus verification (35 label-bar blank-slot, 36 END/RETURN excluded-set) + bookkeeping (37 TCR). Honest `[ ]`. | Finish Req 17 END/RETURN-from-POM handler revision (coordinate with menu-workspace PA-INCOMPLETE-008 + Context Navigation Stack PA-INCOMPLETE-003 -- same POM/nav model) + Req 19 RETRIEVE recall API + History_List overlay (DF.1-6); verification (35/36) + TCR (37). | task-revision (MEDIUM; CR-NR-057-adjacent) |
| PA-SPLIT-009 | SPLIT PROPOSAL (LOW, deferred) | function-keys-and-history (`ff-keys`) | 21 reqs / 524 lines. Two arguably-separable concerns: function-keys (key maps + label bar + PFSHOW, Req 1-4/12-16/20-21) vs command-history (history + RETRIEVE + dropdown, Req 5-11/19). But crate is well-sized (no cap violation) and the two share config/context plumbing -- low split value. | Optional owner-gated spec split; low value, defer. No action. | later (owner-gated; LOW) |
| PA-STD-037 | REFACTOR (ASCII source violation, runtime strings) | function-keys-and-history (`ff-keys`) | 119 non-ASCII bytes (11 non-comment): em-dashes inside runtime `#[error]` strings (error.rs 11-90, ~6 messages) + a test comment. Non-ASCII in user-facing output. Rest doc-comment dashes. | Replace with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-024 | LOGGING-GAP (dead dep + error-typed paths) | function-keys-and-history (`ff-keys`) | `ff-logging` declared but 0 uses (DEAD). error.rs has rich variants (key-parse, key-map-entry, history-load/parse/save, config, dispatch) all returned as KeysError, none logged. history-save/load (Req 6 persistence) + config coercion (Req 11) are natural WARN sites. Key dispatch flows through command-framework (PA-W0.2). | Wire ff-logging for history load/save/parse + config coercion + key-map-entry errors; dev-logging on key dispatch + RETRIEVE. | task-revision (LOW-MED; dev-logging owner/Req-13 aware) |
| PA-WATCH-021 | WATCH (command-field overlay consistency) | function-keys-and-history / command-completion / command-palette | FOUR command-field overlay UIs: history dropdown (Req 10) + RETRIEVE History_List (Req 19) + completion popup (W3.2) + command palette (W3.3). Different content but all command-field overlays. | Confirm shared positioning/keyboard-nav infrastructure (or intentionally separate) at Wave 4 shell. Not a conflict; UI-consistency. | Wave 4 (LOW) |
| PA-STD-038 | REFACTOR (400-line cap, 2 files) | shell-command (`ff-shell`) | `engine.rs` = 523 non-test (shell-mode gate + execution orchestration); `emulator.rs` = 535 non-test (terminal emulator). Both over the cap. | Split engine.rs by concern (mode gate / dispatch / outcome); emulator.rs split folds into PA-SPLIT-010. REFACTOR, no gate. | 6 (consolidation) |
| PA-SPLIT-010 | SPLIT PROPOSAL (LOW, owner-gated) | shell-command (`ff-shell`) | 19 reqs / 405 lines / 27 files. The terminal EMULATOR (emulator.rs 535) is arguably a separable concern from the command-execution engine (exec/capture/pipe). | Optional owner-gated split of the emulator into its own module/crate; low priority (crate cohesive around SHELL). Defer. | later (owner-gated; LOW) |
| PA-WATCH-022 | WATCH (TSO verb disambiguation) | shell-command / command-semantics | shell-command provides `TSO` as a SHELL alias (run OS command); command-semantics Req 9/10 provides emulated mainframe `TSO` commands (OUTPUT/CANCEL/SEND/PROFILE/PRINTDS). Two meanings of `TSO`. | Confirm the `TSO` verb dispatch is disambiguated (which handler wins on `TSO ...`); likely intentional (OS shell vs emulated TSO) but verify no collision. | Wave 5 (or shell wiring) -- LOW |
| PA-LOG-025 | LOGGING-REFINEMENT (audit gate denial) -- CR-NR-058 | shell-command (`ff-shell`) | ACTUALLY LOGS (10 calls -- NOT a dead dep; positive exemplar). Refinement: ensure the shell.mode gate DENIAL is logged at INFO/WARN (security audit) and add dev-logging on external command spawn/params(redacted)/exit-code under the `dev-logging` gate. | Audit the 10 existing logs cover the security-relevant events (gate denial, spawn, exit); add dev-logging spawn/exit. Refinement, not a dead-dep fix. | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-STD-039 | REFACTOR (ASCII source violation, runtime string) | shell-command (`ff-shell`) | 65 non-ASCII bytes (1 non-comment): em-dash in a runtime `#[error]` string (error.rs:107 "macro: shell access denied -- {reason}"). Rest doc-comment dashes. | Replace with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-SPLIT-011 | SPLIT PROPOSAL (MEDIUM, owner-gated) | startup-and-session (`ff-session`) | 16 reqs / 564 lines, two natural clusters: LIFECYCLE (startup ordering / config orchestration / exit / crash-recovery / graceful-degradation / CLI) vs SESSION-STATE persistence/restore (session_state.rs 529 + session_file + session_restore + window_geometry + workspace descriptors + recent_files). | Owner-gated: split `session-state` (SessionState/WorkspaceDescriptor model + restore) from `startup-lifecycle`. The 529-line session_state.rs is the natural extraction. Not forced. | later (owner-gated; MEDIUM) |
| PA-STD-040 | REFACTOR (400-line cap) | startup-and-session (`ff-session`) | `session_state.rs` = 529 non-test lines, over the cap (SessionState + WorkspaceDescriptor + TabState model). Only file over cap. | Split (feeds PA-SPLIT-011). REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-026 | LOGGING-GAP (dead dep on FAULT-TOLERANCE subsystem) -- CR-NR-058 | startup-and-session (`ff-session`) | `ff-logging` declared but 0 uses (DEAD) -- most notable gap so far: ff-session is the fault-tolerance subsystem (STARTUP ordering, CRASH RECOVERY Req 10, GRACEFUL DEGRADATION Req 11 "no single corrupt/missing file prevents startup"). error.rs has exactly the events that MUST be logged (user-data-dir unavailable, session/recovery file corrupt, write failed, plugin-init failed, recovery scan failed) -- ALL returned as SessionError, NONE logged. A silently-degrading startup is undebuggable. | Wire ff-logging: WARN on each skipped/corrupt/missing file (Req 11 degradation), ERROR on unrecoverable, INFO on startup-sequence milestones + crash-recovery actions (Req 10). 2nd-highest-value logging target in W2-3 after ff-dscatalog (PA-LOG-012). Depends on Phase PA-W0.1. | task-revision (MEDIUM-HIGH; dev-logging owner/Req-13 aware) |
| PA-STD-041 | REFACTOR (ASCII source violation, runtime strings) | startup-and-session (`ff-session`) | 89 non-ASCII bytes (9 non-comment): em-dashes inside runtime `#[error]` strings (error.rs, 6+ messages: user-data-dir/session-corrupt/write-failed/plugin-init/recovery-scan/recovery-corrupt). Non-ASCII in user-facing output. Rest doc-comment dashes. | Replace with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-WATCH-023 | WATCH (persistence-vs-functionality scope) | startup-and-session / virtual-catalog-manager / menu-workspace / shell | ff-session Reqs 13 (Desktop Shell Editor Interactions), 14 (ISPF POM + Tabbed Window Container), 19 (File Explorer Context POM Option 2), 20 (TSO Session Lifecycle LOGOFF/TIME/STATUS) touch other units' functionality. ff-session DOES have code (18 refs) but the boundary should be PERSISTENCE/routing only; the POM/File-Explorer functionality = VCM (W2.2) + menu-workspace (W3.5), TSO = shell/cmd-semantics. Same scope-creep family as PA-WATCH-020. | Confirm Reqs 13/14/19/20 describe ff-session's persistence/routing slice, not duplicate functionality. Relocate/cross-ref if needed. | Wave 4 (LOW) |
| PA-STD-042 | REFACTOR (400-line cap, SEVERE, CROSS-CUTTING) | ff-desktop `shell/` coordinator (WorkbenchShell) -- recorded at workspace-model W3.10 | The ff-desktop `shell/` module holds the WORST 400-cap violations in the codebase: `commands.rs` 1255 (~3x cap), `update.rs` 1011, `mod.rs` 922, `render.rs` 873, `render_chrome.rs` 580 (`tests.rs` 4105 excluded -- test module). rust-standards.md prescribes splitting shell into state/render/commands/dialogs; the layout is violated in-place. Cross-cutting (shell coordinator hosting all workspaces/tabs), not owned by one requirements unit. | Split the ff-desktop `shell/` coordinator per the prescribed `state/render/commands/dialogs` layout. REFACTOR, no gate. Pairs with VCM PA-STD-025 (files_panel 1195) as the two largest shell refactors. | 6 (consolidation) -- HIGH within STD |
| PA-TCR-017 | TCR-GAP (total absence, small surface) | workspace-model (`ff-session/src/workspace.rs`) | ZERO TCR rows across 6 reqs; in-file tests exist. Small surface. | Add TCR rows for the 6 workspace-model reqs. No code. | 6 (consolidation) -- LOW |
| PA-WATCH-024 | WATCH (declared unblock dependency) | workspace-model / command-palette / global-search | The spec states workspace-model "unblocks the Command Palette (needs a workspace scope for search) and Global Search (needs a workspace root set)". Confirm the palette + global-search actually consume the WorkspaceState root set (the declared unblock). | Verify Command Palette + Global Search consume the workspace root set from WorkspaceState. At Wave 5 (global-search) + palette. | Wave 5 (LOW) |
| PA-STD-043 | REFACTOR (400-line cap, SEVERE ~2x) | layout-and-docking (`ff-layout`) | `engine.rs` = 812 non-test lines, ~2x the cap (LayoutEngine coordinating panels / tab-groups / floating-windows / personas / serialization). Only file over cap (other 30 decomposed). | Split by concern (panel / tab-group+split / floating-window / persona+serialization). REFACTOR, no gate. Among the larger single-file violations. | 6 (consolidation) |
| PA-STD-044 | REFACTOR (ASCII source + spec mojibake) | layout-and-docking (`ff-layout` + spec) | 61 non-ASCII bytes in `.rs` (2 non-comment: em-dashes in runtime `#[error]` strings, error.rs 40/95). ALSO the spec .md intro has a mojibake `A-section-3` (BOM/UTF-8 artifact, should be "Section 3"). | Replace `.rs` em-dashes with `--`; fix the spec-md mojibake in the doc ASCII sweep. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-027 | LOGGING-GAP (dead dep, LOW) | layout-and-docking (`ff-layout`) | `ff-logging` declared but 0 uses (DEAD). Layout serialization failures (Req 6) return LayoutError, not logged. Not a fault-tolerance subsystem (bad persona falls back to default), so zero-log largely defensible. | Wire a WARN on layout-serialization load/save failure + dev-logging on persona switch / detach / dock; resolve dead dep. | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-TCR-018 | TCR-GAP (coverage enumeration) | layout-and-docking (`ff-layout`) | 1 TCR row for 11 reqs / ~90 criteria (large 31-file crate); in-file tests exist. Thin. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-CONFLICT-010 | CONSISTENCY CONFLICT (requirement triple-specified, ownership ambiguity) | multi-tab-editor / menu-and-statusbar / layout-and-docking | The "Tab Window Chrome" requirement (three-element header: Tab_Header row + Title_Line + Command_Line, regardless of docked/floating) is specified in THREE specs verbatim-similar: multi-tab-editor Req 15, menu-and-statusbar Req 17, layout-and-docking Req 11. The actual IMPLEMENTATION is in NONE of the three model crates -- it is in `ff-desktop/src/shell/render_chrome.rs` (part of the PA-STD-042 shell files). One shell impl, three claiming specs, no owner. Extends/subsumes PA-WATCH-020 (which flagged only the menu-statusbar side). | Owner-gated: designate ONE owner for Tab Window Chrome (recommend multi-tab-editor Req 15 or a dedicated shell spec); menu-and-statusbar Req 17 + layout-and-docking Req 11 CROSS-REFERENCE it. Spec-hygiene + single-owner decision; no code move (impl already unified in the shell). | later (owner-gated; MEDIUM) |
| PA-STD-045 | REFACTOR (ASCII source violation, runtime string) | multi-tab-editor (`ff-tabs`) | 15 non-ASCII bytes (1 non-comment): em-dash in a runtime `#[error]` string (error.rs:36 "resource open failed ... -- {reason}"). Rest doc-comment dashes. | Replace with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-028 | LOGGING-GAP (dead dep, LOW) | multi-tab-editor (`ff-tabs`) | `ff-logging` declared but 0 uses (DEAD). Tab open/close/split dispatch through command-framework (PA-W0.2 covers). resource-open-failed error is a natural WARN site. | WARN on resource-open failure + dev-logging on tab open/close/split; resolve dead dep. | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-TCR-019 | TCR-GAP (coverage enumeration) | multi-tab-editor (`ff-tabs`) | 1 TCR row for 15 reqs / ~120 criteria; in-file tests exist. Thin. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-CONFLICT-011 | CONSISTENCY CONFLICT (orphan model crate vs shell reimplementation) | file-tree-panel (`ff-file-tree`) / virtual-catalog-manager (ff-desktop panels) | The `ff-file-tree` crate is a clean, tested, well-decomposed tree MODEL (TreeNode/FileTreeState/filter/sort/keyboard, deps thiserror/serde only) but is used by NO crate (grep of all Cargo.toml = only its own entry -- ORPHANED). The actual shipping file explorer is ff-desktop `file_explorer_panel.rs` (935) + `files_panel.rs` (1195) with its OWN `FileExplorerPanelState` and ZERO ff-file-tree references; ff-desktop has no ff-file-tree dep. Two parallel file-tree implementations; the tested crate is dead weight. Worst duplication in the analysis (crate-scale). SAME panels as VCM (W2.2). | Owner-gated: (a) rewire the ff-desktop file-explorer panels to CONSUME ff-file-tree (preferred -- also relieves ~half of PA-STD-025 by relocating model logic into the tested crate); OR (b) delete the orphan crate if the shell is canonical. Untangle TOGETHER with PA-STD-025 (files_panel 1195/file_explorer 935) + PA-CONFLICT-005 (posix provider) as ONE file-explorer-panel refactor. | later (owner-gated; HIGH) |
| PA-SPLIT-012 | SPLIT PROPOSAL (LOW, owner-gated) | file-tree-panel (`ff-file-tree`) | 23 reqs / 1015 lines. Native-file-dialog integration (Req 22/23 egui-file-dialog) is arguably separable from the tree model. | Optional spec split of native-dialog integration from the tree model; subsumed by PA-CONFLICT-011 priority. Low value. | later (owner-gated; LOW) |
| PA-TCR-020 | TCR-GAP (coverage enumeration) | file-tree-panel (`ff-file-tree` + ff-desktop panels) | 5 TCR rows for 23 reqs / ~180 criteria (largest Wave-4 spec). The orphan crate's tests exist; the shell-panel side is under-covered. Thin. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-LOG-029 | LOGGING-GAP (deferred to rewire, LOW) | file-tree-panel (`ff-file-tree` / ff-desktop panels) | `ff-file-tree` crate is a pure MODEL with NO ff-logging dep (not a dead dep -- it doesn't declare one), 0 fs, 0 non-comment non-ASCII. Async directory load (Req 3) + file-watch (Req 5) are the natural logging sites but live in the (shell) render side. | Once the panels are rewired onto ff-file-tree (PA-CONFLICT-011 option a), add dev-logging on async load / file-watch + WARN on load failure at that point. No standalone dead-dep issue. | task-revision (LOW; deferred) |
| PA-INCOMPLETE-010 | INCOMPLETE (Req 16 OS mode-follow, 1 task, honest) | theme-and-appearance (`ff-theme`) | 171/172 tasks, 1 OPEN = Task 20 "OS dark/light mode follow (Phase CR)" = Requirement 16 (switch the theme when the OS switches dark/light). Genuine small feature, unbuilt, honestly tracked. Reqs 1-11/13-15 done. | Implement Req 16 OS theme-follow (query OS appearance + switch visual mode). Small, self-contained; LOW priority. | task-revision (LOW) |
| PA-STD-046 | REFACTOR (400-line cap) | theme-and-appearance (`ff-theme`) | `defaults.rs` = 661 non-test lines, over the cap (built-in dark/light/high-contrast default palettes + all colour groups + StyleSlotTable defaults). Only file over cap. | Split by palette / colour-group. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-030 | LOGGING-GAP (dead dep + mandated WARN; satisfies text-decorations delegation) | theme-and-appearance (`ff-theme`) | `ff-logging` declared but 0 uses (DEAD). Req 7.4 (invalid TOML -> "LOG A WARNING") + Req 7.5 (invalid values -> fall back + warn) unimplemented. text-decorations Req 15.8 (W1.15, PA-LOG-011) DELEGATES its theme-value-validation WARN HERE -- so this is the theme-side of that delegation. A user's broken theme silently falling back is confusing. | Wire ff-logging for Req 7.4/7.5 theme-load + invalid-value WARNs (satisfies text-decorations Req 15.8 delegation); dev-logging on theme switch/hot-reload. Resolve dead dep. | task-revision (LOW-MED; dev-logging owner/Req-13 aware) |
| PA-WATCH-025 | RESOLVED CLEAN W4.9 (single-crate producer+verifier) | theme-and-appearance / accessibility | ff-theme Req 5 owns the high-contrast VISUAL MODE (palette producer); accessibility (W4.9) likely owns WCAG contrast-ratio VERIFICATION. Confirm the producer/verifier boundary. | Verify at accessibility (W4.9): high-contrast palette = ff-theme; contrast-ratio checks = accessibility. No overlap/duplication. | W4.9 (LOW) |
| PA-STD-047 | REFACTOR (ASCII source violation, runtime string) | view-zoom (`ff-zoom`) | 112 non-ASCII bytes (2 non-comment): em-dash in a runtime `#[error]` string (error.rs:25; :68 is a test). Rest doc-comment dashes. | Replace with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-031 | LOGGING-GAP (dead dep, LOW) | view-zoom (`ff-zoom`) | `ff-logging` declared but 0 uses (DEAD). Pure display-state model; invalid ZOOM arg + range clamping (Req 4) surface via status/error. | Resolve dead dep; optional dev-logging on ZOOM command + clamp events. | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-TCR-021 | TCR-GAP (coverage enumeration, LOW) | view-zoom (`ff-zoom`) | 5 TCR rows for 9 reqs / ~60 criteria; in-file tests exist. Modest (small surface). | Extend per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) -- LOW |
| PA-STD-048 | REFACTOR (400-line cap) | hex-display (`ff-hex`) | `controller.rs` = 417 non-test lines, just over the cap (hex-mode controller: layout/editing/cursor-sync/scroll orchestration). Only file over cap. | Split by concern (view+layout / editing / cursor+scroll). REFACTOR, no gate. | 6 (consolidation) |
| PA-STD-049 | REFACTOR (ASCII violation -- box-drawing + em-dash in RUNTIME strings) | hex-display (`ff-hex`) | 59 non-ASCII bytes (3 non-comment). NOTABLE: `layout.rs:9` + `dump.rs:82` use a box-drawing vertical bar (U+2502) as a runtime SEPARATOR constant in the hex-dump pane output -- box-drawing is allowed in `.md` but PROHIBITED in `.rs` (documentation.md), and this is RUNTIME output. Plus an em-dash in a runtime warning (editing.rs:122). | Replace the U+2502 separator with ASCII `|`; em-dash with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-032 | LOGGING-GAP (dead sole-dep, LOW) | hex-display (`ff-hex`) | `ff-logging` is the sole Cargo dep yet 0 uses (DEAD). Pure byte-view/edit model; invalid hex input + goto-out-of-range surface via error/status. | Resolve dead dep; optional dev-logging on hex edit / goto / dump-export under the `dev-logging` gate. | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-TCR-022 | TCR-GAP (coverage enumeration) | hex-display (`ff-hex`) | 1 TCR row for 16 reqs / ~110 criteria; in-file tests exist. Thin. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-INCOMPLETE-011 | INCOMPLETE (two UI surfaces unbuilt, honest) | notification-system (`ff-desktop/src/notification/`) | 24/35 tasks, 11 OPEN. MODEL complete: NotificationQueue + notify API (Req 3, mod.rs 189) + Event Log Context (Req 2, event_log_panel.rs 163). UNBUILT: Req 1 Toast Notifications (Task 4.1-4.7 -- `notification/toast.rs` does NOT exist: egui Area overlay, auto-dismiss Info/Success, sticky Warning/Error, "N more..." overflow, render_toasts() in shell/render.rs, tests) + Req 4 Status Bar Integration (Task 6.1-6.4 -- bell icon + unread badge, bell-click opens EventLog + mark_all_read, hide badge when unread==0, tests). Honest `[ ]`; user-visible gap (no toast feedback, no notification indicator). | Implement toast overlay (Task 4) + status-bar bell/badge (Task 6) over the existing NotificationQueue; wire the bell into the ff-menu status bar (W3.6). Self-contained egui UI. | task-revision (MEDIUM) |
| PA-LOG-033 | LOGGING-NOTE (emitter mirrors to ff-logging, LOW) | notification-system / emitting subsystems | notification-system is the USER-FACING event channel (distinct from ff-logging dev channel) -- correctly has no ff-logging dep. Note: an emitting subsystem that surfaces a Warning/Error to the user via NotificationQueue SHOULD typically also log it to ff-logging for support diagnosability -- but that is the EMITTER's responsibility, not the notification module's. | Guidance for emitting subsystems: mirror Warn/Error notifications to ff-logging. No change to the notification module. | task-revision (LOW; note) |
| PA-INCOMPLETE-012 | INCOMPLETE (enable/disable actions unbuilt, honest) | plugin-manager-ui (`ff-desktop/src/plugin_manager_panel.rs`) | 18/24 tasks, 6 OPEN. BUILT: Req 1 plugin listing (reads ff-plugin PluginRegistry, 92 refs) + Req 3 basic details. UNBUILT: Req 2 Enable/Disable (Task 3.1-3.5 -- Disable/Enable buttons calling ff-plugin activate()/deactivate() NOT present, only a doc-comment claim; activation-failure display 3.3; disabled-plugin session-TOML persistence 3.4; tests 3.5) + Task 4.3 config-keys display. ff-plugin exposes 9 lifecycle methods (activate/deactivate/enable/disable, W0.13) -- API is READY; this is UI wiring. A plugin manager that cannot enable/disable is a functional gap. Honest `[ ]` (doc-comment overstates). | Wire Enable/Disable buttons -> ff-plugin activate()/deactivate() + activation-failure display + disabled-plugin session persist + config-keys display + tests; correct the doc-comment. | task-revision (MEDIUM) |
| PA-LOG-034 | LOGGING-GAP (activation-failure, deferred, LOW) | plugin-manager-ui / plugin-architecture (`ff-plugin`) | The panel has no ff-logging dep (UI). On plugin activation FAILURE (Task 3.3, unbuilt), the failure should be LOGGED by ff-plugin (W0.13 owns plugin lifecycle) + shown in the panel. | Ensure ff-plugin logs activation failures; panel shows the error; dev-logging on enable/disable actions once built. Deferred with PA-INCOMPLETE-012. | task-revision (LOW; deferred) |
| PA-INCOMPLETE-013 | INCOMPLETE (screen-reader/accesskit unbuilt, honest) | accessibility (cross-cutting; ff-desktop for Req 4) | 16/22 tasks, 6 OPEN. BUILT: Req 1 WCAG contrast (ff-theme/contrast.rs), Req 2 keyboard-only, Req 3 focus indicators (19 refs, mostly), Req 5 reduced-motion (21 refs). UNBUILT: Req 4 Screen Reader Support (Task 4.1-4.4 -- NO `accesskit` feature in ff-desktop Cargo.toml, no accessible button labels, no live-region status bar, no tests) + Req 3 verification (3.2 context-menu keyboard, 3.3 all-dialog-fields-reachable-by-tab test). Honest `[ ]` (accesskit genuinely absent). | Implement Req 4 (egui/accesskit feature + accessible labels + live-region status bar + tests) + finish Req 3 keyboard-reachability verification. NOTE: full WCAG/screen-reader CONFORMANCE requires MANUAL testing with assistive technologies (NVDA/JAWS/VoiceOver) + expert a11y review -- code presence is necessary but not sufficient. | task-revision (MEDIUM; + manual a11y verification) |
| PA-STD-050 | REFACTOR (ASCII source violation, runtime strings -- larger cluster) | context-help (`ff-help`) | 199 non-ASCII bytes (12 non-comment): em/en-dashes inside runtime `#[error]` strings in error.rs (8+ messages -- lookup/content/parse/registry/search/navigation/config) + a config.rs range en-dash ("0.2-0.5"). Non-ASCII in user-facing output; larger cluster because the HelpError type is verbose. Rest doc-comment dashes. | Replace with `--`/`-`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-035 | LOGGING-GAP (dead dep, LOW) | context-help (`ff-help`) | `ff-logging` declared but 0 uses (DEAD). Rich HelpError type (topic-not-found, content-file-not-found, parse, registry lock-poisoned, search, navigation, config) all returned, none logged. Topic-content load/parse failures are natural WARN sites. 3 fs (topic content loading). | Wire ff-logging for topic-load/parse WARN + config coercion; dev-logging on HELP/F1 topic resolution. Resolve dead dep. | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-DEP-003 | DEAD/OVER-DECLARED DEPENDENCIES | clipboard-operations (`ff-clipboard`) | Cargo.toml declares `ff-edit-operations` + `ff-undo-redo` (among others) but the code uses `EditorTransaction` 0 times and `ScrapStack` 0 times -- no non-doc `ff_undo_redo::`/`ff_edit_operations::` usages. Declared-but-unused deps (dead deps, like the dead-ff-logging pattern but for edit/undo), contradicting the crate's clean-seam design (it returns `ClipboardEntry` data; the caller applies the paste). Over-declares coupling + slows build. | Audit + prune the unused deps (confirm ff-edit-operations / ff-undo-redo / ff-line-commands usage; remove if unused). Also resolve the dead ff-logging dep. Low-risk Cargo cleanup. | 6 (consolidation) -- LOW |
| PA-STD-051 | REFACTOR (ASCII source violation, runtime string) | clipboard-operations (`ff-clipboard`) | 54 non-ASCII bytes (1 non-comment): em-dash in a runtime string/comment (paste.rs:104 "mismatch -- caller should use browse"). Rest doc-comment dashes. | Replace with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-036 | LOGGING-GAP (dead dep, LOW) | clipboard-operations (`ff-clipboard`) | `ff-logging` declared but 0 uses (DEAD). Copy/cut/paste dispatch through command-framework (PA-W0.2 covers). Empty-clipboard/paste-mismatch surface via ClipboardError. | Resolve dead dep (part of PA-DEP-003 cleanup); optional dev-logging on copy/cut/paste + file-insert. | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-TCR-023 | TCR-GAP (coverage enumeration) | clipboard-operations (`ff-clipboard`) | 1 TCR row for 20 reqs / ~150 criteria; in-file tests exist. Thin (20-req crate). | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-CONFLICT-012 | CONSISTENCY CONFLICT (orphan scheduler vs inline reimplementation) | idle-processing (`ff-idle-processing`) / syntax-highlighting / line-wrap | `ff-idle-processing` (clean, complete, tested cooperative idle scheduler -- time budget/priority/cancel/progress) is used by NO crate (grep of all Cargo.toml = only its own; 0 IdleWorkContext/ff_idle_processing refs elsewhere -- ORPHAN). Meanwhile syntax-highlighting reimplements idle-styling INLINE (`engine/idle_styling.rs`, own `IdleStylingConfig` `time_budget_ms: 10` = same budget) with no ff-idle dep; line-wrap (ff-wrap) has no ff-idle dep either. The spec's premise (ONE cooperative scheduler grants frame-budget slices to syntax/wrap/fold/search so they don't compete) is NOT realized -- consumers idle independently. Same anti-pattern as ff-file-tree (PA-CONFLICT-011). | Owner-gated: (a) rewire the idle consumers (syntax re-highlight, wrap-height, fold, search-index) onto `ff-idle-processing` (implement IdleWorkSource/context + register) -- PREFERRED, coordinates the frame budget; OR (b) delete the orphan crate if per-consumer inline idle is intentional. Pairs with PA-CONFLICT-011 (ff-file-tree orphan). Code + owner decision. | later (owner-gated; MEDIUM-HIGH) |
| PA-STD-052 | REFACTOR (ASCII source + Cargo-description mojibake) | idle-processing (`ff-idle-processing`) | 18 non-ASCII bytes in `.rs` (1 non-comment: em-dash in a runtime `#[error]` string, error.rs:18). ALSO the Cargo.toml `description` field has a mojibake artifact (BOM/UTF-8, before "progress"). | Replace the `.rs` em-dash with `--`; fix the Cargo.toml description. REFACTOR, no gate. | 6 (consolidation) |
| PA-TCR-024 | TCR-GAP (TOTAL absence) | idle-processing (`ff-idle-processing`) | ZERO TCR rows for the crate (grep = 0) across 12 reqs / ~90 criteria, despite the crate's tests. RECORDING gap. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-LOG-037 | LOGGING-GAP (deferred to wiring, LOW) | idle-processing (`ff-idle-processing`) | Pure scheduler; sole dep thiserror, NO ff-logging (not a dead dep). Once wired to consumers (PA-CONFLICT-012), dev-logging on slice dispatch / budget-exceeded / cancellation would aid debugging background-work starvation. | Add ff-logging + dev-logging on dispatch/budget/cancel when the scheduler is wired to consumers. No dead-dep issue now. | task-revision (LOW; deferred, dev-logging owner/Req-13 aware) |
| PA-CONFLICT-013 | CONSISTENCY CONFLICT (orphan crate, missing render-path integration) | large-file-performance (`ff-large-file-performance`) / viewport-scrolling / shell (ff-desktop) | `ff-large-file-performance` (clean, complete, tested render-optimization toolkit -- ChunkRenderer, LineLayoutCache, PositionCache, ScrollPredictor, InvalidationCoordinator, Surface trait, PerfConfig) is used by NO crate (grep all Cargo.toml = only its own; 0 `ff_large_file_performance` refs elsewhere -- ORPHAN). The shell/render path (ff-desktop) does NOT dep on it (0) and has 0 LineLayoutCache/PositionCache/ChunkRenderer/ScrollPredictor refs -- so it is neither WIRED nor reimplemented inline (unlike idle-styling PA-CONFLICT-012 which had an inline copy). Consequence: the spec's 60fps-for->1M-lines / >10k-char-line promise is very likely NOT realized in the running app. (document-model's 80 chunk/piece refs are the piece-table DOCUMENT STORAGE virtualization -- a different concern, not a duplication of this crate's render caches.) THIRD orphan crate (w/ ff-file-tree PA-CONFLICT-011, ff-idle-processing PA-CONFLICT-012). | Owner-gated: (a) wire the shell/editor render path onto `ff-large-file-performance` (layout/position caches, chunk render, scroll prediction, invalidation) so large-file responsiveness is actually delivered -- PREFERRED; OR (b) reconcile spec + delete the orphan if the perf strategy changed. Large-file layout is the idle scheduler's (PA-CONFLICT-012) prime workload -- one integration effort wires BOTH (scheduler grants slices; this crate caches/chunks). Code + owner decision. | later (owner-gated; MEDIUM-HIGH) |
| PA-STD-053 | REFACTOR (ASCII source violation, runtime strings) | large-file-performance (`ff-large-file-performance`) | 34 non-ASCII bytes (2 non-comment: status.rs:65, status.rs:124 -- likely em-dash / arrow in status-indicator text). Rest doc-comment dashes. | Replace with ASCII substitutes (`--`, `->`). REFACTOR, no gate. | 6 (consolidation) |
| PA-TCR-025 | TCR-GAP (TOTAL absence) | large-file-performance (`ff-large-file-performance`) | ZERO TCR rows for the crate (grep = 0) across 9 reqs / ~70 criteria, despite the crate's tests. Same total-absence pattern as idle-processing PA-TCR-024 (both orphan infra crates). | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-LOG-038 | LOGGING-GAP (deferred to wiring, LOW) | large-file-performance (`ff-large-file-performance`) | Pure render-optimization infra; sole dep thiserror, NO ff-logging (not a dead dep). Once wired (PA-CONFLICT-013), dev-logging on cache hit/miss, invalidation, chunk-render decisions, and scroll prediction would materially aid diagnosing large-file jank. | Add ff-logging + dev-logging on cache/invalidation/chunk/scroll decisions when the crate is wired to the render path. No dead-dep issue now. | task-revision (LOW; deferred, dev-logging owner/Req-13 aware) |
| PA-CONFLICT-014 | CONSISTENCY CONFLICT (orphan crate + unresolved placement) | external-modification (`ff-external-mod`) / file-operations / shell (ff-desktop) / notification-system | `ff-external-mod` (clean, complete, tested external-change detector -- ExternalModificationDetector, WatchRegistry, ChangeType, ExternalChange, BatchCoalescer, FocusGainedChecker, DocumentState, ExternalModConfig; EXEMPLARY VFS discipline, 0 real fs) is referenced by NO crate (grep all Cargo.toml + src = only its own; 0 `ff_external_mod` refs elsewhere -- ORPHAN, the FOURTH). AND its spec intro literally leaves placement unresolved: "the `ff-external-modification` module (part of the `ff-file-operations` crate OR a standalone crate depending on final architecture)". So the orphan status is partly a never-finalized architectural decision. Detector is not wired into the shell/document lifecycle either way, so external-change handling is not delivered to the user. | Owner-gated: (a) resolve the spec-deferred placement decision -- keep `ff-external-mod` standalone (RECOMMENDED, it is cleanly decomposed) OR fold into ff-file-operations; (b) wire the detector into the shell/document lifecycle (register open docs with WatchRegistry; surface ChangeType via the notification system on external-change / focus-gained re-check). Cross-ref W4.15 file-operations for the placement half. Code + owner decision. | later (owner-gated; MEDIUM) |
| PA-DOC-006 | DOC (crate-name drift) | external-modification (`ff-external-mod`) | Spec + module docs call it `ff-external-modification`; the actual crate dir is `ff-external-mod`. Same crate-name-drift pattern as ff-hex/ff-select/ff-tabmask/ff-forge/ff-keys/ff-completion/ff-wrap/ff-seqnum/ff-dscatalog. | Reconcile spec text to `ff-external-mod` (or rename the crate) so docs match the built artifact. Add to the naming-reconciliation set. Docs only. | 6 (consolidation) |
| PA-STD-054 | REFACTOR (ASCII source violation, runtime strings) | external-modification (`ff-external-mod`) | 90 non-ASCII bytes (6 non-comment): em-dashes in `#[error]` runtime strings (error.rs:23,33,43,52,77,95). Rest doc-comment dashes. | Replace with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-039 | LOGGING-GAP (dead dep + missing watcher logging, MEDIUM) | external-modification (`ff-external-mod`) | `ff-logging` declared but 0 uses (DEAD). A file-watcher is exactly where dev-logging pays off: watch register/unregister, raw fs event -> ChangeType classification, batch-coalesce decisions, focus-gained re-check outcomes -- all hard to debug blind (event races, missed/duplicate events). Rated MEDIUM (above the usual LOW) -- strong CR-NR-058 candidate. | Resolve dead dep + add dev-logging on watch lifecycle + event classification + coalescing + focus-gained re-check under the `dev-logging` gate. | task-revision (MEDIUM; dev-logging owner/Req-13 aware) |
| PA-TCR-026 | TCR-GAP (TOTAL absence) | external-modification (`ff-external-mod`) | ZERO TCR rows for the crate (grep = 0) across 10 reqs / ~80 criteria, despite the crate's tests. Third orphan infra crate in a row with total TCR absence (idle PA-TCR-024, large-file PA-TCR-025, this). | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-DEP-004 | DEAD/OVER-DECLARED DEPENDENCIES | file-operations (`ff-file-ops`) | Cargo.toml declares `ff-undo-redo` but the code uses it 0 times (Transaction 0, no non-doc `ff_undo_redo::`) -- file-ops reads `document.is_dirty()` and takes `is_dirty: bool` params; Revert is a gated reload, not an undo-stack op. Also `ff-logging` declared, 0 uses (dead). Two dead deps. Same over-declaration family as ff-clipboard PA-DEP-003 + the dead-ff-logging cluster. | Audit + prune: confirm ff-undo-redo is unused and remove; resolve the dead ff-logging dep (or start using it -- see PA-LOG-040). Low-risk Cargo cleanup. | 6 (consolidation) -- LOW |
| PA-STD-055 | REFACTOR (ASCII source violation, runtime strings) | file-operations (`ff-file-ops`) | 91 non-ASCII bytes (11 non-comment): em-dashes in error.rs (9) + save.rs (2) runtime strings. Rest doc-comment dashes. | Replace with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-040 | LOGGING-GAP (dead dep + missing persistence logging, MEDIUM) | file-operations (`ff-file-ops`) | `ff-logging` declared but 0 uses (DEAD). Persistence is a HIGH-VALUE, data-safety-relevant logging site: Save / atomic rename-on-write / backup creation / read-only detection / Revert are operations where a silent failure or partial write is dangerous and hard to diagnose. Rated MEDIUM (above the usual LOW) -- strong CR-NR-058 candidate (alongside external-mod PA-LOG-039). | Resolve dead dep + add dev-logging on save / atomic-rename / backup / read-only / revert outcomes under the `dev-logging` gate. | task-revision (MEDIUM; dev-logging owner/Req-13 aware) |
| PA-TCR-027 | TCR-GAP (coverage enumeration) | file-operations (`ff-file-ops`) | 2 TCR rows for 10 reqs / ~90 criteria; in-file tests exist. Thin (better than the four orphans' zero, but under-enumerated). | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-SPLIT-013 | SPLIT CANDIDATE | jes-emulator (`ff-jes`) | 18 reqs / 657 req lines with TWO clean responsibilities: JES job engine (Reqs 1-15: submission, queue/scheduling, initiator pool, monitoring, SYSOUT, retention, hold/release, catalog, APIs, provider, async) vs SDSF panel framework (Reqs 16-18: panel core/extended, P2 overtype/help/log+system/browse+print/SET). The file layout already matches the seam (10 sdsf_* files vs the engine files). TWO files over the 400 non-test cap (ffjcl.rs 469, sdsf_system_panels.rs 409) + five straining (model 390, queue 390, sdsf_commands 369, sdsf_filter 375, sdsf_help 358). | Split into `ff-jes` (engine, Reqs 1-15) + `ff-jes-sdsf` (SDSF framework, Reqs 16-18), SDSF depending on the engine's Job/JobProvider API. Resolves both cap violations. REFACTOR (no behaviour change). | 6 (consolidation) -- MEDIUM |
| PA-CONFLICT-015 | CONSISTENCY CONFLICT (raw-fs / VFS-bypass -- FIRST found) | jes-emulator (`ff-jes`) / ff-vfs | The JES job queue persists its JSON job DB with RAW std::fs, bypassing ff-vfs: queue.rs:49 `std::fs::read_to_string(db_path)`, queue.rs:328 `std::fs::create_dir_all(parent)`, queue.rs:334 `std::fs::write(path, data)`. The crate has NO ff-vfs dependency. This is the FIRST raw-fs VIOLATION found in the analysis, in direct contrast to the EXEMPLARY VFS discipline of external-mod (PA-CONFLICT-014 record) + file-ops (W4.15), both 0 real fs per FFW-ARCH-001. The rest of the project routes I/O through the VFS abstraction; the JES queue store bypasses it (breaks connector abstraction, in-memory-VFS testing, path abstraction). | Route the queue-persistence store through ff-vfs (add the dep; replace read_to_string / create_dir_all / write with VFS equivalents). Watch subsequent Wave-5 emulators/tools for the same bypass. Code. | later (owner-gated; MEDIUM) |
| PA-LOG-041 | LOGGING-GAP (job engine logs nothing, MEDIUM) | jes-emulator (`ff-jes`) | `ff-logging` ABSENT (not a dead dep) and 0 log calls. A JES/job-execution engine is a high-value trace site: job submit / queue enqueue / scheduler dispatch / initiator assignment / job complete-fail-cancel / hold-release / retention purge -- exactly the lifecycle transitions an operator needs, and async concurrency (Req 15) makes silent failures hard to diagnose. Rated MEDIUM -- real CR-NR-058 gap. | Add ff-logging + dev-logging on job-lifecycle transitions + scheduler/initiator decisions + queue-persistence outcomes under the `dev-logging` gate. | task-revision (MEDIUM; dev-logging owner/Req-13 aware) |
| PA-STD-056 | REFACTOR (ASCII source violation, comments only) | jes-emulator (`ff-jes`) | 36 non-ASCII bytes, ALL in comments (0 non-comment). Lower priority than runtime-string violations. | Sweep comment em-dashes/arrows for consistency. REFACTOR, no gate. | 6 (consolidation) -- LOW |
| PA-STD-057 | REFACTOR (SEVERE 400-cap violation) | idcams-emulator (`ff-idcams`) | The WORST file-size violation found in the analysis: `parser/mod.rs` = 1372 non-test lines (41 functions) -- a `mod.rs` that should be re-exports only carries the entire parse logic. Plus `executor/handlers.rs` 833, `services.rs` 765, `parser/ast.rs` 538, `pretty_printer/mod.rs` 366. FIVE files over the 400 cap. | Split parser/mod.rs by parse concern (per-command / statement-group modules; move logic into named submodules, mod.rs -> re-exports); split handlers.rs (per-command handler modules), services.rs (Allocator/Catalog/Vsam into separate files), ast.rs. Comparable to the ff-desktop shell split (PA-W3.6). REFACTOR, no behaviour change. | 6 (consolidation) -- HIGH (severe cap) |
| PA-CONFLICT-016 | CONSISTENCY CONFLICT (orphan + missing dual integration) | idcams-emulator (`ff-idcams`) / command-framework / jes-emulator | `ff-idcams` is a complete engine (execute_idcams, CommandExecutor, IdcamsParser, IdcamsServices, pretty printer) referenced by NO crate (0 `ff_idcams` refs outside; 0 in ff-command/ff-plugin/ff-desktop; 0 in ff-jes) and with NO FileForgePlugin impl. Yet the spec explicitly DESIGNS two integration paths (Req 20): (20.1) register with the FileForgeWorkbench command framework, and (20.4) be invoked via JCL `EXEC PGM=IDCAMS` as a JES BATCH JOB STEP (design shows `JCL EXEC PGM=IDCAMS`). NEITHER is wired -- notably ff-jes has 0 idcams refs, so the natural JES-runs-IDCAMS coupling is entirely absent. Sharper than the Wave-4 orphans: the spec NAMES the exact integration points that were never built. | Owner-gated: (a) register ff-idcams with the command framework (Req 20.1); (b) wire the JES EXEC PGM=IDCAMS batch-step invocation (Req 20.4) so IDCAMS runs inside JES jobs. Pairs with the Wave-4 orphan cluster (PA-W4.1) + JES (W5.1). Code + owner decision. | later (owner-gated; MEDIUM-HIGH) |
| PA-LOG-042 | LOGGING-GAP (orchestrator logs nothing, MEDIUM) | idcams-emulator (`ff-idcams`) | `ff-logging` ABSENT, 0 log calls. An IDCAMS orchestrator with atomic-execution guarantees (Req 22), LASTCC/MAXCC return-code registers (Req 15), and modal IF/THEN/ELSE (Req 16) is a meaningful trace site: command parse, per-command execute, condition-code transitions, atomic rollback, modal branch decisions. Zero logging. Pairs with JES PA-LOG-041 (both emulators log nothing). | Add ff-logging + dev-logging on command-execute / condition-code transitions / atomic-rollback / modal branches under the `dev-logging` gate (when wired, PA-CONFLICT-016). | task-revision (MEDIUM; dev-logging owner/Req-13 aware) |
| PA-TCR-028 | TCR-GAP (thin -- widest by spec size) | idcams-emulator (`ff-idcams`) | 1 TCR row for 26 reqs / ~200 criteria -- the widest coverage gap relative to spec size seen (contrast JES's exemplary 74). In-file tests exist (245 tasks). | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-CONFLICT-017 | CONSISTENCY CONFLICT (JCL handling fragmented; empty stub never owned it) | jcl-resolver (stub) / dataset-allocator (`ff-dsalloc`) / jes-emulator (`ff-jes` ffjcl.rs) | `jcl-resolver` sub-project is an empty stub (.gitkeep only, no crate). Docs correctly flag it a placeholder, BUT JCL resolution ALREADY EXISTS, spread across two crates: `ff-dsalloc` (a full 4-stage JCL symbolic resolver -- JCL text -> Symbol table -> Catalog -> Lint rules; symbolic-parameter scopes; JCL003/JCL012 lint codes; register_resolve_command; [jcl] config table) and `ff-jes/ffjcl.rs` (the FFJCL job-definition AST parser). The empty jcl-resolver, which by its name would own/unify JCL resolution, was never populated -- so JCL handling is fragmented with no single owner. | Owner-gated: (a) keep JCL resolution distributed (ff-dsalloc symbolic resolver + ff-jes FFJCL parser serve different layers) + DELETE the misleading empty jcl-resolver stub + correct the docs [LIGHTER]; OR (b) populate jcl-resolver as the unifying JCL sub-project and migrate both implementations. Owner decision + docs. | 6 (consolidation) -- LOW-MEDIUM |
| PA-TRACK-005 | DOCS-ACCURACY (stub scope misrecorded) | jcl-resolver (stub) / readiness-summary.md | `readiness-summary.md` claims (twice) the jcl-resolver requirements were "incorporated within / folded into FFW-JES Requirement 11". But FFW-JES (ff-jes) Req 11 is "Dataset Catalog Integration", NOT JCL resolution. The real JCL work is complete but lives in ff-dsalloc (symbolic resolver) + ff-jes/ffjcl.rs (FFJCL parser), not FFW-JES Req 11. Misleading provenance note. | Correct readiness-summary.md to point the jcl-resolver scope note at the real locations (ff-dsalloc symbolic resolver + ff-jes/ffjcl.rs FFJCL parser). Docs only. | 6 (consolidation) |
| PA-INCOMPLETE-014 | INCOMPLETE (FALSE-POSITIVE-COMPLETE -- largest in analysis) | database-tool (`ff-database-tool`) | Spec is a full-featured Database IDE (17 reqs, 664 lines: plugin lifecycle, driver registry, connection mgmt + POOLING, SQL editor w/ dialect highlighting + autocomplete, query execution + param binding, sortable/filterable RESULT GRID w/ cell editing, lazy-loading SCHEMA BROWSER, DATA TRANSFER wizards, zoomable ER DIAGRAM, ADMINISTRATION dashboards, async I/O, multi-DB, command/VFS/layout integration). Implementation is a FOUNDATION SKELETON: 6 files, ~716 non-test lines TOTAL, modules only for driver/connection/sql TYPE MODELS + error types; Cargo deps are ONLY thiserror + serde (NO sqlx/rusqlite for drivers/queries, NO tokio for async Req 13, NO egui for the 7 panels, NO ff-vfs Req 16, NO ff-command Req 15, NO ff-layout Req 17). tasks.md frames the built work as "Wave A (Foundation): crate scaffold, error types, driver abstraction". YET all 157 sub-tasks are [x] and the unit is presented complete. ~14 of 17 requirements unimplemented while tracking says done. | Re-open the database-tool tasks: keep the foundation subset (driver/connection/sql type models + error) as done; RE-OPEN the panel / pooling / async / ER / transfer / admin / multi-DB / integration tasks as `[ ]`; correct the "complete" framing. Then an owner-prioritised implementation effort. Tracking integrity + honest re-scope. | later (owner-gated; HIGH -- tracking integrity) |
| PA-STD-058 | REFACTOR (ASCII source violation, doc-comment mojibake) | database-tool (`ff-database-tool`) | lib.rs header doc-comment has mojibake em-dashes (UTF-8 corruption) in several bullet lines (e.g. "ff-database-tool <bad> Integrated Database IDE Plugin"; "Connection Management <bad> Create..."). | Replace with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-043 | LOGGING-GAP (deferred to implementation, MEDIUM-when-built) | database-tool (`ff-database-tool`) | ff-logging ABSENT, 0 log calls. A DB IDE is a high-value logging site (connection open/close/pool, query execute + timing, transaction, errors) but the implementation is skeleton-stage (PA-INCOMPLETE-014) so logging is premature now. | Add ff-logging + dev-logging on connection/pool/query lifecycle when the IDE is actually implemented. Deferred; not a dead-dep issue now. | task-revision (deferred; dev-logging owner/Req-13 aware) |
| PA-TCR-029 | TCR-GAP (deferred to implementation) | database-tool (`ff-database-tool`) | 0 TCR rows for 17 reqs. But with ~14 reqs unbuilt (PA-INCOMPLETE-014), full TCR is premature; add rows for the foundation types (driver/connection/sql) now, defer the rest to the implementation. | Add TCR rows for the built foundation; enumerate the rest as the IDE is implemented. No code. | 6 (consolidation) |
| PA-SPLIT-014 / PA-STD-059 | REFACTOR (400-cap violation, single-file plugins) | compiler-toolchain-integration (`ff-gcc-toolchain`, `ff-rust-toolchain`) | Each plugin crate is a single oversized lib.rs: ff-gcc-toolchain/lib.rs = 421 non-test lines, ff-rust-toolchain/lib.rs = 462 non-test lines. Each does detection + installation + build-invocation + diagnostic-parsing in one file. (ff-toolchain-api is 1 file, under cap -- fine.) | Split each plugin lib.rs by concern (detection.rs / install.rs / build.rs / diagnostics.rs; lib.rs = plugin impl + re-exports). REFACTOR, no behaviour change. | 6 (consolidation) -- MEDIUM |
| PA-STD-060 | REFACTOR (ASCII source violation, comments + mojibake box-drawing) | compiler-toolchain-integration (all 3 crates) | Non-ASCII in comments: box-drawing separators in .rs files (NOT allowed in Rust source per documentation.md -- box-drawing is .md-only), plus corrupted-encoding artifacts and em-dashes (ff-toolchain-api 22 bytes, ff-gcc-toolchain 35, ff-rust-toolchain 32). gcc line 589 is an em-dash in a test comment. | Replace box-drawing separators with the ASCII `// === ... ===` form; replace em-dashes with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-044 | LOGGING-GAP (detect/install/build, MEDIUM) | compiler-toolchain-integration (all 3 crates) | ff-logging ABSENT, 0 log calls. A toolchain detect/install/build system is a meaningful trace site: detection probes (which found what, versions), INSTALL steps (winget/apt/brew invocation + outcomes -- fail in many env-specific ways), build invocation (command line, exit code), diagnostic-parse results. Zero logging. | Add ff-logging + dev-logging on detection / install steps / build invocation / diagnostic parsing under the `dev-logging` gate. Install-step logging especially valuable (env-specific failures). | task-revision (MEDIUM; dev-logging owner/Req-13 aware) |
| PA-INCOMPLETE-015 | INCOMPLETE (one honestly-tracked open task) | batch-execution (ff-desktop `batch/cli.rs`) | Task 10.2 "Add `--batch-log <file>` support: redirect ff-logging output to <file>" (Validates Req 10.2) is the sole open `[ ]` task (38/39 done). State verified: the `--batch-log <file>` FLAG is parsed + stored (cli.rs:43 help, cli.rs:119-121 arg handling, test cli.rs:303) but the parsed path is NOT yet wired to redirect the ff-logging sink to that file. This is an HONEST `[ ]` (tracking correctly shows the gap -- contrast the database-tool false-positive-complete PA-INCOMPLETE-014). | Finish task 10.2: connect `--batch-log <file>` to an ff-logging file sink so headless runs write a structured log file. Small, well-scoped; ties CR-NR-058 (logging-subsystem file sink). Code + test. Then all 39 tasks close. | later (LOW-MEDIUM; small, scoped) |
| PA-STD-061 | REFACTOR (ASCII source violation, runtime display strings) | asa-report-preview (`ff-asa`) | 8 non-comment non-ASCII bytes: MULTIPLICATION SIGNS (`x` written as the Unicode multiply sign) and em-dashes in RUNTIME DISPLAY strings -- error.rs:11 (navigate message), printer.rs profile descriptions ("132 columns <mult> 60 lines"), dimensions_annotation (`{}<mult>{}`). These are display text, NOT legitimate control characters. | Replace the multiplication sign with ASCII `x` and em-dashes with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-DOC-007 | DOC (crate-name drift) | asa-report-preview (`ff-asa`) | Spec calls it `ff-asa-report-preview`; the crate dir is `ff-asa`. Same crate-name-drift pattern as ff-hex/ff-select/ff-tabmask/ff-forge/ff-keys/ff-completion/ff-wrap/ff-seqnum/ff-dscatalog/ff-external-mod. | Reconcile spec text to `ff-asa` (or rename). Add to the naming-reconciliation set. Docs only. | 6 (consolidation) |
| PA-TCR-030 | TCR-GAP (coverage enumeration) | asa-report-preview (`ff-asa`) | 1 TCR row for 12 reqs / ~90 criteria; 160 tasks + in-file tests exist. Thin. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-LOG-045 | LOGGING-GAP (optional, LOW) | asa-report-preview (`ff-asa`) | ff-logging ABSENT, 0 log calls. ASA detect/parse/render is a low-stakes, deterministic pure-transform pipeline (text -> preview model; no I/O, no concurrency, no external process); errors surface via AsaError. Zero-log largely defensible. | Optional dev-logging on ASA auto-detect decisions + export outcomes under the `dev-logging` gate. Low priority. | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-STD-062 | REFACTOR (400-cap violation) | lua-macro-engine (`ff-lua`) | Four files at/over the 400 non-test cap: engine.rs = 506 (the runtime core), tso_builtins.rs = 431, ispf.rs = 405 (over); execio.rs = 382 (near). | Split engine.rs by concern (runtime init / execution + security gate / API registration / state); split tso_builtins + ispf by command group. REFACTOR, no behaviour change. | 6 (consolidation) -- MEDIUM |
| PA-INCOMPLETE-016 | INCOMPLETE (two honestly-tracked open groups) | lua-macro-engine (`ff-lua`) | 220/227 tasks done; 7 open in two coherent groups: (1) Task 25 "Macro Library panel (Phase CR)" -- the Req 12 Macro Library MANAGEMENT UI panel is unbuilt (engine + management logic exist; the panel does not). (2) Tasks 26.1-26.5 "Macro and FFCMD command sequences" -- route each macro/FFCMD line through the SHARED command-semantics chain executor + compose fail-stop (command-semantics Req 11.4), tests, TCR for Req 5.8-5.11. Group 2 is ANOTHER LEG of the CR-NR-057 COMMAND-CHAINING BUNDLE (w/ command-framework Req 10 PA-INCOMPLETE-003, command-semantics Req 11 PA-INCOMPLETE-007, menu-workspace Req 5 PA-INCOMPLETE-008, function-keys Req 17 PA-INCOMPLETE-009, shared split_chain PA-WATCH-017). Honestly tracked `[ ]`. | (a) Build the Macro Library panel (Req 12, task 25). (b) Complete the FFCMD-sequence chaining (tasks 26.x) AS PART OF the CR-NR-057 Phase DH bundle -- reuse the shared chain executor, do NOT build a separate one; no piping (editor.command(str) returns only success). Code + tests. | later (MEDIUM; group 2 -> Phase DH) |
| PA-LOG-046 | LOGGING-GAP (dead dep + security-audit logging, MEDIUM-HIGH) | lua-macro-engine (`ff-lua`) | `ff-logging` declared but 0 uses (DEAD). A security-sensitive scripting engine is a high-value logging site: macro execution start/end + which macro, SECURITY-MODE gate decisions (allowed/prompted/denied -- audit-relevant), error + rollback (Req 6), auto-reload triggers (Req 8), directory-scan results (Req 9). Rated MEDIUM-HIGH (above the usual LOW) because the security-gate decisions are audit-relevant, and shell-command (the sibling code-execution surface) ALREADY logs -- lua-macro should match it. | Resolve the dead dep + add dev-logging on macro-execute / security-gate decisions / error-rollback / auto-reload / scan under the `dev-logging` gate. Strong CR-NR-058 candidate. | task-revision (MEDIUM-HIGH; dev-logging owner/Req-13 aware) |
| PA-DOC-008 | DOC (crate-name drift) | lua-macro-engine (`ff-lua`) | Spec calls it `ff-macro`; the crate dir is `ff-lua`. Same crate-name-drift pattern as the others. | Reconcile spec text to `ff-lua` (or rename). Add to the naming-reconciliation set. Docs only. | 6 (consolidation) |
| PA-WATCH-026 | WATCH (raw-fs macro-dir scanning) | lua-macro-engine (`ff-lua`) | engine.rs:163 reads a macro script (`std::fs::read_to_string`) and scanner.rs:94 scans the macro directory (`std::fs::read_dir`) with raw std::fs (other ~14 scanner fs hits are `#[cfg(test)]` fixtures). Macro scripts live in a macro DIRECTORY (user files), not the workbench VFS -- so raw fs is arguably acceptable (like toolchain locating OS compilers, W5.5), LOWER-stakes than the JES job-DB raw-fs (PA-CONFLICT-015). | Decide whether macro-dir scanning should be VFS-mediated for consistency, or is legitimately outside the VFS (likely the latter). Watch, not a hard violation. | analysis (LOW; likely resolve as acceptable) |
| PA-CONFLICT-018 | CONSISTENCY CONFLICT (FIFTH orphan; foundation crate its stated consumer ignores + duplicates inline) | language-service (`ff-language-service`) / syntax-highlighting (`ff-syntax-highlighting`) | The spec's premise is that ff-language-service is "the foundational layer the syntax-highlighting engine CONSUMES to tokenize and colour source code". But ff-language-service is referenced by NO crate (workspace Cargo.toml mentions = 1, its own; 0 `ff_language_service` refs elsewhere -- ORPHAN), and ff-syntax-highlighting does NOT depend on it (0) -- instead reimplementing its OWN language detection/definition INLINE (5 refs to detect/LanguageDef/by_extension/content-based in its src). The shell has 0 refs too. So the stated consumer duplicates the exact capability. FIFTH orphan crate (w/ ff-file-tree, ff-idle-processing, ff-large-file-performance, ff-external-mod). Sharpest case (spec names the consumer) and the SECOND foundation crate syntax-highlighting ignores -- it also reimplements idle-styling inline while ff-idle-processing sits orphaned (PA-CONFLICT-012). | Owner-gated: rewire ff-syntax-highlighting onto ff-language-service (consume LanguageDefinition + detection + lexer-state API; delete the inline detection) -- PREFERRED (designed architecture); OR delete/absorb the orphan + reconcile the spec. Standardise the language-TOML load path (ff-config/VFS) when wired. One coherent effort with PA-CONFLICT-012 (same consumer). Code + owner decision. | later (owner-gated; MEDIUM-HIGH) |
| PA-STD-063 | REFACTOR (400-cap violation) | language-service (`ff-language-service`) | definition.rs = 464 non-test lines (over the 400 cap); query.rs = 378 (near). | Split definition.rs by concern (definition model / TOML load / keyword+comment+string sections / embedded-language handling). REFACTOR, no behaviour change. | 6 (consolidation) -- MEDIUM |
| PA-LOG-047 | LOGGING-GAP (dead dep + detection logging, LOW-MEDIUM) | language-service (`ff-language-service`) | `ff-logging` declared but 0 uses (DEAD). Language detection is a modest logging site: which definition loaded from where, detection outcome (extension vs content-based), TOML parse failures (a bad language TOML should log, not silently skip). Most valuable once wired (PA-CONFLICT-018). | Resolve the dead dep + add dev-logging on definition load / detection outcome / TOML parse errors under the `dev-logging` gate. | task-revision (LOW-MEDIUM; dev-logging owner/Req-13 aware) |
| PA-TCR-031 | TCR-GAP (TOTAL absence) | language-service (`ff-language-service`) | 0 TCR rows for 10 reqs, despite the crate's tests. Consistent with the orphan crates' total-absence pattern (idle PA-TCR-024, large-file PA-TCR-025, external-mod PA-TCR-026). | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-STD-064 | REFACTOR (ASCII source violation, runtime string) | language-service (`ff-language-service`) | 1 non-comment non-ASCII: error.rs:44 (likely em-dash in an error string). | Replace with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-CONFLICT-019 | CONSISTENCY CONFLICT (orphaned framework + ASA/hex duplication + PREVIEW double-owner) | custom-file-viewers (`ff-viewers`) / asa-report-preview (`ff-asa`) / hex-display (`ff-hex`) | Three overlapping conflicts. (1) ORPHAN framework (SIXTH orphan): ff-viewers referenced by NO crate (0 external ff_viewers/FileViewer/ViewerRegistry refs) while the point solution ff-asa IS wired (173 refs). (2) ASA + hex viewer DUPLICATION: ff-viewers ships its OWN asa_report.rs / hex.rs / csv_table.rs / image.rs built-in viewers (keys "asa-report","hex") -- separate implementations from ff-asa (wired ASA preview) + ff-hex (hex-display W4.6); ff-viewers has NO dep on ff-asa/ff-hex, so TWO ASA rendering paths + TWO hex paths exist. (3) PREVIEW command DOUBLE-OWNER: ff-viewers defines PREVIEW (Req 3, 105 refs + command.rs/PreviewCommandAction) AND ff-asa defines PREVIEW (Req 3, W5.7). | Owner-gated: decide the viewer ARCHITECTURE -- (a) adopt ff-viewers as THE framework and make its ASA/hex viewers WRAP ff-asa + ff-hex (framework owns registry + PREVIEW + trait; point crates render), wire it into the shell, retire ff-asa's separate PREVIEW ownership [PREFERRED -- extensible design]; or (b) keep the standalone point crates (ff-asa wired) + delete/absorb the orphaned framework + reconcile the duplicate PREVIEW. Result: ONE PREVIEW owner, ONE ASA path, ONE hex path. Pairs with the orphan cluster (PA-W4.1) + ff-asa PREVIEW (W5.7). Code + owner decision. | later (owner-gated; MEDIUM-HIGH) |
| PA-STD-065 | REFACTOR (ASCII source violation, runtime strings) | custom-file-viewers (`ff-viewers`) | 20 non-comment non-ASCII: em-dashes (U+2014) in runtime display/config-error strings -- command.rs viewer-list formatting ("{} -- {}"), config.rs invalid-value messages ("... is not a table -- using ..."). | Replace with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-048 | LOGGING-GAP (dead dep + selection logging, LOW) | custom-file-viewers (`ff-viewers`) | `ff-logging` declared but 0 uses (DEAD). Viewer registry + selection + refresh is a modest logging site (which viewer selected for a file, registration conflicts, refresh triggers). Most valuable once wired (PA-CONFLICT-019). | Resolve the dead dep + add dev-logging on viewer selection / registration / refresh under the `dev-logging` gate. | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-TCR-032 | TCR-GAP (coverage enumeration) | custom-file-viewers (`ff-viewers`) | 1 TCR row for 10 reqs; 79 tasks + in-file tests exist. Thin. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-STD-066 | REFACTOR (400-cap violation + misleading filename) | compare-and-merge (`ff-compare-merge`) | `session.rs` = 557 non-test lines AND its module doc says "Diff engine: Myers and Patience algorithms operating on line slices" (`pub struct DiffEngine`) -- so the file named session.rs actually IS the DIFF ENGINE (Req 2), a cap violation plus a misleading filename. `merge.rs` = 392 (near cap). | Extract the diff engine out of session.rs into `diff_engine.rs` (Myers) + `patience.rs` (or a `diff/` submodule); leave genuine session/navigation state in a correctly-named file; split merge.rs if it grows. REFACTOR, no behaviour change. The name mismatch is worth fixing regardless of size. | 6 (consolidation) -- MEDIUM |
| PA-DOC-009 | DOC (crate-name drift) | compare-and-merge (`ff-compare-merge`) | Spec calls it `ff-compare`; the crate dir is `ff-compare-merge`. Same crate-name-drift pattern as the others. | Reconcile spec text to `ff-compare-merge` (or rename). Add to the naming-reconciliation set. Docs only. | 6 (consolidation) |
| PA-TCR-033 | TCR-GAP (coverage enumeration) | compare-and-merge (`ff-compare-merge`) | 1 TCR row for 17 reqs; 183 tasks + in-file tests exist. Thin. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-STD-067 | REFACTOR (ASCII source violation, runtime strings) | compare-and-merge (`ff-compare-merge`) | 3 non-comment non-ASCII: em-dashes (U+2014) in error.rs error strings, and a MINUS SIGN (U+2212) in result.rs:136 diff-stats format string ("+{} <U+2212>{} ~{}"). | Replace em-dashes with `--` and the minus sign with ASCII `-`. REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-049 | LOGGING-GAP (optional, LOW) | compare-and-merge (`ff-compare-merge`) | ff-logging ABSENT, 0 log calls. Diff/merge is a deterministic pure-transform engine (inputs -> diff/merge result); errors surface via CompareError. Modest value: compare invoked on what, three-way merge conflict counts, binary-vs-text decision. | Optional dev-logging on compare invocation + merge-conflict outcomes under the `dev-logging` gate. Low priority (pure transform, like ff-asa PA-LOG-045). | task-revision (LOW; dev-logging owner/Req-13 aware) |
| PA-CONFLICT-020 | CONSISTENCY CONFLICT (safe-write bypass on bulk replace, data-safety) | global-search (`ff-global-search`) / file-operations (`ff-file-ops`) | Req 5 (cross-file replace) writes with RAW std::fs, bypassing ff-file-ops: replace.rs:60 `std::fs::read_to_string`, replace.rs:91 `std::fs::write` (search.rs:163 reads for matching; lines 119/154/177 are test fixtures). The doc comment (replace.rs:39) claims "writes back via ..." but the code uses raw std::fs::write. This bypasses ff-file-ops' safe-write guarantees (atomic rename-on-write, backup copies, read-only detection). A BULK cross-file replace writing raw (no atomic rename, no backup) is riskier than a single-file edit -- a crash mid-replace can leave files partially written with no backup. Cousin of JES raw-fs (PA-CONFLICT-015) but higher stakes (bulk mutation of user files). | Route the cross-file replace read+write through ff-file-ops' atomic-rename + backup path (the dep is already declared). Resolves PA-DEP-005 too. Code. | later (owner-gated; MEDIUM-HIGH -- data-safety) |
| PA-DEP-005 | DEAD/OVER-DECLARED DEPENDENCY (+ corrects W4.15) | global-search (`ff-global-search`) / file-operations (`ff-file-ops`) | `ff-file-ops` is DECLARED in ff-global-search's Cargo.toml but used 0 times in code (0 `ff_file_ops` refs -- the replace path uses raw std::fs instead). Dead dep. IMPORTANT CORRECTION to W4.15: that record concluded ff-file-ops "is NOT an orphan (consumed by ff-global-search)" based on the Cargo.toml DECLARATION -- but the code does not use it. So ff-file-ops' only declared consumer is a dead-dep; the "consumed" claim was declaration-only. | Either USE ff-file-ops for the replace writes (PREFERRED -- resolves PA-CONFLICT-020 and makes the W4.15 claim true) or drop the dep. Update the W4.15 record's "consumed" note. Cargo cleanup + docs. | 6 (consolidation) -- LOW (ties PA-CONFLICT-020) |
| PA-LOG-050 | LOGGING-GAP (bulk mutation should log, MEDIUM) | global-search (`ff-global-search`) | `ff-logging` ABSENT, 0 log calls. Cross-file search + BULK REPLACE is a meaningful logging site: search scope + match count, and especially the REPLACE (how many files modified, any write failures, read-only skips). A bulk mutation should log what it changed. | Add ff-logging + dev-logging on search execution (scope, match count) + replace (files modified, failures) under the `dev-logging` gate. If replace routes through ff-file-ops (PA-CONFLICT-020), that logging lands there (PA-LOG-040). | task-revision (MEDIUM; dev-logging owner/Req-13 aware) |
| PA-CONFLICT-021 | CONSISTENCY (framework built ahead of its DEFERRED implementers) -- REFINED/DOWNGRADED by W5.15 | connector-extensibility (`ff-connector-extensibility`) / connector-network-fs + other deferred remote connectors / connector-local-fs / ff-vfs | ORIGINAL W5.13 reading: the framework's purpose is "the trait future VFS connectors MUST implement," yet the one existing connector (ff-connector-local-fs) has 0 dep on it (implements ff-vfs directly) and 0 external refs -- looked like a SEVENTH orphan. W5.15 REFINEMENT: the connector-network-fs spec states the future connector implements the connector-extensibility trait (VfsProvider + lifecycle + auth + capability). So the architecture is INTENTIONAL: local (initial release) = plain ff-vfs VfsProvider; DEFERRED remote connectors (network-fs/ftp-sftp/cloud/mainframe) = connector-extensibility. The framework is a base built AHEAD of its deferred implementers, NOT an abandoned orphan. Its "0 implementers" is because the remote connectors are deferred/unbuilt, by plan. REMOVED from the whole-project orphan cluster. | DOWNGRADE to LOW-MEDIUM / WATCH: KEEP the framework (do NOT delete). (a) Add an implementers-are-deferred note to its spec so future readers do not mistake it for an orphan; (b) re-confirm the trait API against real FTP/SFTP semantics when the first remote connector is actually built. Code/docs, low priority. | later (owner-gated; LOW-MEDIUM -- downgraded from MED-HIGH) |
| PA-STD-068 | REFACTOR (400-cap violation) | connector-extensibility (`ff-connector-extensibility`) | registry.rs = 460 non-test lines (over the 400 cap). | Split registry.rs by concern (registration protocol / capability advertisement / lifecycle / lookup). REFACTOR, no behaviour change. | 6 (consolidation) -- MEDIUM |
| PA-TCR-034 | TCR-GAP (TOTAL absence) | connector-extensibility (`ff-connector-extensibility`) | 0 TCR rows for 7 reqs, despite the crate's tests (84 tasks). Consistent with the orphan crates' total-absence pattern. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-STD-069 | REFACTOR (400-cap violation) | connector-local-fs (`ff-connector-local-fs`) | provider.rs = 542 non-test lines (well over the 400 cap) -- the LocalFsProvider VfsProvider impl (read/write/list/metadata/stream/etc.) in one file. | Split provider.rs by operation group (read/write / directory-list / metadata / streaming) into submodules; leave the LocalFsProvider impl as a thin coordinator. REFACTOR, no behaviour change. | 6 (consolidation) -- MEDIUM |
| PA-STD-070 | REFACTOR (ASCII source violation, comment mojibake) | connector-local-fs (`ff-connector-local-fs`) | provider.rs doc-comment header has a mojibake em-dash (corrupted encoding: "LocalFsProvider <bad> implements VfsProvider ..."). Same recurring Wave-5 comment-mojibake pattern as ff-jes/ffjcl, database-tool, ff-toolchain-api. | Replace with `--`. REFACTOR, no gate. | 6 (consolidation) |
| PA-TCR-035 | TCR-GAP (coverage enumeration) | connector-local-fs (`ff-connector-local-fs`) | 1 TCR row for 7 reqs; 89 tasks + in-file tests exist. Thin. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-TRACK-006 | DOCS (deferred-spec inventory hygiene) | connector-network-fs (+ other deferred connector specs) | The deferred connector specs (network-fs, and likely ftp-sftp/cloud/mainframe per W5.16-18) have requirements.md + design.md but NO tasks.md and NO crate -- honest deferral (design.md marked "DEFERRED -- not in initial release"). Fine for deferred work, BUT the project-master should clearly list them as DEFERRED/future so they are counted as neither complete nor incomplete (avoid miscounting). | Mark the deferred connector specs clearly as DEFERRED/future in project-master (a status column or explicit note). Docs / inventory hygiene only. | 6 (consolidation) -- LOW |
| PA-STD-071 | REFACTOR (docs ASCII violation, design.md mojibake) | connector-network-fs (docs) | design.md STATUS line has mojibake: a corrupted warning-emoji byte sequence + an em-dash ("STATUS: DEFERRED <bad em-dash> Not in initial release"). | Replace the emoji with an ASCII marker (e.g. "STATUS: DEFERRED") and the em-dash with `--`. Docs; no gate. | 6 (consolidation) |
| PA-CONFLICT-022 | CONSISTENCY CONFLICT (EBCDIC + ASA duplication -- reimplements peers) | fileforge-integration (`ff-forge`) / encoding-and-characters (`ff-encoding`) / asa-report-preview (`ff-asa`) | ff-forge reimplements TWO capabilities other crates own, with 0 deps on either: (1) EBCDIC -- ff-forge has its own ebcdic.rs codepage tables + conversion (95 EBCDIC refs), no dep on ff-encoding which OWNS EBCDIC (81 refs); (2) ASA carriage control -- ff-forge has its own asa.rs (AsaControl::parse_asa_char, auto-detect, Req 7), no dep on ff-asa (the wired ASA preview crate, W5.7). Combined with ff-viewers/asa_report.rs (W5.10), ASA is now implemented in THREE places (ff-asa / ff-viewers / ff-forge) and EBCDIC in TWO. Same reimplement-instead-of-reuse pattern as ff-viewers (W5.10). Extends the ASA-duplication half of PA-CONFLICT-019 to a three-way finding. | Owner-gated: make ff-forge DEPEND ON + reuse ff-encoding (EBCDIC) and ff-asa (ASA detection), deleting its inline ebcdic.rs codepage tables + asa.rs; OR if ff-forge's flat-file EBCDIC/ASA needs differ materially (e.g. field-level COMP-3-aware conversion), DOCUMENT why the duplication is intentional + share the low-level tables (one EBCDIC table, one ASA parser). Consolidate ASA onto ff-asa across all three crates. Code + owner decision. | later (owner-gated; MEDIUM) |
| PA-DOC-010 | DOC (crate-name drift) | fileforge-integration (`ff-forge`) | Spec calls it `ff-fileforge`; the crate dir is `ff-forge`. Same crate-name-drift pattern as the others. | Reconcile spec text to `ff-forge` (or rename). Add to the naming-reconciliation set. Docs only. | 6 (consolidation) |
| PA-TCR-036 | TCR-GAP (coverage enumeration) | fileforge-integration (`ff-forge`) | 1 TCR row for 16 reqs; 198 tasks + in-file tests exist. Thin. | Add per-requirement TCR rows citing existing tests. No code. | 6 (consolidation) |
| PA-STD-072 | REFACTOR (ASCII source violation, comments + a few runtime) | fileforge-integration (`ff-forge`) | 16 non-comment non-ASCII: mostly EBCDIC codepage COMMENT annotations in ebcdic.rs documenting mappings (e.g. cent/not-sign glyphs -- the CODE correctly uses `\u{...}` escapes), plus an arrow (U+2192, "NEL -> LF") in a comment, a test emoji literal, and em-dashes in error.rs error strings. | Replace comment arrows/em-dashes with ASCII; rewrite EBCDIC codepage comments in ASCII (e.g. "0x4A -> U+00A2 (cent)"); the test emoji literal is borderline (acceptable as test data). REFACTOR, no gate. | 6 (consolidation) |
| PA-LOG-051 | LOGGING-GAP (conversion diagnostics, LOW-MEDIUM) | fileforge-integration (`ff-forge`) | ff-logging ABSENT, 0 log calls. Flat-file parsing/conversion is a meaningful logging site: EBCDIC/COMP-3 conversion failures, VB RDW errors, structure-overlay mismatches, auto-detect decisions (Req 8) -- the "why did my flat file render wrong" diagnostics. | Add ff-logging + dev-logging on conversion failures / RDW errors / overlay mismatches / auto-detect under the `dev-logging` gate. | task-revision (LOW-MEDIUM; dev-logging owner/Req-13 aware) |
| PA-STD-073 | REFACTOR (400-cap violation, low urgency) | automated-dialog-testing (`ff-fftest`) | parser.rs = 406 non-test lines (the FFTest scripting-language parser, Req 3 -- just over the 400 cap). | Split parser.rs (lexer / grammar / AST) if it grows; borderline, low urgency. REFACTOR, no behaviour change. | 6 (consolidation) -- MEDIUM (low urgency) |
| PA-LOG-052 | LOGGING-GAP (optional, LOW) | automated-dialog-testing (`ff-fftest`) | ff-logging ABSENT, 0 log calls. A test framework has its OWN Reporting (Req 7) + bug-report (Req 12) mechanisms -- its "logging" is the structured test report, not ff-logging -- so the absence is largely defensible. | Optional dev-logging on script-execution steps + assertion failures to aid debugging FFTest itself, under the `dev-logging` gate. Low priority (reporting layer covers the main need). | task-revision (LOW / optional; dev-logging owner/Req-13 aware) |
| PA-TRACK-007 | DOCS (master narrative stale vs analysis) | project-master (readiness-summary.md, sub-project-audit.md, api-consistency-report.md, validation-19.*.md) | The master's analysis integration is confined to tasks.md (all 160 PA-W/analysis refs live there -- correct + current, all six PA-W0..W5 phases present + coherent). But the master's TOP-LEVEL NARRATIVE documents have 0 analysis refs: readiness-summary.md, sub-project-audit.md, api-consistency-report.md, validation-19.2/19.3/19.4.md. So the readiness/audit/validation story is STALE -- it presents the pre-analysis "all specified / ready" picture and never mentions the analysis's material findings (SIX orphan crates; database-tool FALSE-POSITIVE-COMPLETE PA-INCOMPLETE-014; the DUPLICATION cluster ASA x3 / EBCDIC x2 / viewers / language-service; the DATA-SAFETY raw-fs write bypasses PA-CONFLICT-015/020; the CR-NR-057 Phase DH bundle). A reader would wrongly conclude the project is complete/ready. | Reconcile the master narrative with the analysis: update readiness-summary.md (+ a note in sub-project-audit.md) to REFERENCE `docs/specs/project-analysis/` (checklist + incomplete-work-register + consistency-matrix) as the current source of truth for outstanding work, and summarise the headline findings + the PA-W0..W5 remediation phases. Docs; Wave-6 finalization deliverable. | 6 (consolidation) -- MEDIUM |
| PA-DOC-011 | DOC (master doc accuracy: stale path + incomplete deferred list) | project-master (readiness-summary.md) | Two concrete inaccuracies: (1) readiness-summary scopes to `.kiro/specs/` but the specs now live under `docs/specs/` (stale path); (2) it states "Deferred (4 sub-projects)" but NAMES only 3 (connector-network-fs, connector-ftp-sftp, connector-mainframe) -- OMITS connector-cloud (W5.17 confirmed the 4th deferred connector). Count 4 is right; enumeration missing cloud. | Fix `.kiro/specs/` -> `docs/specs/`; add connector-cloud to the named Deferred list; fold in PA-TRACK-005 (jcl-resolver scope NOT in FFW-JES Req 11, since that correction lives in a master doc). Docs only. | 6 (consolidation) -- LOW |
