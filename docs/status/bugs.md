# FileForge Workbench -- Bug Register

**Format:** One row per bug. Update `Status` in-place -- never delete a row.

## Status Values

| Status | Meaning |
|--------|---------|
| `OPEN` | Confirmed, not yet fixed |
| `IN PROGRESS` | Actively being worked |
| `FIXED` | Code change made -- awaiting verification |
| `VERIFIED` | Fix confirmed by tester |
| `WONT FIX` | Accepted limitation or out of scope |

## Severity Values

| Severity | Meaning |
|----------|---------|
| `Critical` | Data loss or crash |
| `High` | Feature completely broken |
| `Medium` | Feature partially broken or awkward workaround exists |
| `Low` | Cosmetic or minor inconvenience |

---

## Bug Table

| ID   | Status | Severity | Component | Description | Linked Req | Manual Test | Notes |
|------|--------|----------|-----------|-------------|------------|-------------|-------|
| B001 | FIXED | High     | `ff-desktop` | On Opening the Appliction the ffwb.exe "ISPF" Like Disalog is not visible, it opens into a editor. | Req 14.1, 14.1b | 🔲 Manual: verify POM tab appears as first tab in tab bar on launch | Fixed Phase CL: `ensure_pom_tab_present()` added to startup block -- when session restores file tabs but no POM tab, a POM tab is prepended at index 0. 589 tests pass. |
| B002 | FIXED | High     | `ff-desktop` | The "X" on The Editor Window tabs does not close the file.
| B003 | FIXED | High     | `ff-desktop` | There is no "Close" option on file options menu, There is no way to close files.
| B004 | FIXED | High     | `ff-desktop` | The Editor Window does not have a primary command and line command areas. | -- | -- | Fixed Phase U: shell-level `Command ===>` field is the primary command area; gutter prefix area wired in Phase U (21.2, 21.6). Both areas present and functional. Marked FIXED Phase CM review. |
| B005 | FIXED | Medium | `ff-desktop` | Key Assignments dialog stops accepting keystrokes after Tab is pressed a few times -- shell Tab-cycle consumed all Tab events globally, stealing focus from dialog TextEdit widgets. | Req 20 | -- | Fixed Phase AS: added `modal_open` field; Tab-cycle and `command_field_focus_requested` suppressed when any modal is open. |
| B006 | FIXED | Medium | `ff-desktop` | Key Assignments dialog stops accepting keystrokes after Ctrl+C is pressed inside a dialog text field -- shell function key dispatch and Ctrl+S handler ran unconditionally every frame, competing with dialog input on Windows clipboard stall. | Req 20 | -- | Fixed Phase AS: fkey dispatch and Ctrl+S guard wrapped in `if self.modal_open` check. |
| B007 | VERIFIED | Medium | `ff-desktop` | Typing `2` in the command field shows "Error in 2: command not yet implemented" -- Phase AS shell routing for option 2 was compiled into the binary but the running executable was stale (not rebuilt after Phase AS). | Req 19.1, 19.4 | -- | Root cause: stale debug binary. Fixed by `cargo build`. No code change required. |
| B008 | FIXED | High | `ff-desktop` | New Catalog dialog text fields cannot be typed into -- clicking a field does not focus it, Tab does nothing. Root cause: `modal_open` flag did not include catalog/dataset dialogs, so the shell Tab-cycle and `command_field_focus_requested` stole focus every frame. | Req 3.1, 3.3 | -- | Fixed: `modal_open` extended to include `FilesDialogState::None` check. |
| B009 | SUPERSEDED | High | `ff-desktop` | Allocated datasets are never displayed in the Files Panel content area. `AllocOutcome::Confirmed` discards the validated `AllocParams`; `ContentAreaState::entries` is never populated from any data source; clicking a catalog in the tree shows "No entries to display." | Req 5.1, 10.1 | -- | Superseded by Phase BU (SQLite catalog integration, CR-CH-006). Dataset store replaced by ff-dscatalog SQLite backend; content area now reads from CatalogRegistry API. No separate fix required. |
| B010 | FIXED | High | `ff-desktop` | Catalogs created via the New Catalog dialog are lost on restart. The `CatalogRegistry` is never saved to disk on exit and never loaded from disk on startup. Task 10.2 and 10.3 in virtual-catalog-manager/tasks.md were marked `[x]` but the wiring code was never written. | Req 2.1, 2.2 | -- | Fixed Phase AU: `SessionManager::save_catalog_registry()` and `load_catalog_registry()` added; wired into `on_exit()` and startup block in `shell.rs`. Catalogs persisted to `catalogs.toml` alongside `session.toml`. 382 tests pass. |
| B011 | FIXED | Medium | `ff-desktop` | Mainframe dataset names entered in the Allocate Dataset dialog are stored as-is (mixed case). Mainframe DSNs must be uppercase. | Req 5.8 | -- | Fixed Phase AW: `validate()` now calls `.to_uppercase()` on the trimmed dataset name before storing in `AllocParams`. 399 tests pass. |
| B012 | FIXED | High | `ff-desktop` | Two datasets with the same name can be allocated within the same catalog. The `add_dataset()` function pushes without checking for duplicates. | Req 5.9 | -- | Fixed Phase AW: `validate_for_catalog()` added; shell confirm handler uses it with existing DSN list; duplicate rejected with inline error. 399 tests pass. |
| B013 | FIXED | High | `ff-desktop` | Application opens without the Home catalog -- File Explorer shows "No catalogs open" message. Root cause: `ffwb.exe` binary (built 2026/08/20 22:14) predates `update.rs` (modified 2026/08/20 22:46) which contains the Phase AX `ensure_default_home_catalog` wiring. Stale binary never wrote `catalogs.toml`. | Req 14.1, 14.2, 14.3 | -- | Fixed by `cargo build`. No code change required. |
| B014 | FIXED | High | `ff-desktop` | Expanding a Native catalog node in the File Explorer shows "(no files)" -- Native catalogs should list actual files from disk but the tree only reads from `files_panel.datasets` (allocated dataset store), never from the filesystem. | Req 19.6 | -- | Fixed: `render_native_children()` added -- reads `std::fs::read_dir` for Native catalogs; directories listed first then files alphabetically; double-click opens files. Mainframe/POSIX catalogs continue to use dataset store via `render_dataset_children()`. 404 tests pass. |

1. Assign the next `B###` ID.
2. Set `Status` to `OPEN`.
3. Fill in `Severity`, `Component` (crate name or `ff-desktop`), and a clear one-line `Description`.
4. Link to the relevant `requirements.md` criterion in `Linked Req`.
5. Reference the manual test plan row in `Manual Test` if applicable.
6. If the bug reveals a **missing or incorrect acceptance criterion**, update `docs/specs/<sub-project>/requirements.md` and add a 🔴 row to `docs/quality/TCR.md` before fixing the code (per the new-requirements gate).

---

## Changelog

| Date | Change |
|------|--------|
| Phase W | Initial register created -- B001–B010 from known gaps documented in `docs/quality/manual-test-plan.md` and `docs/quality/TCR.md` |
| Phase AS | B005 added -- Key Assignments dialog Tab-steal focus bug (FIXED) |
| Phase AS | B006 added -- Key Assignments dialog Ctrl+C keystroke freeze bug (FIXED) |
| Phase AS | B007 added -- option 2 "command not yet implemented" error -- stale binary, VERIFIED fixed by rebuild |
| Phase AS | B008 added -- New Catalog dialog fields not focusable -- modal_open missing catalog dialogs (FIXED) |
| Phase AS | B009 added -- allocated datasets never appear in Files Panel -- design gap, dataset store not implemented |
| Phase AU | B010 added -- catalog registry never persisted; catalogs lost on restart -- task 10.2/10.3 not implemented |
| Phase AU | B010 FIXED -- `save_catalog_registry()` / `load_catalog_registry()` wired into `shell.rs` `on_exit()` and startup; 382 tests pass |
| Phase AW | B011 added -- Mainframe dataset names not uppercased on allocation (FIXED) |
| Phase AW | B012 added -- duplicate dataset names allowed within same catalog (FIXED) |
| Phase AX | B013 added -- Home catalog missing on launch; stale binary predated Phase AX wiring; FIXED by `cargo build` |
| Phase AX | B014 added -- Native catalog expansion showed "(no files)"; fixed by adding `render_native_children()` using `std::fs::read_dir` |
| Phase BB | B019 added -- File Explorer Panel not resizable; fills entire central panel with no splitter |
| Phase BB | B019 FIXED -- `SidePanel::left` with `resizable(true)`, min 120px, max 600px, default 260px |
| Phase BK | B024 added -- Tab from Files Panel command field does not move focus to first catalog in the tree |
| Phase BK | B024 FIXED (final) -- root cause: Tab consumed by `update()` focus-cycle before `render_central_panel` ran; fix intercepts Tab in `update()` when File Explorer active and shell command field has egui focus, sets `explorer_focused` directly instead of advancing `focus_stop` |
| Phase BL | B024 Tab cycle refinement -- Tab past last tree node now exits tree and returns focus to CommandField; cursor highlight added to catalog nodes and file nodes; within-tree Tab advance fixed (was wrapping infinitely, never exiting) |
| Phase BM | B025 added -- Tab order in File Explorer is wrong: `collect_visible_node_paths` groups all catalogs then all files; correct order is interleaved: cat:A → A/file1 → A/file2 → cat:B → B/file1 → ... (FIXED) |
| Phase BN | B026 added -- Tab on a directory node in File Explorer does not expand it or jump to its first child; `collect_visible_node_paths` never recurses into subdirectories and `open_directories` state does not exist (FIXED) |

| B026 | FIXED | Medium | `ff-desktop` | Tab on a directory node in the File Explorer does not expand it or jump to its first child. `collect_visible_node_paths` never recurses into subdirectories; there is no `open_directories` state to track which dirs are expanded. | Req 20.2 file-tree-panel | -- | Fixed: added `open_directories: HashSet<String>` to state; synced from `render_native_children`; `collect_visible_node_paths_with_dirs` recurses into open dirs; `auto_open_node()` helper in `update.rs` handles both `cat:` and directory nodes on Tab. 481 tests pass. |
| B015 | FIXED | High | `ff-desktop` | Tab close button ("×") is missing -- tabs have no visible close button on their headers. | Req 3.8 multi-tab-editor | -- | Fixed: inline `×` button added to each tab header in `render_tab_bar()`; clicking it calls `close_tab(i)`. |
| B016 | VERIFIED | Low | `ff-desktop` | Tab header bracket style is inconsistent -- some tabs render with `[]` delimiters, others have none. No documented rule governs which tabs get brackets. | Req 3.2, 3.3 multi-tab-editor | -- | Rule: system/panel tabs (`[POM]`, `[FILES]`, `[SETTINGS]`, `[EXPLORER]`) use `[]` brackets; file editor tabs show the filename without brackets. This is intentional ISPF-style convention. |
| B017 | FIXED | Medium | `ff-desktop` | Windows junction points (e.g. `C:\Users\<user>\My Documents`) appear as directories in the File Explorer and produce "permission denied" errors when expanded. These are OS-level compatibility junctions that Windows intentionally restricts. | Req 15.1 file-tree-panel | -- | Fixed Phase BB: `collect_native_entries()` calls `metadata().ok()?` -- entries where metadata fails are silently skipped; junction points never appear in the listing. |
| B018 | FIXED | Medium | `ff-desktop` | Locked system files (e.g. `NTUSER.DAT`) produce an I/O error ("process cannot access the file because it is being used by another process") when the user attempts to open them in the editor. | Req 1.8 multi-tab-editor | -- | Fixed Phase BB: `open_file_node()` attempts `File::open()` before handing to editor; OS error 32 stored in `last_error` and shown in status bar; no editor tab opened. |
| B019 | FIXED | Medium | `ff-desktop` | The File Explorer Panel (`=2` / `=FILES`) is not resizable -- it fills the entire central panel as a tab with no splitter or drag handle. The user cannot adjust the width of the file listing area. | Req 1.3 file-tree-panel | -- | Fixed: `render_central_panel` now uses `egui::SidePanel::left("file_explorer_side").resizable(true).min_width(120.0).max_width(600.0)` with a `CentralPanel` placeholder alongside it. Width persisted in `file_explorer_panel_width` field. |
| B020 | FIXED | High | `ff-desktop` | Mainframe catalog not persisting across sessions -- `save_catalog_registry()` is only called in `on_exit()`; if the app is force-closed after creating a Mainframe catalog the registry change is lost. | Req 2.1 virtual-catalog-manager | -- | Fixed Phase BO: `save_catalog_registry()` called immediately after `DialogOutcome::Confirmed` in NewCatalog and DeleteCatalog handlers in `update.rs`. |
| B021 | FIXED | Medium | `ff-desktop` | Default repository path for a new Mainframe catalog is empty -- `NewCatalogForm::with_defaults()` leaves `repository_path` as empty string; the live-update only fires when the user types in the Name field, so the field is blank on dialog open even when `default_mainframe_root` is configured. | Req 12.1 virtual-catalog-manager | -- | Fixed Phase BO: `with_defaults()` now pre-populates `repository_path` with `mainframe_root.clone()` so the field is non-empty on dialog open. |
| Phase BG | B020 added -- Mainframe catalog not persisting on force-close; fixed by calling `save_catalog_registry()` immediately after `Confirmed` in NewCatalog and DeleteCatalog handlers in `update.rs` |
| Phase BG | B021 added -- Default repository path empty on Mainframe catalog dialog open; fixed by pre-populating `repository_path` with `default_mainframe_root` in `NewCatalogForm::with_defaults()` |
| B022 | FIXED | High | `ff-desktop` | Allocated datasets not persisted across sessions -- `files_panel.datasets` (`HashMap<String,Vec<AllocatedDataset>>`) is in-memory only; never serialised to disk. All allocated datasets are lost on restart. | Req 13.1, 13.2 virtual-catalog-manager | -- | Fixed Phase BO: `save_datasets()`/`load_datasets()` added to `SessionManager` writing `datasets.toml`; wired into startup restore, `AllocateDataset` confirmed handler, and `on_exit()`. |
| B023 | FIXED | High | `ff-desktop` | Opening a Mainframe dataset attempts a local filesystem read -- `FilesPanelAction::OpenFile` passes the bare DSN (e.g. `PAYROLL.EMPLOYEE`) to `tabs.open_file()` which resolves it as a CWD-relative path, producing a VFS "resource not found" error. Same bug also triggered from File Explorer Panel double-click via `render_dataset_children()`. | Req 10.3 virtual-catalog-manager | -- | Fixed: (1) `render.rs` intercepts `OpenFile` for Mainframe catalogs and shows informational message. (2) `file_explorer_panel.rs` `render_dataset_children()` double-click sets `state.last_error` instead of `open_path` for Mainframe/POSIX datasets. |
| B024 | FIXED | Medium | `ff-desktop` | Tab from Files Panel command field does not move focus to first catalog in the tree. Three previous fix attempts failed because `selectable_label` cannot hold egui keyboard focus -- `request_focus(resp.id)` is silently ignored. | Req 20.1 file-tree-panel | -- | Final fix: replaced egui-focus approach with state-driven `focused_catalog: Option<String>` field. Tab sets it to the first visible catalog name; rendered with a `rect_stroke` highlight; Up/Down arrows navigate; Enter selects; Tab clears. |
| Phase BH | B022 added -- allocated datasets not persisted; fixed by adding `save_datasets()`/`load_datasets()` to `SessionManager` writing `datasets.toml`; wired into startup restore, `AllocateDataset` confirmed handler, and `on_exit()` |
| Phase BO | B020 FIXED -- `save_catalog_registry()` now called immediately on NewCatalog/DeleteCatalog confirm, not only on `on_exit()` |
| Phase BO | B021 FIXED -- `NewCatalogForm::with_defaults()` pre-populates `repository_path` with configured mainframe root on dialog open |
| Phase BO | B022 FIXED -- `save_datasets()`/`load_datasets()` wired; datasets survive force-close and restart |
| Phase BH | B023 added -- opening Mainframe dataset attempted local VFS read; fixed by intercepting `OpenFile` for Mainframe catalogs in `render.rs` and showing "Mainframe dataset editing is not yet implemented" instead |
| Phase BH (cont.) | B023 also triggered from File Explorer Panel double-click; fixed by setting `state.last_error` in `render_dataset_children()` instead of `open_path` |

| B029 | FIXED | Low | `ff-config` | 3 unit tests in `ff-config` fail on Windows due to hardcoded schema default values: `init_with_no_config_files_succeeds_with_schema_defaults` asserts `"lf"` but gets `"crlf"`; `shutdown_without_watcher_is_safe` asserts `"info"` but gets `"debug"`; `init_loads_project_config_when_project_root_provided` asserts a mismatched value. Tests encode platform-specific defaults that differ on Windows. | Req 1 configuration-system | -- | Fixed: three tests rewritten to build `ConfigHandle` directly from `SchemaRegistry` + `ReloadManager` instead of calling `init()`, which loads the real user config file from disk and overrides schema defaults. Workspace now 432 passed; 0 failed. |
| B030 | FIXED | Medium | `ff-jes` | Two unit test failures: (1) `scroll_up_half` asserts `37` but `50 - 25/2 = 38` by integer arithmetic -- wrong expected value in test. (2) `filter_ge_operator` uses lexicographic string comparison for numeric `PRTY` field -- `"10" >= "5"` is false lexicographically, so priority 10 fails a `>=5` filter. | Req 17.5, 17.12 FFW-JES | -- | Fix (1): correct test expectation to 38. Fix (2): parse numeric fields as integers before comparing with `>=`, `<=`, `>`, `<` operators. |

| B027 | FIXED | High | `ff-desktop` | File Explorer right pane is blank for Native catalogs -- `render_native_dialog` calls `select_file()` which opens `egui-file-dialog` in floating-window mode; `update(ctx)` renders a detached `egui::Window` outside the panel, leaving the right pane empty. | Req 23.4 file-tree-panel | -- | Fixed Phase BR: replaced `render_native_dialog` floating-window call with inline `ScrollArea` + `render_native_children`; right pane now shows file tree for Native catalogs; 497 tests passing. |

| Phase BR | B009 SUPERSEDED -- original design gap resolved by Phase BU SQLite catalog integration; AllocatedDataset HashMap replaced by ff-dscatalog CatalogRegistry API |
| Phase CA.impl | B029 added -- 3 ff-config unit tests fail on Windows due to hardcoded platform-specific schema defaults ("lf" vs "crlf", "info" vs "debug") |
| Phase CA.impl | B030 added -- ff-jes `scroll_up_half` test has wrong expected value (38 not 37); `filter_ge_operator` uses lexicographic string comparison for numeric priority field, fails for multi-digit values |
| Phase BR | B027 FIXED -- replaced `render_native_dialog` floating-window call with inline `ScrollArea` + `render_native_children`; right pane now shows file tree for Native catalogs; 497 tests passing |

| B028 | FIXED | High | `ff-desktop` | Opening a Mainframe dataset fails with "dataset file not found" -- `resolve_dataset_path` joins the catalog's `path` field (which may contain forward slashes) with DSN components using `std::path::Path::join`, producing a mixed-separator path on Windows (e.g. `mainframe/Payroll\PAYROLL\EMPLOYEE`). The resulting path does not exist on disk because dataset allocation is metadata-only and never creates the physical file. | Req 16.1, 16.3, 16.4, 16.5 virtual-catalog-manager | -- | Root cause 1: mixed separators -- fixed by normalising forward slashes before join. Root cause 2: file never created on allocation -- fixed by creating file+parent dirs on first open (ISPF behaviour). 499 tests passing. |

| Phase BR | B028 added -- `resolve_dataset_path` produces mixed-separator path on Windows when catalog `path` contains forward slashes; file not found |
| Phase BR | B028 FIXED -- `resolve_dataset_path` now normalises forward slashes to OS separator before joining; mixed-separator path on Windows eliminated; 497 tests passing |
| Phase BR | B028 FIXED -- two root causes: (1) mixed separators in `resolve_dataset_path` fixed by normalising forward slashes; (2) physical file never created on allocation -- fixed by `create_dataset_file` helper that creates file+parent dirs on first open; 499 tests passing |
| Phase DB (post) | B032 added -- Settings opens the flat SettingsPanel instead of the data-driven Settings_Menu (Menu_Workspace); cw-requirements Phase CW-impl top-level migration never wired |
| Phase CL | B001 FIXED -- `ensure_pom_tab_present()` added to `shell/update.rs`; startup block calls it after session restore; POM prepended at index 0 when no POM tab in restored session; 589 tests pass |
| Phase CM review | B004 FIXED -- primary command area (Command ===>) and gutter prefix area both present since Phase U; original description no longer accurate |
| Phase CM review | B031 added -- gutter prefix area shows line numbers as static text only; user cannot type line commands (D/I/R/C/M etc.) into gutter rows; ff-line-commands crate is implemented but not wired to gutter input |
| B031 | FIXED | High | `ff-desktop` | The editor gutter (prefix area) displays line numbers as static read-only text. The user cannot type a line command (D, I, R, C, M, A, B, X, etc.) into any gutter row. The `ff-line-commands` crate is fully implemented but the gutter in `editor_panel.rs` renders numbers via `ui.painter().text()` with no per-row `TextEdit` or input capture. | Req 1 line-commands | -- | Root cause: submission condition used `lost_focus() && key_pressed(Enter)` -- egui never delivers both in the same frame. Fix: removed `key_pressed(Enter)` guard; submission now fires on `lost_focus()` alone when field is non-empty. 3 new unit tests. All workspace tests pass. |
| Phase B031 fix | B031 FIXED -- gutter prefix submission condition corrected: removed `key_pressed(Enter)` guard; `lost_focus()` alone triggers submit; 3 new unit tests; all workspace tests pass |
| Phase CX | B033 added -- `logging.directory` ignored at runtime; `init_default()` called before config load and never reconfigured; logs always go to platform default. Gated via CR-CH-015 (two-phase init / `ff_logging::reconfigure`). |
| Phase DC | B033 FIXED -- `ff_logging::reconfigure` (Req 11) + `ff-desktop::apply_logging_config` two-phase wiring; config-only log redirection now works; 9008 tests pass, verify.ps1 clean. |
| Phase (command-resolution) | B032 FIXED (CR-CH-025) -- removed the hardcoded `SETTINGS` verb; Settings opens via keyword-less Menu_Name resolution (unified command-resolution chain), so it is a real Menu_Workspace like POM/user menus; flat config browser moved to the `CONFIG [<namespace>]` command. |
| Phase (config-tab) | B058 added + FIXED -- CONFIG/Settings panel had a phantom Tab stop before the Filter field; the SettingsPanel arm was never wired into the CR-CH-023 tab-order model. Fixed by a stable Filter-field id reported as first/last interior + focus latch (mirrors B057). Regression test `full_shell_config_first_tab_focuses_filter_field`. verify.ps1 CLEAN. |
| Phase (workspace-conformance) | B059 added -- audit found Plugin Manager / Event Log / Macro Library / Search Results / Command Configurator arms all latent phantom-stop (same class as B056/B057/B058). Added `.kiro/steering/workspace-conformance.md` (mandatory focus-contract + full-shell test for every workspace). Fixing each arm TDD. |
| Phase (workspace-conformance) | B059 FIXED -- all five arms wired (report first/last interior + honour_interior_focus_latch); five `full_shell_*_first_tab_focuses_interior` regression tests; RED-confirmed for Plugin Manager. FilesPanel/FileEditor excluded (special cases). verify.ps1 CLEAN; ffwb.exe rebuilt. |

| B032 | FIXED | Medium | `ff-desktop` | The Settings workspace does not use the Menu Workspace (menu-option) framework. It opens as the custom flat-list `SettingsPanel` (TabKind::SettingsPanel) instead of the data-driven Settings_Menu -- a Menu_Workspace (TabKind::MenuWorkspace) backed by `menus/settings.toml` as defined by the menu-workspace spec (requirements.md Req 1-3, 11; design.md Sections 3, 6, 7, 11). menu-workspace Req 11.2 states `MENU SETTINGS` resolves to `menus/settings.toml`; cw-requirements.md defines Settings_Menu as the Menu_Workspace and says opening Settings opens the Settings_Menu (namespace selector), with the flat list being the secondary Settings_Namespace_View. The `settings.toml` default file and MenuWorkspace machinery exist, but `SETTINGS`/`0`/`=0` still call `open_settings_view(None)` which opens the flat `SettingsPanel` directly; the top-level migration to the Menu Workspace pattern (Phase CW-impl) was specced but never wired. | menu-workspace Req 2.1, 3.1, 11.2; cw-requirements.md Req 9.1, 10.4; configuration-system Req 15.1 | 🔲 Manual: SETTINGS / option 0 opens the Settings_Menu list (Menu_Workspace) backed by settings.toml; selecting a namespace opens the filtered flat view; A opens the full flat list; F3/END from the menu returns to POM | Re-reported via prompt: "settings menu not using the proper menu option framework". Duplicate of the original B032 report ("the Settings workspace is supposed to be a menu workspace, but is still a custom workspace when running the application"); kept as one entry to avoid a duplicate ID. Fix deferred -- sort out later. Requirements gate not needed: criteria already exist in menu-workspace and cw-requirements; this is a wiring task (Phase CW-impl). Re-reported again (Phase core-test-triage): "Settings menu is not a menu based workspace. it should largely look like the POM except the option list should be configurable, and each option should have a matching command. i should be able to switch to each option in the settings menu by typing in the related command for each option on the command line and pressing enter." Confirms scope: Settings = POM-modelled Menu Workspace with per-option command routing. Underpinned by the menu-workspace editor CR-NR-068 + configurable menu bar CR-NR-067. Still a wiring task; no new id. FIXED (Phase command-resolution, CR-CH-025): the hardcoded `SETTINGS` verb was removed; opening the Settings menu is now keyword-less Menu_Name resolution of the token `SETTINGS` through the unified command-resolution chain (command-framework Req 8.11), so the Settings_Menu is a real Menu_Workspace exactly like `POM` or any user menu, and `SETTINGS T` chains to the option keyed `T` (Req 11.7). The flat config-key browser moved to the `CONFIG [<namespace>]` command. Tests: `settings_command_opens_menu_workspace_not_flat_panel`, `settings_menu_end_returns_to_pom`, `settings_t_chains_to_theme_editor`. verify.ps1 CLEAN (FULL, nextest). |
| B033 | FIXED | Medium | `ff-desktop` | The `logging.directory` config setting is ignored at runtime. `ff-desktop/src/main.rs` calls `ff_logging::init_default()` (platform default directory) in startup step 1, BEFORE the configuration system loads in step 2, and never re-points the log sink at the configured directory afterwards. Consequently logs always land in the platform default (`%LOCALAPPDATA%\FileForgeWorkbench\logs` on Windows) regardless of `logging.directory` in any config layer, contradicting logging-subsystem Req 4.1/4.2. `ff-logging` exposes `init`/`init_default` but no post-config reconfigure path. | Req 4.2 logging-subsystem (+ new Req 11) | 🔲 Manual: set `logging.directory` in `.ffworkbench/config.toml`, launch, verify log files appear in the configured directory not the platform default | Fixed Phase DC (CR-CH-015): two-phase init added -- `ff_logging::reconfigure(LogConfig)` (Req 11) swaps the file sink after config loads; `ff-desktop::apply_logging_config` builds LogConfig from resolved `logging.*` keys and reconfigures before the GUI shell. `.ffworkbench/config.toml` redirects dev logs to `.ffworkbench/logs`. 9008 tests pass, verify.ps1 clean. |

| B034 | FIXED | Critical | `ff-file-ops` | Durability failures in the save path are silently swallowed. `persistence.rs:63-65,78,92,143-145,180-182` discard the results of `flush().await`, `sync_all().await`, `close().await`, and temp-file cleanup with `let _ = ...`. `save.rs:47` drops a failed backup with `let _ = e;` and a comment stating "In production this would use ff-logging WARN". A non-durable or corrupted save produces no log record, so data-loss defects are undiscoverable from the logs. | logging-subsystem Req 8 (error message standards); ff-file-ops has no logging criterion yet | -- | Reported via prompt: "make a list of possible gaps in logging". Logging-gap bug. Fix must add WARN/ERROR at each swallowed durability error; requirements gate needed for ff-file-ops logging criteria before code. FIXED (Phase bug-sweep, Wave 1, gate CR-NR-085 adding file-operations Req 7.10-7.12): `persistence.rs` -- AtomicWriteStrategy now CHECKS flush()/sync_all() of the temp file: on failure it deletes the temp, `log_error!`s, and returns `FileOpsError::VfsWriteError { operation: atomic_write_flush|atomic_write_fsync }` BEFORE the rename (a non-durable temp never overwrites the good target); a temp that cannot be re-opened for fsync `log_warn!`s (reduced crash-safety) instead of silently continuing; a close() failure after a successful fsync is `log_warn!`-ed (data already durable). DirectWriteStrategy / DeleteFirstStrategy `log_warn!` on flush/sync_all/close failure (best-effort durability, Req 7.11) rather than `let _ =`. `save.rs` -- the backup-failure `let _ = e` is replaced with `ff_logging::log_warn!` (Req 7.5/7.12); the save still proceeds. Tests (`persistence_tests.rs`): `atomic_write_aborts_and_preserves_target_on_fsync_failure`, `..._on_flush_failure` (target preserved + temp cleaned + error; confirmed RED before the fix), `atomic_write_still_succeeds_when_fsync_ok`, `direct_write_handles_fsync_failure_without_panic`, `delete_first_handles_fsync_failure_without_panic`; the mock `MockVfsFile` gained `FailOn::Flush`/`Fsync` injection. The WARN-emission for the non-fatal paths needs a running log subsystem and is verified by code review / MANUAL. verify.ps1 CLEAN (FULL, nextest). |
| B035 | FIXED | High | `ff-vfs` | The VFS abstraction -- the I/O core every file operation flows through -- emits zero log records. Non-test code contains `expect`/`unwrap`/`let _ =` with no diagnostic output, so any underlying I/O failure loses its origin context by the time it surfaces downstream. | logging-subsystem Req 7 (multi-crate coverage) | -- | Reported via prompt: "possible gaps in logging". Logging-gap bug. Fix: add WARN/ERROR at VFS error boundaries; gate ff-vfs logging criteria first. FIXED (Phase bug-sweep, Wave 2, gate CR-NR-086 -> logging-subsystem Req 9.6): added `ff-logging` as a dep to `ff-vfs`; a `log_vfs_err(operation, uri, err)` helper `log_warn!`s the operation + `scheme://path` and returns the error unchanged; the `Vfs` dispatch methods read/write/delete/rename/stat/list all route their error path through it, so an I/O failure crossing the abstraction is no longer invisible. Test `vfs::tests::dispatch_error_propagates_unchanged_through_log_wrapper` proves the error round-trips unchanged; log EMISSION is MANUAL (no cross-crate capture sink). verify.ps1 CLEAN (FULL, nextest). |
| B036 | DEFERRED | High | `ff-connectors` | Remote connectors (FTP/SFTP, cloud, network-fs, mainframe) emit no log records. Only `ff-connector-local-fs` logs. Network/remote I/O failures (timeouts, auth failures, partial transfers) leave no trace, making remote-access bugs undiscoverable from the logs. | logging-subsystem Req 7 | -- | Reported via prompt: "possible gaps in logging". Logging-gap bug spanning ff-connector-ftp-sftp, ff-connector-cloud, ff-connector-network-fs, ff-connector-mainframe. Gate connector logging criteria first. DEFERRED (Phase bug-sweep, Wave 2): investigation found NONE of these connector crates exist -- only `ff-connector-local-fs` (real, already logs) and `ff-connector-extensibility` (the plugin TRAIT/framework for future connectors, docs-only). So there is no code to instrument now. REFRAMED as a forward-looking, BINDING obligation recorded in logging-subsystem Req 9.8 (CR-NR-086): every VFS connector -- current and future -- SHALL emit Log_Records for connect/authenticate failures and recoverable I/O errors per Req 9.1, verified at EACH connector's own gate when it is built. Re-open/close as each remote connector lands. Not a code fix in this wave. |
| B037 | OPEN | Medium | `ff-connector-local-fs` | Directory-entry metadata failures are silently dropped. `provider.rs:272,274,437,474` and `metadata.rs:121-134` use `.ok()` on `metadata()`/`modified()`/`file_type()`/`read_link()`, so entries may under-report size, type, or mtime with no log record. A user seeing wrong file sizes/dates has no diagnostic trail. | file-tree-panel Req 15; logging-subsystem Req 8 | -- | Reported via prompt: "possible gaps in logging". Logging-gap bug. Fix: DEBUG/WARN when metadata retrieval fails per entry. FIXED (Phase bug-sweep, Wave 2, gate CR-NR-086 -> logging-subsystem Req 9.7): in `ff-connector-local-fs` the `.ok()`-swallowed per-entry failures now `log_debug!` the entry + attribute and fall back to the previous absent/partial value (graceful degradation preserved): `provider.rs` `list` (metadata, modified-time, file_type) and `stat` (modified-time), and `metadata.rs::stat` (modified/created/accessed timestamps + `read_link` symlink target). Regression guard `create_and_list_directory` now asserts size+modified are populated on the success path after the restructure. Log EMISSION is MANUAL. verify.ps1 CLEAN (FULL, nextest). |
| B038 | OPEN | Medium | `ff-desktop` | Logging fallback and drop diagnostics are never surfaced to the user. `ff_logging::is_fallback()`, `dropped_count()`, and `WorkbenchApp::logging_status()` (ff-core/app.rs:96) are captured but no status-bar/render code reads them. If logging silently degrades to no-op mode (e.g. log directory not writable) or drops records under load, the user has no indication. | logging-subsystem Req 1 (fallback), Req 8.5 (drop counter); menu-and-statusbar | 🔲 Manual: force log dir unwritable, launch, verify status bar shows a logging-degraded indicator | Reported via prompt: "possible gaps in logging". Logging-gap bug. Fix: render is_fallback()/dropped_count() in the status bar; gate the status-bar criterion first. FIXED (Phase bug-sweep, Wave 2, gate CR-NR-086 -> logging-subsystem Req 8.7): the shell status-bar render now reads `ff_logging::is_fallback()`/`dropped_count()` via a pure `logging_degradation_reason(is_fallback, dropped) -> Option<String>` helper and shows a red `LOG!` indicator (with a tooltip naming the fallback and/or dropped count) when degraded, hidden when healthy; registered for automation under `ids::LOGGING_DEGRADED`. Tests: `logging_degradation_reason_covers_all_states` (pure, all states) + `full_shell_status_bar_hides_logging_indicator_when_healthy` (egui_kittest, healthy -> hidden); the DEGRADED render is MANUAL (no test hook to force the global fallback state). verify.ps1 CLEAN (FULL, nextest). |
| B039 | FIXED | High | `ff-desktop` | Theme changing has ceased to work. Selecting a theme from the Settings menu calls `set_theme()` (`shell/render_chrome.rs`), which sets `self.palette` in-memory AND persists via `set_user_value(theme.active, section_name())`. The per-frame hot-reload block in `shell/update.rs` (~L392-405) then re-reads `theme.active` through `config_handle.get_string` and rebuilds the palette from the persisted value, so the persisted value wins each frame. Two confirmed defects break the round-trip: (1) MODE-STRING MISMATCH -- `VisualMode::section_name()` emits `"high_contrast"` (underscore) but the `theme.active` schema `allowed_values` in `main.rs register_builtin_schema` lists `"high-contrast"` (hyphen). On `reload_file`, `validate_table`/`validate_value` (`ff-config/src/validate.rs` L110-113) rejects the out-of-set value and substitutes the schema default `"dark"` -- so High Contrast never sticks and reverts to dark next frame. (2) FOLLOW-OS CLOBBER + UNREGISTERED KEY -- the block at `shell/update.rs` ~L410-425 reads `theme.follow_os` and, when true, overwrites `self.palette` from the OS dark/light preference every frame, silently undoing any selection; `theme.follow_os` is defined in `ff-config/src/keys.rs` but is NOT registered in `register_builtin_schema`, so its effective value is not schema-guaranteed false. If any config layer leaves it true, all theme switches are reverted. | theme-and-appearance Req 5.x (mode round-trip), Req 16.1-16.4 (follow OS); configuration-system Req 7.4 (enum validation), Req 15.4 (user write) | 🔲 Manual: (a) select each theme from Settings menu -- Dark/Light/Legacy apply and persist; High Contrast should apply and persist; (b) set theme.follow_os=false and confirm menu selections stick across frames | Reported via prompt: "After recent changes, Theme changing has ceased to work". Diagnosed read-only. Root cause is a spec/wiring gap, not a recent code edit to the theme path itself (theme wiring in update.rs/render_chrome.rs unchanged since Phase DB); the reported regression correlates with the config-schema state (B033 two-phase config init / schema registration). Fix path: (1) align `section_name()` output and `theme.active` allowed_values on a single spelling (`high_contrast`); (2) register `theme.follow_os` with default false and/or only apply follow_os when the user has not made an explicit selection. Requirements gate required before code (touches theme-and-appearance + configuration-system criteria). Re-reported (Phase core-test-triage): "Theme changing also not working. This should also be part of the core." Confirms the bug and elevates theme changing to CORE scope (needs a Core Acceptance Test Plan row). Separately, CR-NR-070 adds a Theme Settings workspace + theme commands (set-theme, invoke-theme-settings) -- pair the B039 fix with CR-NR-070 so the workspace operates on a working theme pipeline. FIXED (Phase theme-fix): single confirmed root cause -- `theme.active` schema `allowed_values` in `main.rs::register_builtin_schema` listed `"high-contrast"` (hyphen) but `set_theme()` persists `VisualMode::section_name()` = `"high_contrast"` (underscore); config validation rejected the out-of-set value and substituted the schema default `"dark"`, so High Contrast reverted every reload. Fix: changed the schema `allowed_values` (and description) to `"high_contrast"` to match `section_name()`. The follow_os concern in the original diagnosis was NOT a defect: `theme.follow_os` IS registered (ff-config `register_core_schema`, default false) and the per-frame OS-follow block is gated behind `if follow_os` and is spec-compliant with theme-and-appearance Req 16.4 (follow_os false -> theme.active used exclusively). No requirements gate needed -- fix restores existing criteria (theme-and-appearance Req 5.2/5.3, configuration-system Req 7.4). Regression test `theme_active_allowed_values_accept_every_visual_mode_section_name` added (registers the builtin schema, asserts every VisualMode::section_name() is in allowed_values AND round-trips via from_str_loose). 858 ff-desktop tests pass; verify.ps1 CLEAN (full, nextest). FOLLOW-UP (Phase theme-command): owner re-reported "theme switching still not working via menu or the THEME command". Two findings: (a) the `THEME` command was never implemented (fell through to the command engine's generic "command not yet implemented") -- NOT part of B039; implemented under theme-and-appearance Req 17 (command parity, architecture-brief Principle 2). (b) The menu previously called `set_theme` directly, bypassing the command layer, and `set_theme` swallowed persist errors with `let _ =` (a silent-revert path: if the persist failed, the next frame's read-back reverted the palette). Hardened `set_theme` to surface persist failures (no silent revert) and routed the Settings menu buttons through `handle_command("THEME <mode>")` so menu == command. The B039 schema fix itself is verified correct at source; the persistent user-visible symptom is most consistent with a STALE BINARY (source fix present but app not rebuilt -- cf. B007/B013) plus the now-fixed silent-revert path. Retest after a fresh `cargo build`/run. FIXED (Phase theme-followos, confirmed by runtime logging): the REAL root cause was the `theme.follow_os` clobber that the original bug note flagged and I wrongly dismissed. Instrumented `[THEME-DIAG]` logging proved the sequence every frame: `set_theme` set the palette to Legacy, persist OK, readback=legacy, then the per-frame follow_os block (update.rs) -- because `theme.follow_os=true` (leaked into the real user config by test-isolation hazard B048) -- rebuilt the palette back to the OS default (Dark). So the theme was set and clobbered on every frame and never visibly changed. Fix: `set_theme` now turns `theme.follow_os` OFF whenever the user makes an explicit selection (menu or THEME command), per theme-and-appearance Req 16.4/16.7 (follow_os must not override an explicit choice). Also fixed the leaked config (`follow_os=false`) and the underlying B048 hazard (see below). Regression test `set_theme_disables_follow_os_so_selection_is_not_clobbered`. verify.ps1 CLEAN full nextest. |

| B040 | FIXED | Critical | `ff-desktop` | FFWB crashes (process exits) the moment the "Modern File Explorer (preview)" toggle is enabled from Settings > Utilities. Enabling `nav_explorer_preview` causes `render_nav_explorer_preview` to run on the next frame and the application panics. | file-tree-panel Req 24.1, 24.3 | 🔲 Manual: open File Explorer (option 2), Settings > Utilities > check "Modern File Explorer (preview)" -- app must not crash; tree loads home dir | Reported via prompt: "The switch is not working, when i select the option, FFWB shuts down!". ROOT CAUSE (confirmed by reproduction, backtrace captured): `render_nav_explorer_preview` constructed `PosixProvider::new` -> `LocalFsProvider::new` -> `FileWatcher::new` which calls `tokio::spawn` at `ff-connector-local-fs/src/watcher/mod.rs:94`. `tokio::spawn` requires a Tokio runtime context; the provider was built directly in the egui update loop (winit main thread, NOT inside the runtime), so spawn panicked "there is no reactor running". The editor's TabManager avoids this by constructing its provider inside `runtime.block_on`. FIX: wrap both provider-construction sites in `render.rs` (seed path + Expand-effect path) with `let _rt_guard = self.runtime.enter();` so the watcher can spawn. Verified: forced preview on + File Explorer active, app ran 7s with no panic (empty stderr). |

| B041 | FIXED | High | `ff-desktop` | Creating a Mainframe catalog and then allocating a dataset to it fails with "Allocation failed: [catalog] validate: repository invalid at '...catalogs\mainframe\{name}': repository root does not exist". Catalog creation only stored `VirtualCatalog` metadata in the registry (persisted to `catalogs.toml`); the physical `ff-dscatalog` repository structure (`storage/`, `pds/`, `gdg/`, `temp/`, `catalog.db`) was never created. `Repository::initialize()` was called only in tests; the New Catalog dialog's `create_repository_now` checkbox was inert (never read). At allocation time `Catalog::mount()` -> `Repository::validate()` failed because the directory did not exist. This also made the modern explorer unable to expand the (nonexistent) catalog directory. | virtual-catalog-manager Req 12.3, 13.1 | 🔲 Manual: create a Mainframe catalog, allocate a dataset -- allocation succeeds and the repository dirs appear on disk; expand the catalog in the explorer | Reported via prompt: "creating new catalogs ... and allocating datasets ... is currently broken: repository root does not exist". Root cause via context-gatherer trace: metadata-only creation, no repository init in any production path. FIX (two layers): (1) creation-time -- `catalog_manager_dialog.rs` OK branch now calls `Repository::new(path).initialize(name)` for Mainframe catalogs when `create_repository_now` is set (default on), surfacing any failure as a form error; (2) self-heal at mount -- `catalog_registry.rs::open_dscatalog` initialises the repository if `validate()` fails, healing catalogs already persisted metadata-only (idempotent: `create_dir_all` + `IF NOT EXISTS` schema + `INSERT OR IGNORE`). New unit test `allocate_into_uninitialised_mainframe_catalog_self_heals` passes. |

| B042 | FIXED | Medium | `ff-desktop` | In the modern File Explorer, the context-menu edit operations (Delete, Rename, New File/Folder, Paste) failed on a file or folder inside a POSIX or Native catalog with "Delete is only supported for local files in this view." (and the equivalent Rename/New/Paste messages). Root cause: catalog-subtree child nodes inherit the `catalog` URI scheme from the catalog root (`child_uri` reuses the parent scheme) with a catalog-root-relative path `/{name}/{subpath}`, but the edit-op guards accepted only `posix`/`local`, and they built their writable `PosixProvider` rooted at the HOME directory rather than the catalog's backing path. So catalog-subtree edits were rejected (and would resolve wrongly even if allowed). Local Files edits worked. Mainframe dataset nodes use scheme `dataset` and are a separate (Slice B) case. | file-tree-panel Req 24.6, 16.10-16.13 | 🔲 Manual: expand a POSIX/Native catalog, right-click a file, Delete/Rename/New/Paste -- must operate on the catalog's real host file, not error | Reported via prompt: "Delete is only supported for local files in this view." FIX: added a shell `nav_edit_provider(uri)` resolver -- posix/local -> writable provider rooted at home + `uri.path()`; catalog -> `split_catalog_uri_path` + `registry.get_by_name` -> writable provider rooted at the catalog `path` + catalog-relative sub-path (rejects read-only catalogs and Mainframe/dataset with a clear message). All four edit-ops (delete/rename/new/paste) + parent refresh (`refresh_nav_parent`, scheme-dispatched) rewired to use it; paste resolves source and destination providers independently so cross-catalog copy works. Mainframe `dataset`-scheme edits remain deferred to Slice B. Test `catalog_child_uri_splits_to_catalog_relative_path_for_edit_ops` proves the catalog-rooted provider + relative-path delete. |

## Changelog (logging-gap batch)

| Date | Change |
|------|--------|
| Phase (log-audit) | B034 added -- ff-file-ops save path swallows flush/sync/close/backup errors (Critical logging gap) |
| Phase (log-audit) | B035 added -- ff-vfs I/O core has zero logging (High logging gap) |
| Phase (log-audit) | B036 added -- remote connectors (FTP/SFTP, cloud, network-fs, mainframe) emit no logging (High logging gap) |
| Phase (log-audit) | B037 added -- ff-connector-local-fs metadata failures dropped via .ok() (Medium logging gap) |
| Phase (log-audit) | B038 added -- logging fallback/drop diagnostics never shown in status bar (Medium logging gap) |
| Phase (theme-triage) | B039 added -- theme changing ceased to work; two root causes: high_contrast/high-contrast mode-string vs allowed_values mismatch (validation reverts to dark), and follow_os per-frame palette clobber with theme.follow_os unregistered in schema |
| Phase (settings-menu-triage) | B032 re-reported ("settings menu not using the proper menu option framework") -- existing OPEN entry strengthened to cite the menu-workspace framework spec (Req 2.1/3.1/11.2) rather than duplicating a new ID; fix deferred |
| Phase (nav-preview-crash) | B040 added -- Modern File Explorer preview toggle crashes FFWB; root cause: PosixProvider/LocalFsProvider watcher `tokio::spawn` called outside a runtime context in the egui update loop |
| Phase (nav-preview-crash) | B040 FIXED -- wrapped both provider-construction sites in `render.rs` with `self.runtime.enter()` guard; reproduced the panic (backtrace at watcher/mod.rs:94), confirmed fix by running with preview forced on for 7s with no crash |
| Phase (catalog-repo-init) | B041 added -- creating a Mainframe catalog never creates its on-disk repository; first dataset allocation fails "repository root does not exist" |
| Phase (catalog-repo-init) | B041 FIXED -- initialise repository at catalog creation (wired the inert `create_repository_now` checkbox) and self-heal at mount in `open_dscatalog` (idempotent `Repository::initialize`); new test allocates into an uninitialised catalog and succeeds |
| Phase (catalog-edit-ops) | B042 added -- modern explorer Delete/Rename/New/Paste reject POSIX/Native catalog subtree files ("only supported for local files"); catalog children carry scheme `catalog` + catalog-relative path but edit-ops accept only posix/local and root the provider at HOME |
| Phase (catalog-edit-ops) | B042 FIXED -- `nav_edit_provider` resolver roots a writable provider at the owning catalog path for `catalog`-scheme nodes (honors read_only, rejects Mainframe/dataset); all four edit-ops + refresh rewired; catalog-relative delete test added |

| B043 | FIXED | High | `ff-desktop` | The `SWAP` command does not work. Expected ISPF behaviour: `SWAP n` switches to the n-th tab/workspace; `SWAP 1` -> first tab, `SWAP 2` -> second tab; `SWAP LIST` pops up a selectable list of open tabs where the user can either click one with the mouse or type a number and press Enter to switch to that tab. None of these currently function. Compounding gap: there is no acceptance criterion in any requirements.md for `SWAP`, and no row in the Core Acceptance Test Plan -- so this behaviour is neither specified nor tested. | multi-tab-editor (SWAP command -- NO criterion exists yet); menu-and-statusbar 12 (PF9 swap is split-screen focus, a different concept) | 🔲 Manual: type `SWAP 2` on the command line -> active tab becomes the 2nd tab; `SWAP 1` -> 1st tab; `SWAP LIST` -> tab-picker list; click a row or type a number + Enter -> switches to that tab | Reported via prompt: "SWAP command does not work. SWAP 2 Should switch to 2nd Tab ... SWAP List should popup a list of tab's ... No test criteria in the test to test this!" This is BOTH a bug and a requirements/test gap: `SWAP` (n / LIST) must be specified in multi-tab-editor requirements + given Core Acceptance Test Plan rows before/with the fix (new-requirement gate applies to the missing criterion). Diagnose read-only first. FIXED (Phase swap-command, gate CR-CH-017): root cause was a naming collision -- `SWAP` was only wired for split-screen focus swap (Req 19.12); `SWAP n`/`SWAP LIST` fell through and did nothing. Gated as multi-tab-editor Req 18, then implemented: `SWAP n` activates the n-th tab (1-based, validated, out-of-range/invalid errors); `SWAP LIST` opens a tab picker overlay (rows `{n}: {title}`, click OR type-number+Enter to select, Escape cancels); bare `SWAP` keeps swapping split focus when a split is active and otherwise opens the picker. Command-layer routed (command parity). 7 SWAP tests (5 new + 2 preserved); verify.ps1 CLEAN full nextest. NOTE: the picker's mouse-click/number-Enter selection is egui-render (covered by the command-layer flag + set_active tests, not a headless UI click). |
| B044 | PARTIAL FIX | Medium | `ff-desktop` / `ff-connector-local-fs` | File Navigator "Local Files" node shows content that does not match the real filesystem, and some nodes are non-functional. Observed: (1) it is unclear what physical path "Local Files" is rooted at; (2) the "Documents" folder shown under it lists three empty child nodes "My Music", "My Pictures", "My Videos" that do not reflect the actual drive contents; (3) a "My Documents" node is shown but is NOT expandable and double-clicking it does nothing (likely a Windows compatibility junction -- see B017 which hid junctions in the legacy panel; the modern NavModel explorer does not apply the same handling). | file-tree-panel Req 24.1, 24.5 (modern NavModel explorer); relates to B017 (junction handling in legacy panel) | 🔲 Manual: open File Explorer (option 2), expand Local Files -> the tree matches the real user profile contents; the shown root path is discoverable; junction/redirect folders (Documents/My Documents/My Music etc.) either resolve to their real target or are omitted, never shown as dead/empty non-expandable nodes | Reported via prompt: "File Navigator: has a node for 'Local Files' ... some obvious folders ... do not contain what is actually on the drive ... 'Documents' ... has three nodes ... 'My Music', 'My Pictures' and 'My Videos' all empty ... 'My Documents' ... not expandable and double clicking on it nothing happens." Diagnose read-only: confirm the NavModel Local Files seed root and how it enumerates Windows known-folder junctions vs the legacy panel's `collect_native_entries` metadata-skip (B017). PARTIAL FIX (Phase nav-open-fix): (1) FIXED -- restored the B017-style per-entry resilience in the modern path: `LocalFsProvider::list()` previously aborted the WHOLE listing with `?` if any single entry's `file_type()` failed; now it skips that entry (logs DEBUG) and continues, so one locked/permission-denied entry can no longer blank or truncate a directory. (2) SEED ROOT: "Local Files" is intentionally rooted at the user home directory (not the drive root) -- this is by design (home is the natural default); the "content doesn't match the drive" impression is the home-vs-drive expectation, not a defect. (3) CARVED OUT -- Windows known-folder junctions (Documents/My Documents/My Music/Pictures/Videos are directory reparse points) report via `DirEntry::file_type()` as symlink/neither, so they map to `NodeType::SymbolicLink` LEAF nodes (not expandable). Making directory junctions expandable requires following reparse points (with cycle/permission/policy decisions -- follow all symlinks? only dir junctions? depth?) which is a behaviour/spec decision, NOT a safe blind fix. Logged as CR-NR-072 (symlink/junction directory traversal in the navigator) for the gate. Kept the per-entry-skip fix; did not guess a follow-symlinks policy. CONFIRMED (Phase bug-sweep Wave 3b, read-only audit): the per-entry skip is live in `LocalFsProvider::list` (provider.rs ~L257-273: a per-entry `file_type()` error is logged at DEBUG and `continue`d, never aborting the listing); the success-path guard `create_and_list_directory` asserts a normal entry lists with size+type populated. The SKIP branch itself is a defensive I/O-error path that cannot be triggered portably in a unit test (it needs a real un-stat-able entry -- a locked file / permission-denied Windows known-folder junction / broken reparse point), so its verification is MANUAL (core test-plan row 3.2: a single locked/permission-denied entry must not blank or truncate the listing). CR-NR-072 exists (change-log, PENDING GATE) and test-plan row 3.2a ([B]) tracks the junction-expansion capability. This is the intended terminal state of B044 as PARTIAL: the code fix landed, the deferred capability is gated, and the test-plan + traceability map are in sync. No further code this wave. |
| B045 | FIXED | High | `ff-desktop` | Detaching a workspace does not work. There is no working way to detach a workspace tab into its own OS window. Dragging a workspace tab does not detach it. Right-clicking a tab shows a menu of tab-management actions ("close, close all but this, close all to the left, close all to the right, close all unchanged, clone to other tab, move to other tab, pin tab") but NO "Detach"/"Undock"/"Float"/"Move to new window" action. layout-and-docking Req 3 (Detached Workspaces) is fully specified (undock via context menu / shortcut / drag; drag a tab >20px outside the tab bar creates a Detached Workspace; redock on close) but is not wired into the running shell. This is CORE #1 (shell framework: tabbed, DETACHABLE workspaces) and Core Acceptance Test Plan rows 1.5/1.6 depend on it. | layout-and-docking Req 3.1-3.11 (undock/redock, drag-out); Core Acceptance Test Plan 1.5, 1.6 | 🔲 Manual: (a) right-click a tab -> a Detach/Undock action is present and creates a separate OS window with the workspace intact; (b) drag a tab >20px outside the tab bar and release -> new Detached Workspace at the release point; (c) close the detached window -> workspace redocks to its origin | Reported via prompt: "Detaching a workspace does not work? How are supposed to do that? ... dragging it does not detach it. Right clicking on the tab, brings up a menu 'close, close all but this, close all to the left, close all to the right, Close all unchanged clone to other tab move to other tab, pin tab and exit'". Diagnose read-only: layout-and-docking spec exists; determine what (if any) undock path is wired in ff-desktop and whether the tab context menu should expose Detach. RE-CONFIRMED (owner re-test): core-acceptance-test-plan rows 1.1-1.9 all PASS; detaching a workspace is STILL not working (rows 1.9/1.10/1.11 remain failing). Prioritised for fix as part of the outstanding-bug sweep; the fix runs the requirements gate (layout-and-docking detach criteria are unbuilt -- Phase AL/AO detachable-window work was deferred) before code, with automated coverage where headless-testable and a justified MANUAL note for real OS-window behaviour. FIXED (Phase bug-sweep Wave A, gate CR-CH-035, menu-and-statusbar Req 18.1-18.9): the detach path was already wired but the floating window rendered a placeholder `ui.label("Tab N -- floating")` because `show_viewport_deferred` takes a `'static move` closure that cannot borrow the shell. Fix: (1) render Detached Workspaces with `show_viewport_immediate` (synchronous, borrows `&mut self`) drawing the tab's REAL Context -- factored `render_active_tab_body(ctx, ui)` out of `render_central_panel` and the floating loop temporarily makes the detached tab active, renders it (Title_Line + content), and restores the active index (Req 18.1/18.2/18.8); (2) 80-char OS-title truncation via pure `truncate_title` routed through the shared `title_line_text` (Req 18.5); (3) faithful redock via new `TabManager::remove_at`/`insert_at`/`move_tab` -- remove+reinsert at the origin index preserving other tabs' order, NOT the old positional `swap` (Req 18.3/18.9); (4) `FloatingTab` now tracks the tab by stable `TabId` (resolved to the live index each frame via `index_of_id`) so concurrent detach/redock never desyncs; (5) unified the 16-window limit on `floating_tabs.len()` across BOTH the "Move to Other View" context item and the SPLIT/SPLIT DETACH commands (was an `is_floating`-count inconsistency). The primary tab bar already hid `is_floating` tabs (Req 18.4). Automated: `full_shell_detach_sets_floating_and_records_floating_tab`, `full_shell_redock_restores_tab_at_origin`, `full_shell_detach_rejected_at_16_window_limit` (build_eframe), plus `TabManager` `remove_at`/`insert_at`/`move_tab` order + active-follow + clamp unit tests and `truncate_title` (multibyte-safe). MANUAL (justified, testing.md real-multi-viewport exception): the actual separate OS window appearance/taskbar/move-resize, and drag-out-to-detach (Req 18.6, real pointer-vs-OS-window geometry) -- test-plan 1.10. verify.ps1 CLEAN (FULL nextest); ffwb.exe rebuilt. The detached window's title routes through the shared derivation so the Wave B Workspace Definition model swaps the title source in one place. FURTHER FIXED (Phase bug-sweep Wave A cont., CR-CH-036, Req 18.10): the owner reported the detached window had NO command line of its own and the primary window's command line controlled the detached context. Root cause: the shell has ONE `command_text` + one active-tab index and the whole command pipeline is bound to `active_tab()`. Fix WITHOUT a pipeline rewrite: each `FloatingTab` now owns a `WorkspaceCommandContext` (its own command_text/scroll/open_error/focus/outcome); the detached window renders its OWN salted `Command ===>` field (`render_detached_command_field`) and dispatches through the UNCHANGED pipeline under a scoped `with_workspace_context(tab_index, &mut ctx, f)` swap that temporarily installs the detached tab as active + its buffers, runs, then restores the primary context. So a command in the detached window acts on ITS tab and the primary command line is untouched (bidirectional isolation, proven by `detached_command_acts_on_its_tab_not_the_primary`; swap round-trip by `with_workspace_context_saves_and_restores_primary_context`). Also added drag-a-tab-out-of-the-bar to detach (Req 18.6: tab header now `Sense::click_and_drag`, >20px-outside-bar threshold sets detach_pending). Documented follow-ups: shared RETRIEVE ring across windows, and physical F-keys targeting a specific detached window (the command FIELD independence is delivered). Real multi-viewport OS window remains MANUAL. verify.ps1 CLEAN (FULL nextest); ffwb.exe rebuilt. |
| B046 | PARTIAL FIX | High | `ff-desktop` / `ff-keys` | Function keys "not working" -- reframed after read-only investigation + owner clarification. The PF DISPATCH MECHANISM WORKS (owner confirms F3/END fires correctly; unit test `egui_fkey_assigned_key_returns_command` proves F3->END resolves; nothing consumes F-key events before the dispatch block in `update.rs`). The real defects: (1) FIXED -- `KeyMap::default_global()` bound F7="UP MAX" and F8="DOWN MAX"; per navigation-commands Req 3.1/3.3 bare UP/DOWN scroll one page and MAX is invoked by typing `M`/`MAX` on the command line then UP/DOWN, so the defaults are now F7="UP", F8="DOWN". (2) OPEN (coverage/config) -- only F1/F3/F7/F8/F12 have defaults; F2/F4/F5/F6/F9/F10/F11 (and Ctrl/Alt/Shift variants) are unbound. Owner: the WHOLE keylist F1-F12 + modifiers must be configurable, defaults as listed but alterable through configuration -> this is the Key Assignment editor (CR-NR-069) + fold function-keys spec into CORE. (3) F1/HELP fires but shows "Help not yet available for ..." -- help CONTENT is missing (see CR-NR-071). | function-keys-and-history (whole spec, CORE); navigation-commands Req 3.1/3.3 (UP/DOWN vs MAX); CR-NR-069 (configurable keylist); menu-and-statusbar 11-14 (PF2/PF9/PF3 split) | 🔲 Manual: F7/F8 scroll one page (not to end); typing M/MAX then UP/DOWN scrolls to top/bottom; F1 opens help; each configured PF key fires | Reported "Functions keys are not working". Owner clarified: "Up Max and Down Max should not be bound ... they should be up and down only and only Down Max When we type m or max on the command line and press the up or down key ... whichever screen we are on the whole keylist should be configurable ... f1 through f12 with its ctrl alt etc options ... default configuration set to [these] but alterable through the configuration ... END command is working correctly except when from a POM that is not the last open POM [CR-CH-016] ... F3 is working". PARTIAL FIX (Phase fkey-defaults): F7/F8 defaults corrected to UP/DOWN (ff-keys `default_global`); regression assertions updated. Remaining OPEN scope: configurable keylist (CR-NR-069) + PF coverage + help content (CR-NR-071). The MAX-on-UP/DOWN command-line modifier behaviour must be confirmed present in the scroll command engine (verify separately). RECONCILED (Phase bug-sweep Wave 4a, read-only audit): every remnant is now parcelled to a tracked item -- (a) configurable keylist F1-F12 + Ctrl/Alt/Shift -> CR-NR-069 (Key Assignment editor, PENDING GATE; test rows added at its gate); (b) F1/HELP content -> CR-NR-071 (PENDING GATE; test-plan 7.1 [B]); (c) PF2/PF9/PF3 split-screen -> menu-and-statusbar Req 11-14 (test-plan 7.4-7.6 [F], a separate build, not a keymap defect); (d) the MAX-on-UP/DOWN modifier was AUDITED and is NOT present -- `nav_manager.up`/`.down` with no numeric arg call `up_page`/`down_page` and never consult `self.scroll_amount`, so `SCROLL MAX` then UP/DOWN does NOT scroll to top/bottom. Per navigation-commands Req 3.1/3.3 bare UP/DOWN scrolling one page is the CURRENTLY-SPECIFIED behaviour (so this is not a violation of an existing criterion), but it does not match the ISPF SCROLL-field-governs-PF7/PF8 model the owner described. Logged as NEW requirement CR-NR-087 (SCROLL-amount-aware bare UP/DOWN: PAGE/HALF/MAX/CSR/DATA/n), PENDING GATE (owner scheduling); test-plan row 7.3a corrected to [F] and backed by CR-NR-087. No blind fix (changing UP/DOWN semantics needs owner approval + the gate). B046 remains PARTIAL: the dispatch works, F7/F8 defaults are fixed, and all residual capability is now gated (CR-NR-069/071/087) or a separate build (split-screen). |

| B047 | FIXED | High | `ff-desktop` | Opening a file from the modern File Navigator fails for real local files: "Cannot read '/OneDrive - Standard Bank/Clipbook.md': [vfs] read: resource not found: vfs://local/C:/On". The navigator `Open` (`resolve_open` -> `OpenTarget::Editor(uri)`) dispatches `file.open` with `uri.path()`, but the resulting VFS URI is malformed/mis-rooted: the Local Files tree is seeded via a root-jailed `posix`/`local` provider at the home dir, so a node path like `/OneDrive - Standard Bank/Clipbook.md` (which is relative to that jail root) is being resolved as an absolute `vfs://local/C:/On...` that does not exist. The path also appears truncated in the error. Root cause is the same navigator path-resolution family as B044: the provider-relative URI produced by the NavModel does not match how `file.open` resolves it. | file-tree-panel Req 24.2, 24.9 (open); virtual-file-system (URI resolution) | 🔲 Manual: expand Local Files, double-click a real file (incl. paths with spaces / OneDrive) -- it opens in the editor without "resource not found" | Reported via prompt: "Trying to open a file from the file navigator i get message: 'Cannot read '/OneDrive - Standard Bank/Clipbook.md': [vfs] read: resource not found: vfs://local/C:/On'". Diagnose read-only with B044 (shared navigator path-resolution root cause): reconcile the NavModel node URI (provider-relative, jailed at home) with the `file.open`/VFS resolution so real local files open. FIXED (Phase nav-open-fix): confirmed root cause -- the modern explorer seeds Local Files via a `posix` provider jailed at `home_dir`, so a node's `uri.path()` (e.g. `/OneDrive - Standard Bank/Clipbook.md`) is relative to that jail, but `file.open` -> `TabManager::open_file` reads through a default-rooted `LocalFsProvider` expecting a REAL absolute path. Fix: added `WorkbenchShell::nav_open_path(uri)` which joins the correct root (home for posix/local; catalog `path` for catalog subtrees) with the provider-relative path using the host separator (rejects `..` traversal), and rewired both `OpenTarget::Editor` and `OpenTarget::External` to dispatch/launch the resolved absolute host path instead of `uri.path()`. Two regression tests added (`nav_open_path_resolves_posix_uri_to_absolute_host_path` incl. the spaces/OneDrive case; `nav_open_path_rejects_parent_traversal`). 860 ff-desktop tests pass; verify.ps1 CLEAN full nextest. |

| B048 | FIXED | Medium | `ff-config` / test-suite | Tests that call `ConfigHandle::set_user_value` (e.g. theme/follow_os/reduce_motion tests, and `open_workspace_force` which writes workspace overrides) persist to the REAL user config file `%APPDATA%\ffworkbench\config.toml` because `paths::user_config_path()` uses `dirs::config_dir()` with no test override. Under parallel test execution (nextest, separate processes sharing the same file) this causes: (a) contamination -- a leaked key (e.g. `workspace.custom_key`) made `close_workspace_removes_settings_from_config` fail deterministically; (b) file corruption -- concurrent writes produced a malformed trailing line (`hello"`). Tests must be deterministic and must not read/write external files outside `tests/fixtures/` (testing.md). | configuration-system Req 15.4; testing.md (test isolation) | -- | Reported during the THEME command work (Phase theme-command). Interim mitigation applied: repaired the polluted real config file; made the new THEME tests self-clean via `remove_user_value`. Proper fix (needs its own change, possibly a gate): inject a config path for tests (e.g. an env override honoured by `user_config_path`, or a `set_user_value_to(path)` seam) so `set_user_value`-invoking tests write a TempDir, never the real user config. Several pre-existing tests share this hazard. FIXED (Phase theme-followos): added the `FFWB_USER_CONFIG_PATH` env override to `ff_config::paths::user_config_path()` (production behaviour unchanged when unset); `make_shell()` now sets it to a unique per-call temp file before `init()`, so every shell test's `set_user_value` writes a throwaway temp config instead of the real `%APPDATA%\ffworkbench\config.toml`. Under nextest (process-per-test) this fully isolates. All 866 ff-desktop tests pass under nextest; verify.ps1 CLEAN. This surfaced while fixing B039 (my `set_theme` follow_os write collided with `theme_follow_os_can_be_set_to_true` on the shared real file). RESIDUAL (observed Phase swap-command): the fix isolates fully under NEXTEST (process-per-test, the authoritative gate) but NOT under plain `cargo test` (in-process threads share the process-global `FFWB_USER_CONFIG_PATH`, so `close_workspace_removes_settings_from_config` can still flake there). verify.ps1 uses nextest and is clean. Fully closing the `cargo test`-threads case needs per-handle/thread-local config-path injection (a larger ff-config change) rather than a process env var -- tracked here, not blocking the gate. |

| B049 | FIXED | Low | `ff-keys` | Command-history de-duplication treats `THEME LEGACY` and `theme legacy` as two different entries. `HistoryEntry::is_duplicate_of` is case-insensitive on the command NAME (first token) but CASE-SENSITIVE on arguments (deliberate for FIND: `FIND 'ERROR'` and `FIND 'error'` are different searches, and there is a test enforcing this). For commands whose arguments are themselves case-insensitive (e.g. `THEME <mode>`, where `LEGACY`/`legacy` are the same action per theme-and-appearance Req 17.2), the case-sensitive-argument rule produces surprising duplicate history entries. | function-keys-and-history (history dedup); theme-and-appearance Req 17.2 | -- | Reported via prompt: "THE COMMAND HISTORY IS SUPPOSED TO DE-DUPLICATED. BUT IT SEES THEME LEGACY AND theme legacy AS 2 different commands". This is a behaviour/design decision, NOT an obvious bug: fully case-insensitive dedup would break the intended case-sensitive FIND-argument behaviour. Options: (a) normalise arguments per-command (case-insensitive for known case-insensitive verbs like THEME); (b) make dedup fully case-insensitive and revisit the FIND rule; (c) accept as-is. Needs an owner decision before code. Not fixed blindly. FIXED (Phase theme-followos, owner confirmed intent): chose the ISPF-accurate quote-aware rule -- `HistoryEntry::is_duplicate_of` now compares arguments case-INSENSITIVELY outside quotes and case-SENSITIVELY inside single/double quotes (`normalise_args_for_dedup`). So `THEME LEGACY` == `theme legacy` (unquoted) while `FIND 'ERROR'` != `FIND 'error'` (quoted operand stays distinct). This satisfies the owner expectation without breaking the deliberate case-sensitive search-string behaviour. Tests: `history_entry_dedup_case_insensitive_unquoted_args`, `history_add_deduplicates_theme_regardless_of_case`, and the existing `..._case_sensitive_inside_quotes` still passes. verify.ps1 CLEAN. |

## Changelog (CORE test-session batch)

| Date | Change |
|------|--------|
| Phase (core-test-triage) | B043 added -- SWAP command (SWAP n / SWAP LIST) does not work; also a requirements + test-plan gap (no criterion, no test row) |
| Phase (core-test-triage) | B044 added -- File Navigator "Local Files" shows content not matching the real drive; Documents shows empty My Music/My Pictures/My Videos; My Documents not expandable (junction handling gap in the modern NavModel explorer) |
| Phase (core-test-triage) | B045 added -- Detaching a workspace does not work; tab context menu has no Detach/Undock action and drag-out does not float; layout-and-docking Req 3 not wired (CORE #1, blocks test-plan 1.5/1.6) |
| Phase (core-test-triage) | B046 added -- Function keys not working; whole function-keys-and-history spec to be folded into CORE with a dedicated Core Acceptance Test Plan function-keys group |
| Phase (fkey-defaults) | B046 PARTIAL FIX -- dispatch confirmed working (owner: F3 fires); corrected F7/F8 defaults from "UP MAX"/"DOWN MAX" to "UP"/"DOWN" in ff-keys default_global (MAX is a command-line modifier, not an F-key binding). Remaining OPEN: configurable keylist (CR-NR-069) + PF coverage + help content (CR-NR-071) |
| Phase (fkey-triage) | B047 added -- opening a real local file from the modern File Navigator fails "resource not found: vfs://local/C:/On..."; navigator URI mis-rooted/malformed vs file.open resolution (shared root cause with B044) |
| Phase (theme-command) | THEME command implemented (theme-and-appearance Req 17, command parity) -- `THEME <mode>`/bare/invalid; Settings menu now dispatches the THEME command (menu == typed command); set_theme surfaces persist errors (no silent revert). B039 follow-up: source fix verified; residual symptom most consistent with a stale binary -- retest after rebuild |
| Phase (theme-command) | B048 added -- tests calling set_user_value write the REAL user config (%APPDATA%\ffworkbench\config.toml); no test path override; caused contamination + corruption that failed close_workspace_removes_settings_from_config. Interim: repaired the file, made THEME tests self-clean; proper fix = inject a config path for tests |
| Phase (theme-followos) | B039 FIXED (real root cause) -- runtime [THEME-DIAG] logging proved the per-frame follow_os block clobbered every explicit theme selection back to the OS default; `set_theme` now disables theme.follow_os on explicit selection (Req 16.4/16.7). The earlier schema-spelling fix was necessary but not sufficient; the follow_os clobber (flagged in the original bug note, wrongly dismissed) was the actual cause |
| Phase (theme-followos) | B048 FIXED -- added FFWB_USER_CONFIG_PATH override to ff_config::paths::user_config_path; make_shell sets a unique temp config per test so set_user_value never touches the real user config; 866 ff-desktop tests pass under nextest |
| Phase (theme-followos) | B049 added -- command-history dedup treats `THEME LEGACY` vs `theme legacy` as distinct (case-sensitive arguments, deliberate for FIND); needs an owner decision (per-command normalisation vs fully case-insensitive) before code |
| Phase (theme-followos) | B049 FIXED -- quote-aware dedup: arguments compared case-insensitively outside quotes, case-sensitively inside quotes; THEME LEGACY==theme legacy, FIND 'ERROR'!=FIND 'error'; 3 tests, verify.ps1 CLEAN |
| Phase (unified-menu-renderer) | B051 added + FIXED -- Settings/non-POM menus rendered white, not the POM colour scheme; shared renderer only had calendar colours. MenuCalendarColours -> MenuColours (adds option_key/command/description); shell menu_colours() maps from PomColours; rows drawn as coloured LayoutJob like the POM. verify.ps1 CLEAN |
| Phase (theme-editor-fixes) | B052 added -- Theme Editor "Save As" button no-op: token editor's lost_focus EditToken clobbers the button action in the same frame (single action slot). To be fixed with CR-CH-019 |
| Phase (theme-editor-fixes) | B052 FIXED -- render splits button action from token_action; button actions take priority; Save As no longer clobbered. verify.ps1 CLEAN |
| Phase (bug-sweep Wave 3a) | B050 FIXED / CR-CH-034 -- Tab_Header + Title_Line derive a non-Home Menu_Workspace label from the LIVE loaded menu (`menu_workspace.tab_title()`), not the cached `tab.title`, so an in-place context switch can never leave the header stale. menu-and-statusbar Req 17.10. Regression test `title_line_menu_workspace_uses_loaded_menu_not_stale_title`. verify.ps1 CLEAN (FULL nextest); ffwb.exe rebuilt |
| Phase (bug-sweep Wave 3b) | B044 CONFIRMED terminal-PARTIAL (read-only audit, no code) -- per-entry skip live in `LocalFsProvider::list` (success-path guard `create_and_list_directory`; skip branch is a defensive I/O-error path verified MANUALLY at test-plan 3.2); junction/symlink expansion stays CR-NR-072 (PENDING GATE, test-plan 3.2a [B]). bugs.md + test-plan + traceability in sync |
| Phase (bug-sweep Wave 4a) | B046 RECONCILED (read-only audit, no code) -- all remnants parcelled: configurable keylist -> CR-NR-069, F1 help content -> CR-NR-071, PF2/PF9/PF3 split -> menu-and-statusbar 11-14, and the MAX-on-UP/DOWN modifier AUDITED (nav_manager up_page/down_page ignores scroll_amount) -> logged NEW CR-NR-087 (SCROLL-amount-aware bare UP/DOWN), PENDING GATE. test-plan 7.3a corrected to [F] + backed by CR-NR-087; traceability map updated |
| Phase (bug-sweep Wave 4a) | CR-NR-087 added -- bare UP/DOWN (PF7/PF8) should honour the active SCROLL amount (PAGE/HALF/MAX/CSR/DATA/n) per the ISPF model; currently they always scroll one page (spec-compliant with navigation-commands 3.1/3.3 as written). PENDING GATE (owner scheduling); completes the B046 MAX-modifier remnant without re-binding PF7/PF8 |
| Phase (bug-sweep Wave A) | B045 FIXED / CR-CH-035 -- Detached Workspaces render REAL interactive content via `show_viewport_immediate` (was a placeholder under `show_viewport_deferred`); `render_active_tab_body` factored out and reused; 80-char title (`truncate_title`); faithful redock via `TabManager::remove_at`/`insert_at`/`move_tab` at the origin index; `FloatingTab` tracks tab by stable `TabId`; 16-window limit unified on `floating_tabs.len()`. menu-and-statusbar Req 18.1-18.9. Full-shell detach/redock/limit tests + TabManager reorder + truncate_title unit tests. Real OS window + drag-out (18.6) MANUAL. verify.ps1 CLEAN (FULL nextest); ffwb.exe rebuilt |
| Phase (bug-sweep Wave A cont.) | B068 FIXED + B045 detached bundle 2 / CR-CH-037, CR-CH-038, CR-NR-088, CR-NR-089 -- RETURN now goes to the POM (non-POM) / closes the workspace (from a POM), END walks back one step (CR-CH-038, menu-workspace 14.10); detached window Close (X) = RETURN on its own context, no redock/no Alt+F4 (CR-CH-037, redock_pending removed); F-keys work in the detached window targeting its context (B068); DOCK command re-docks to origin + default Shift+F2 = DOCK, Base F2 = SPLIT (CR-NR-088); detached window renders its own salted menu bar (CR-NR-089). Tests: return_from_non_pom_*, detached_function_key_return_acts_on_its_tab, full_shell_dock_*, key_map_default_global_has_full_shift_row. Real OS-window close + visual menu bar MANUAL. verify.ps1 CLEAN (FULL nextest); ffwb.exe rebuilt |
| Phase (bug-sweep Wave A cont.) | B045 / CR-CH-036 -- Detached Workspaces are INDEPENDENT command contexts: each `FloatingTab` owns a `WorkspaceCommandContext` (command_text/scroll/open_error/focus/outcome); the detached window renders its OWN `Command ===>` field (salted ids, own status) via `render_detached_command_field`, and dispatches through the UNCHANGED pipeline under a scoped `with_workspace_context` swap (install detached tab as active + its buffers, run, restore). Stops the cross-window command-line bleed + gives each window its own command line (menu-and-statusbar Req 18.10). Also implemented drag-a-tab-out-of-the-bar to detach (Req 18.6, `Sense::click_and_drag` + >20px threshold). Tests with_workspace_context_saves_and_restores_primary_context + detached_command_acts_on_its_tab_not_the_primary. Shared RETRIEVE ring + per-window physical F-keys = documented follow-ups; real OS window MANUAL. verify.ps1 CLEAN (FULL nextest); ffwb.exe rebuilt |

| B051 | FIXED | Medium | `ff-desktop` | The Settings Menu_Workspace (and every non-POM menu) rendered its whole option list in the default text colour (white), not the POM's colour scheme. Under the Legacy theme the POM colours option rows by column (key=white, command/label=turquoise #00AAAA, description=green) but the shared `render_menu_workspace` only coloured the calendar; option rows used a plain `selectable_label` with inherited text colour, so Settings did not match the POM. Root cause: the shared renderer received only calendar colours (`MenuCalendarColours`), never the POM option-column colours. | menu-workspace Req 2.1a/2.1c (laid out identically to the POM); Req 13.4/13.5/13.6 (Legacy option colours) | 🔲 Manual: open Settings (`=0`) under the Legacy theme -- option key/command/description columns show white/turquoise/green exactly like the POM | Reported via prompt: "The settings menu does not follow the color scheme of the Pom. the whole list is in white...". Fixed: `MenuCalendarColours` -> `MenuColours` gained `option_key`/`option_command`/`description`; shell `menu_colours()` maps them from `PomColours` (option_key/option_label/normal_text); each row now drawn as a coloured `LayoutJob` (transparent Button) matching the POM. Disabled rows stay grey. verify.ps1 CLEAN. |
| B052 | FIXED | Medium | `ff-desktop` | In the Theme Editor, the "Save As" (and by the same mechanism "Copy"/"Save") button did not work. Root cause: the render (`theme_editor_panel::render`) used a single `action` local; the colour-token editor is rendered AFTER the Copy/Save/Save As buttons and set `action = EditToken(..)` on a hex field's `lost_focus()`. Clicking "Save As" makes the focused hex `TextEdit` lose focus that same frame, so its `lost_focus` branch overwrote the button's `SaveAs` action with `EditToken`, and the save was discarded. | theme-and-appearance Req 20.5 (Save/Save As write the theme file) | 🔲 Manual: open Theme Editor, type a new name, click Save As -> a new theme file is written and becomes the edit target | Reported via prompt: "the 'Save As' Button is not working". FIXED (Phase theme-editor-fixes, with CR-CH-019): render now splits `action` (buttons/selectors) from `token_action` (token lost_focus EditToken); button actions take priority (returned when set, else the token action), so a same-frame token commit can no longer clobber a button click. Regression test `theme_editor_save_as_after_edit_writes_file_b052`. verify.ps1 CLEAN. |
| B050 | FIXED | Medium | `ff-desktop` | Typing `=1` (a fastpath option jump) from a Files workspace changes the workspace CONTENT to the target context (reported: Settings) but the TAB HEADER still reads its previous label ("files"). The tab-bar header renders `tab.title` (render_chrome.rs `render_tab_bar`, ~L372-388) and the title-line uses `title_line_text` (shell/mod.rs) which for panel kinds also returns `tab.title`. So when an in-place context switch changes `tab.kind`/content without also updating `tab.title`, the header goes stale. Root-cause class: an option/context-switch path that mutates the active tab's kind/content but does not set `tab.title` to match the new kind (whereas `transform_active_pom_tab` and `open_settings_view`'s non-POM branch both DO set the title). Exact triggering path is obscured statically because POM option targets are data-driven (the POM is a configurable Menu_Workspace, so option 1's target depends on the running `menus/` config) and the Files panel has its own internal command field/handling. | menu-and-statusbar Req 15 (Title_Line reflects active tab); multi-tab-editor Req 3 (tab header title); startup-and-session 14.6 (option routing) | 🔲 Manual: from a Files workspace type `=1` -- the tab header updates to match the new context (e.g. "[SETTINGS]"), not the stale "files" | Reported via prompt: "When typing =1 from a files workspace the context of the window changes to the settings workspace but the tab heading still says files... it should say settings?". Likely fix: ensure every in-place context switch sets `tab.title` alongside `tab.kind` (or, more robustly, derive the panel/system tab header + title-line from `tab.kind` so the label can never go stale). Confirm which `=1`-from-Files path fires (POM option-1 target in the running config; whether it routes via the shell `handle_command` or the Files panel's internal command field) before fixing the precise site. FIXED (Phase bug-sweep Wave 3a, CR-CH-034, menu-and-statusbar Req 17.10): took the robust class-eliminating fix -- the Tab_Header (`render_chrome.rs render_tab_bar`) and the Title_Line (`shell/mod.rs title_line_text`) no longer render the cached `tab.title` for a non-Home Menu_Workspace; both derive the label from the tab's LIVE loaded menu (`menu_workspace.tab_title()`), falling back to the cached title only when no menu is loaded. So an in-place context switch that reloads the menu can never leave the header stale, regardless of which dispatch path fired or the running config. `is_home` still shows the POM banner/`[POM]`; `workspace_name` still wins (CX Req 1.4); panel kinds keep their fixed bracket labels; the file-editor path is unchanged. Regression test `title_line_menu_workspace_uses_loaded_menu_not_stale_title` (RED before, green after -- a stale `[FILES]` title on a loaded "Settings" menu now renders `[SETTINGS]`). verify.ps1 CLEAN (FULL nextest); ffwb.exe rebuilt. |

| B053 | FIXED | Medium | `ff-desktop` | END from the Menus Editor always returns to the POM, even when the editor was opened from the Settings menu (POM -> Settings -> Menus). END should return one level to where the editor was opened from (the Settings menu), matching the existing Settings_Namespace_View behaviour (cw-requirements Req 10.4, which returns END to the Settings_Menu). Root cause: CR-NR-075's END handling put `TabKind::MenusEditor` in the unconditional `pending_return_to_pom = true` branch, so it ignores the navigation origin. | menu-workspace Req 13.1 (END returns to origin); cw-requirements Req 10.4 (return one level to the intermediate Context) | 🔲 Manual: POM -> `=0` (Settings) -> `M` (Menus editor) -> END returns to the Settings menu; POM -> `M` -> END returns to the POM | Reported via prompt: "Navigating from POM to Settings To Menus and then pressing end takes me back to the POM, it should take me back to the Settings Menu." Fix: record the editor's origin (opened-from-Settings vs opened-from-POM) when `open_menus_editor` runs, and on END return to the Settings menu when the origin was Settings, else the POM (mirroring the Settings_Namespace_View precedent). Folded into the CR-NR-075 implementation commit (pre-commit fix to just-added code). |

| Phase (menus-editor) | B053 added -- END from the Menus Editor always returns to the POM even when opened from Settings; should return one level to the Settings menu (mirror the Settings_Namespace_View END precedent). Fixed in the CR-NR-075 implementation: track the editor's open-from origin and route END accordingly |

| Phase (menus-editor) | B053 FIXED -- Menus Editor records opened_from_settings; END returns to the Settings menu when opened from Settings, else the POM. Interim per-flag fix; to be superseded by the per-tab Navigation_Stack CR (uniform pop-the-stack END + START semantics). verify.ps1 CLEAN |

| B056 | FIXED | High | `ff-desktop` | POM Tab order regressions after CR-CH-023. (1) Tab from the command field walks the 6 POM option buttons (0/1/2/L/M/X) with NO visible focus indicator, so focus appears to vanish for 6 presses before reaching the menu bar (Req 16.10: a focused option must render with reversed colours -- not implemented in `render_menu_workspace`). (2) When focus reaches the menu bar and Enter is pressed on what should be the first item ("Settings"), the SECOND menu ("File Catalogs") opens instead -- an off-by-one between the captured `menu_first_id` (`settings_btn.response.id`) and the widget egui actually activates. Also revealed a test gap: the CR-CH-023 full-shell tests asserted "Tab leaves command field / not SCROLL / cycle wraps" but NOT that focus lands on a visible option or that the first menu item is Settings, so the tests passed while the behaviour was wrong. | Req 16.10 menu-and-statusbar; menu-workspace 15.2 | 🔲 Manual: POM, Tab -> first option shows focus outline; Tab to menu bar -> Settings focused; Enter opens Settings | FIXED: (1) root cause of the "invisible" stops was `render_calendar_row` using `egui::SelectableLabel` (focusable) for each week row -- ~5-6 focusable day rows sat between the calendar and the menu bar; changed to plain `ui.label` (non-focusable, Req 15.10). (2) The calendar `<`/`>` day cells and non-today cells also switched to plain labels. (3) A focus outline is now painted on the keyboard-focused option (Req 16.10). (4) The interior focus id could not be requested from a stale previous-frame id, so the shell Boundary_Policy now uses a one-shot latch (`focus_first_interior_requested`) honoured by the interior render with the FRESH same-frame id; menu-bar ids round-trip fine and are used directly for the wrap/reverse. With the spurious calendar-row stops removed, the first menu button reached is Settings (verified: focus lands on the captured Settings id immediately after the calendar `>`), so Enter opens Settings. Root cause of the test gap fixed too: added full-shell `egui_kittest` `build_eframe` tests asserting first-Tab lands on the reported first interior and that Settings is the first menu button reached. verify pending. Reported via prompt: "focus shifts ... to somewhere invisible and only after 6 presses does it arrive at the Menu bar ... instead the File catlogs Menu opens" |
| B057 | VERIFIED | Medium | `ff-desktop` | Theme Editor Context Tab order has a phantom "invisible" focus stop between the command line and the Theme selector combo. Observed order: Command line -> [invisible] -> Theme drop-down -> Set Active -> Reset to built-in -> ... Root cause: the `TabKind::ThemeEditor` render arm (`shell/render.rs`, ~L766) was never wired into the CR-CH-023 unified tab-order model. Unlike the `MenuWorkspace` and `MenusEditor` arms, it does NOT report `self.first_interior_id` / `self.last_interior_id` and never calls `honour_interior_focus_latch`. With `first_interior_id = None`, the shell Boundary_Policy cannot perform the command-field -> first-interior latch, so egui-native traversal lands on the first focusable frame element (the panel's outer container / scroll-region allocation) before the ComboBox -- the phantom stop. Same class as B056 (POM) but in the Theme Editor Context, which was missed when CR-CH-023 landed. | Req 16.3/16.8 menu-and-statusbar (CR-CH-023); theme-and-appearance Req 20.1 | 🔲 Manual: open the Theme Editor (bare `THEME`), Tab from the command line -> focus must land on the Theme selector combo (no invisible stop), then Set Active / Reset / New name / Copy / Save As / Save / colour hex fields, then menu bar, then wrap | Reported via prompt: "Tabbing in the Theme editor is: Command line > Invisible ? > Theme drop down > Set Active > ... What is the invisible item?" Diagnosed read-only. Fix: `theme_editor_panel::render` returns first (`theme_editor_select` combo) + last stable interior ids; the ThemeEditor arm sets `first_interior_id`/`last_interior_id` and calls `honour_interior_focus_latch`, mirroring the MenusEditor arm. Full-shell egui_kittest regression test asserts the first interior stop is the combo. Existing criterion (CR-CH-023 Req 16) -- no gate needed. |
| B059 | FIXED | Medium | `ff-desktop` | Multiple workspace Contexts have the SAME phantom "invisible" Tab-stop bug as B056/B057/B058, latent because they had not been Tab-tested yet: their `TabKind` render arms in `shell/render.rs` render the panel but do NOT report `first_interior_id`/`last_interior_id` and do NOT call `honour_interior_focus_latch`, so the outer match leaves both `None` and Tab from the command line lands on a phantom container stop before the first real control. Affected arms (audit): Plugin Manager (first control = Filter field), Event Log (level-filter combo / text filter), Macro Library (Filter field), Search Results (Search query field), Command Configurator (Add button / list). FilesPanel and FileEditor are separate cases (FilesPanel has bespoke B024 catalog-focus handling; the editor body is a natively-focusable text widget) -- assessed individually. | Req 16.3/16.8 menu-and-statusbar (CR-CH-023) | 🔲 Manual: open each workspace (PLUGINS, LOG, MACROS, SEARCH, COMMANDS), Tab from the command line -> focus lands on the first real control, no invisible stop | Reported via prompt: owner asked whether all workspaces should work the same after the B058 fix. Audit-driven (not yet user-observed on each screen). Fix: per the new `workspace-conformance.md` steering rule -- give each first control a stable id, report first/last interior ids, call `honour_interior_focus_latch`; full-shell egui_kittest test per workspace. Existing criterion (CR-CH-023 Req 16) -- no gate needed. FIXED (Phase workspace-conformance): each arm now reports first/last interior ids + calls `honour_interior_focus_latch` -- Plugin Manager (`plugin_manager_panel::filter_field_id`), Macro Library (`macro_library_panel::filter_field_id`), Search Results (`search_results_panel::query_field_id`), Event Log (`event_log_panel::render` returns the level-filter combo's fresh response id), Command Configurator (`CommandConfiguratorState.first_interior_id` = the Add button's response id). Five full-shell egui_kittest regression tests (`full_shell_*_first_tab_focuses_interior` via a shared `assert_first_tab_lands_on_reported_interior` helper); confirmed RED for Plugin Manager without the wiring. Steering rule `.kiro/steering/workspace-conformance.md` added so the class cannot recur. FilesPanel + FileEditor intentionally excluded (FilesPanel has bespoke B024 catalog-focus handling; the editor body is a natively-focusable text widget) -- to be revisited under the WorkspaceContext framework. verify.ps1 CLEAN (FULL, nextest); ffwb.exe rebuilt. |
| B060 | FIXED | Medium | `ff-desktop` | Two "invisible" Tab stops after the last option on the Settings menu Workspace = the calendar `<` / `>` month-nav buttons, drawn OFF the visible right edge but still focusable. TWO coupled causes: (1) the Settings menu inherits `show_calendar = true` (the `MenuFile` default) so the calendar renders on Settings at all; (2) LAYOUT DEFECT (the real bug) -- in `render_menu_workspace` the option list sits in a `ui.vertical { ScrollArea::vertical }` inside a `ui.horizontal_top`, and the calendar is placed to its RIGHT at the option column's natural width + 32px, which does NOT reflow to the panel clip width. Empirically confirmed (throwaway egui_kittest probe, since deleted): the `>` button stays at x approx 745 regardless of panel width; at panel width >= 760 it is visible, but at <= 600 it renders at x=745 OUTSIDE the clip rect (0..600) -- invisible yet still a Tab stop. So ANY calendar-on menu in a workspace narrower than the option-list natural width hides the calendar while keeping its two phantom Tab stops. NOT the B056-B059 code defect (the render correctly omits calendar + its stops when `show_calendar = false`; `menu_workspace_no_calendar_last_interior_is_last_option`). User rule: calendar optional per menu; WHEN shown it MUST be visible and its `<`/`>` reachable; WHEN hidden (or when it cannot fit) it MUST NOT be a Tab stop; Settings defaults calendar OFF but turning it ON must make it visible. Fix: (a) `DEFAULT_SETTINGS_TOML` `show_calendar = false`; (b) lay the calendar out so a shown calendar is always within the visible area (reserve its column from the right; give the option list the remainder), and if it genuinely cannot fit, hide it AND drop its ids from the interior focus contract. | menu-workspace Req 1.8 (+ new criteria: Settings default calendar-off; shown-calendar-is-visible; hidden/unfit-calendar-not-a-Tab-stop) | 🔲 Manual: (1) open Settings -> no calendar, Tab walks options then menu bar, no extra stops; (2) set Settings `show_calendar = true` in a narrow window -> calendar is visible and `<`/`>` are reachable, never off-screen phantom stops | Logged via prompt: "in the settings menu, after the last item in the menu there are 2 invisible tabs. I think these are the Calendar tabs but calendar is hidden?" then "Calendar can appear on any menu workspace like it does on the pom but can be hidden... When hidden it should not be part of the tab stops." then "the calendar for the settings menu says true [but] the calendar is invisible or not displayed" and "Settings menu will default to show calendar as false, but if i change it to true the calendar must become visible. if i create other menus the calendar option should remain optional and if selected be visible". Related to deferred CR-NR-059 (calendar-responsive-hide). CHANGE REQUEST CR-CH-026; requirements gate before fix. FIXED (Phase calendar-visibility, CR-CH-026, TDD): (a) `DEFAULT_SETTINGS_TOML` now sets `show_calendar = false` so Settings shows no calendar by default; (b) `render_menu_workspace` computes `display_calendar = show_calendar && available_width >= OPTION_LIST_MIN_WIDTH(260) + CALENDAR_GAP(32) + CALENDAR_MIN_WIDTH(180)`, constrains the option `ScrollArea` to `available - gap - CALENDAR_MIN_WIDTH` (reserved right column, no overlap), renders the calendar ONLY when it fits, and reports the calendar `<`/`>` ids ONLY when displayed (else last interior = last enabled option). Empirically the `>` button now stays within the clip at wide widths and the calendar is omitted (zero Tab stops) when too narrow. Tests: `default_settings_toml_hides_calendar`, `menu_calendar_shown_when_wide_next_button_is_on_screen` (asserts `>` rect within `ui.clip_rect()` -- the B060 regression guard), `menu_calendar_omitted_when_too_narrow_no_calendar_tab_stops`, `full_shell_settings_tab_walks_options_only_no_calendar_stops`. All 80 menu_workspace + 20 full-shell tests pass; verify.ps1 CLEAN (FULL, nextest); ffwb.exe rebuilt. |
| B058 | FIXED | Medium | `ff-desktop` | The Settings/CONFIG flat-panel Context (`TabKind::SettingsPanel`, opened by `CONFIG` / `CONFIG <ns>`) has a phantom "invisible" focus stop between the command line and the first control (the Filter field). Same class as B057 (Theme Editor) and B056 (POM): the `TabKind::SettingsPanel` render arm (`shell/render.rs`, ~L606) was never wired into the CR-CH-023 unified tab-order model -- it renders the panel but does NOT report `self.first_interior_id`/`self.last_interior_id` and never calls `honour_interior_focus_latch`. The outer match resets both ids to `None`, so the shell Boundary_Policy cannot latch the command-field -> first-interior jump and egui-native traversal lands on the panel container/scroll allocation before the Filter field. | Req 16.3/16.8 menu-and-statusbar (CR-CH-023); configuration-system Req 15/20 | 🔲 Manual: open CONFIG, Tab from the command line -> focus must land on the Filter field (no invisible stop first) | Reported via prompt: "config workspace, there is an invisible tab stop after command line". Diagnosed read-only. Fix: give the Filter TextEdit a stable id, report it as the first (and, as a safe always-present anchor, last) interior id from the SettingsPanel arm, and call `honour_interior_focus_latch` -- mirroring the Theme Editor (B057) and Menus Editor arms. Full-shell egui_kittest regression test. Existing criterion (CR-CH-023 Req 16) -- no gate needed. FIXED (Phase config-tab): `settings_panel::filter_field_id()` gives the Filter TextEdit a stable id; the `TabKind::SettingsPanel` render arm reports it as `first_interior_id`/`last_interior_id` and calls `honour_interior_focus_latch` (mirrors the Theme Editor / Menus Editor arms). Regression test `full_shell_config_first_tab_focuses_filter_field` (confirmed RED without the wiring, GREEN with it). verify.ps1 CLEAN (FULL, nextest); ffwb.exe rebuilt. |
| B061 | FIXED | High | `ff-desktop` | Chained fastpath navigation is unimplemented at the command line: typing `=0.K` (pop to POM, select option 0, then option K) shows `Error in =0.K: command not yet implemented`. Root cause: the shell's `handle_command` only resolved SINGLE-segment fastpaths (`=0`, `=1`) via `resolve_pom_option_key`; the multi-segment resolver `menu_workspace::commands::resolve_chained_path` existed and was unit-tested but was `#[allow(dead_code)]` and NEVER called from dispatch, so a dotted/semicolon path fell through every intercept to the CommandEngine stub. Contradicts an EXISTING requirement (menu-workspace Req 5, Chained Navigation: `=<k1>.<k2>...` up to 4 levels, `.`=collapse/STOP, `;`=push/PUSH, POM as Navigation_Origin). | Req 5 menu-workspace | 🔲 Manual: from any workspace type `=0.K`; the Keys Workspace opens (POM option 0 = Settings, then K = KEYS) and END returns to POM | Fixed: new `WorkbenchShell::try_chained_fastpath` replaces the single-digit-only dotted block in `handle_command`. It splits a path on `.`/`;` into <=4 segments; a leading `=` is the Navigation_Origin (pops to the POM before segment 1, so `=0` resolves against the POM from any Workspace); a bare dotted path still requires a single-digit first segment (so `abc.def`/DSNs are not hijacked); each segment is dispatched as an Option_Key through `handle_command`, opening each sub-menu and activating the option on the real navigation path. The superseded `#[allow(dead_code)]` `resolve_chained_path` (unconsumed `|CHAIN:` marker) and the now-unused string-only `lookup_option` were removed. Tests: `shell::tests::chained_fastpath_equals_zero_dot_k_opens_keys_workspace`, `chained_fastpath_pops_to_pom_origin_from_non_pom` (RED before, GREEN after); legacy `2.1` fastpath + `abc.def` fall-through unaffected. verify.ps1 CLEAN (FULL, nextest); ffwb.exe rebuilt. |
| B062 | FIXED | Low | `ff-command` / `ff-desktop` | The command line uppercases the ENTIRE input for matching (`handle_command`: `let upper = cmd.trim().to_uppercase()`), conflating the case-insensitive VERB with case-sensitive ARGUMENTS. Command verbs SHOULD be case-insensitive, but arguments MUST be case-preserving -- critical for `FIND '<text>'`, `CHANGE '<from>' '<to>'`, `LOCATE <label>`, `THEME <Name>`, `NAME <Name>`, paths, etc. Today correctness depends on each handler remembering to slice the ORIGINAL `cmd` (not `upper`) for its argument; some handlers do, but the pattern is fragile and inconsistent, and any handler reading the argument from `upper` silently corrupts case. This will bite Find/Replace (CHANGE/FIND) where search/replace case must be preserved. VERIFIED (audit + guard tests): a full audit of every `handle_command` arm found NO arm actually passes a slice of `upper` as its argument -- EVERY argument-bearing arm (FIND, CHANGE, LOCATE, THEME, EXCLUDE, SHOW, START, MENU, EDIT, SWAP, RESET BARE, STATUS, CREATE, NAME, WORKSPACE paths, ...) slices its argument from the ORIGINAL `cmd`, so argument case is NOT corrupted today; `upper` is used ONLY for case-insensitive verb matching. The downstream `CommandEngine`/`PrimaryCommandParser` also already uppercases only the first token and preserves argument tokens verbatim. So the feared corruption does not occur. Rather than the risky whole-line `(verb, rest)` refactor (which has real hazards: multi-token verbs `RESET BARE`/`FILE CATALOGS`/`SPLIT DETACH`, the `X`/`=X` intercepts, and the `=`/`.`/`;` fastpaths -- a large change for zero behaviour benefit), the fragility is LOCKED IN with regression guard tests: `shell::tests::{command_arguments_preserve_case_in_history, parse_two_args_preserves_argument_case, mixed_case_theme_verb_and_argument_resolve}` (mixed-case FIND/CHANGE/LOCATE args preserved through the whole path; CHANGE parser preserves both args; lowercase verb + cased argument resolves). Severity lowered High->Low (no live corruption). If a future handler mistakenly slices from `upper`, these tests fail. verify.ps1 CLEAN (FULL, nextest). REFACTOR (Option C, owner-chosen -- least complicated, fewer lines, same result): added `helpers::verb_arg(cmd, verb) -> Option<&str>` (first word `eq_ignore_ascii_case`, returns the TRIMMED case-preserved remainder, empty for the bare verb) and replaced the ~25 hand-counted `upper.starts_with("X ")` + `cmd.trim()[n..]` slice pairs across `handle_command` (EDIT, START, MENU, MENU.OPEN, KEYS, CONFIG, LOCATE, UP/DOWN/LEFT/RIGHT, SORT, FIND, CHANGE, THEME, PROFILE, HILITE, SCROLL, NAME, NUM, STATUS, CREATE, REPLACE, BROWSE, VIEW, COMPARE) with `if let Some(arg) = verb_arg(cmd, "VERB")` -- removing the magic byte offsets and centralising the "case-insensitive verb, case-preserving arg" rule in ONE place. Multi-token verbs (RESET BARE / FILE CATALOGS / SPLIT DETACH / CAPS|NULLS|STATS ON|OFF / EXCLUDE ALL / SHOW ALL / RESET variants), the `X`/`=X` exit intercept, the EXCLUDE/SHOW dual-alias arms, and the `=`/`.`/`;` fastpaths keep their explicit checks. Behaviour identical (whole suite green). Test `verb_arg_matches_case_insensitively_and_preserves_argument`. verify.ps1 CLEAN; ffwb.exe rebuilt. | command-framework (command parsing); relates to find-and-replace, navigation-commands, command-semantics | 🔲 Manual: once fixed, `FIND 'Error'` must not match `error` when case-sensitive; `THEME Default Legacy` preserves case | LOGGED + DEFERRED (owner: "log + defer"). Fix direction: parse the line into `(verb, rest)`, uppercase ONLY the verb for matching, and pass `rest` (original case) to every handler; audit each `commands.rs` arm to read its argument from `rest`/`cmd`, never `upper`. To be done BEFORE (or as part of) the Find/Replace work. No code yet. FIXED (Phase bug-sweep, Wave 0): the Option C `verb_arg(cmd, VERB)` helper (`shell/helpers.rs`) matches the verb case-insensitively and returns the case-PRESERVED, trimmed remainder; every argument-taking arm in `commands.rs` (EDIT/START/MENU/KEYS/CONFIG/LOCATE/UP/DOWN/LEFT/RIGHT/SORT/FIND/CHANGE/THEME/PROFILE/HILITE/SCROLL/NAME/NUM/STATUS/CREATE/REPLACE/BROWSE/VIEW/COMPARE/...) now reads its argument via `verb_arg` (never `upper`), so a search/replace/theme-name argument reaches its handler with original case. `handle_command` still uppercases a COPY (`upper`) for keyword matching only. Regression tests: `verb_arg_matches_case_insensitively_and_preserves_argument`, `command_arguments_preserve_case_in_history`, `parse_two_args_preserves_argument_case`, `mixed_case_theme_verb_and_argument_resolve`. verify.ps1 CLEAN (FULL, nextest). The "no code yet / deferred" note was stale. |
| B064 | FIXED | High | `ff-config` / `ff-desktop` | RESET BARE run under a profile (`ffwb -p ispf`) does NOT reset the theme, AND (deeper) the configuration file is NOT isolated per profile. Two coupled causes: (1) CR-NR-081 gap -- `ff_config::paths::user_config_path` resolves `<config>/ffworkbench/config.toml` directly via `dirs::config_dir()`, NOT through the profile-aware `UserDataDir` seam, so `theme.active`/`theme.active_name` are SHARED across all profiles (violates the CR-NR-081 isolation guarantee, startup-and-session Req 22.3/22.9). (2) `reset_in_memory_to_baseline` archives files but never CLEARS the in-memory theme config keys, so `resolve_startup_palette` re-resolves the same theme (a built-in name resolves with no file, so the theme is not reset). | startup-and-session Req 22.3/22.9 (profile isolation); configuration-system Req 19 (RESET BARE) | 🔲 Manual: `ffwb -p ispf`, set a theme, RESET BARE -> theme returns to Default Legacy; the default profile's theme is unchanged | Fix (step 1, BUG, no gate): (A) make `ff_config` profile-aware so `user_config_path` -> `profiles/<slug>/config.toml` when a profile is active (default unchanged), completing CR-NR-081; (B) `reset_in_memory_to_baseline` clears `theme.active_name`/`theme.active`/`theme.follow_os` (via `remove_user_value`) so the palette resolves to the Default Legacy baseline, scoped to the ACTIVE profile only. New `RESET BARE <profile>` / `RESET BARE ALL` syntax is a SEPARATE gated requirement (step 2). FIXED (step 1): (A) `ff_config::paths` gained a set-once `ACTIVE_PROFILE` + `set_active_profile`/`active_profile`; `user_config_path`/`user_config_dir` now resolve `<config>/ffworkbench/profiles/<slug>/...` when a profile is active (env override still wins; default unchanged), and `main.rs` step-0 sets BOTH `ff_session` and `ff_config::paths` from the same `--profile` parse before `init()`. (B) `reset_in_memory_to_baseline` now `remove_user_value`s `theme.active_name`/`theme.active`/`theme.follow_os` and calls `set_active_theme("Default Legacy")`, so the ACTIVE profile's theme resets to the Default Legacy baseline (other profiles untouched -- writes go to the active profile's config.toml only). Tests: `ff-config paths::tests::user_config_path_is_profile_aware`; `ff-desktop shell::tests::execute_reset_bare_resets_theme_to_default_legacy`. verify.ps1 CLEAN (FULL, nextest); ffwb.exe rebuilt. Step 2 (`RESET BARE <profile>`/`ALL`) still needs its own gate. FIXED (Phase bug-sweep, Wave 0): step 2 was DELIVERED by CR-NR-083 (this session) -- `RESET BARE <profile> [<profile> ...]` and `RESET BARE ALL` resolve a profile list, archive each best-effort, and only reset the running shell when the active profile is in the list; the confirmation dialog names the target profile(s). With step 1 (active-profile theme reset + profile-aware config) also done, B064 is fully resolved. Tests: `reset_bare::tests::resolve_*`/`enumerate_*`/`profile_udd_path_*` (9), `ff-session default_base_and_profiles_root_ignore_active_profile`, `shell::tests::{reset_bare_bare_resolves_single_default_target, reset_bare_unknown_named_profile_errors_without_dialog, execute_reset_bare_resets_theme_to_default_legacy}`. verify.ps1 CLEAN (FULL, nextest). |
| B067 | FIXED | High | `ff-desktop` | F12 RETRIEVE does not always recall the previous command, especially in a newly created workspace. Regression introduced by B066: `dispatch_key_command` now merges the `Command ===>` field content into the dispatched string, so F12 with a NON-EMPTY field dispatches `RETRIEVE <field>` (e.g. `RETRIEVE 1`). But `handle_command` matches RETRIEVE by EXACT string in two places -- the history-exclusion guard (`if upper != "RETRIEVE"`, commands.rs ~L111) and the dispatch branch (`if upper == "RETRIEVE"`, ~L436) -- unlike START/LOCATE/etc. which use `starts_with`. So `RETRIEVE 1` (a) is ADDED to history (pollution), (b) SKIPS the recall branch entirely, and (c) falls through to the CommandEngine route which calls `retrieve_state.reset()` and errors. RETRIEVE only works when the field is empty (bare `RETRIEVE`). A freshly created/navigated workspace commonly leaves the command field non-empty, which is why it fails "especially in a new workspace". History is shell-global and NOT per-tab (so it is not a per-tab reset problem). | function-keys-and-history Req 19.1-19.7 (RETRIEVE recall); Req 22.2 (RETRIEVE Excluded_Command); command-framework Req 9.8 (B066 merge) | 🔲 Manual: type a command, then type any text in the field and press F12 -> the previous command is recalled (not an error); repeated F12 walks older; F12 with empty field also recalls | Reported via prompt: "F12 Retrieve should bring back the previous typed command, it does not always do that ... Especially in a new workspace just created". Regression from B066 (478c598). FIXED: both RETRIEVE guards in `handle_command` now match the VERB by prefix (`upper == "RETRIEVE" || upper.starts_with("RETRIEVE ")`) -- the history-exclusion guard and the dispatch branch -- so the merged `RETRIEVE <field>` produced by B066's `dispatch_key_command` still triggers recall and is still excluded from history. The handler continues to read `self.command_text` (the actual field) for the LIST/empty trigger (Req 19.1). Tests: `retrieve_via_key_with_nonempty_field_recalls_previous_command`, `retrieve_list_via_key_opens_history_overlay`. verify.ps1 CLEAN FULL nextest; ffwb.exe rebuilt. |
| B066 | FIXED | High | `ff-desktop` | Function keys do not pass the `Command ===>` field content as the command argument. Typing `1` then pressing F9 (bound to `SWAP`) should behave exactly like typing `SWAP 1` + Enter (swap to tab 1); instead F9 runs bare `SWAP`, which with no split opens the tab picker (the `SWAP LIST` behaviour). The typed `1` is ignored. Root cause: the F-key dispatch in `shell/update.rs` (~L707-753) resolves ONLY the binding's command string (`b.command()`) and calls `dispatch_bound_command(&cmd)` -> `handle_command(<verb only>)`; it never reads `self.command_text`. Contrast the Enter path (`render_command_field`, render.rs ~L89-95) which passes the FULL field text (verb + args) to `handle_command`. This is the missing implementation of an ALREADY-SPECIFIED criterion: command-framework Req 9.8 ("WHEN a function key ... bound to a command is pressed, THE shell SHALL invoke that command with the current `Command ===>` field contents as its Argument_String -- observably identical to typing `<command> <field>` + Enter ... e.g. type `8`, press DOWN -> `DOWN 8`"), with 9.7 (merge at the shared dispatch boundary), 9.9 (do NOT force-clear the field; consumption is the command's decision), 9.10 (indistinguishable from a typed invocation). Bare F9 (empty field) must still swap to the previously active workspace (existing bare-SWAP semantics). | command-framework Req 9.8 (also 9.1, 9.7, 9.9, 9.10); function-keys-and-history Req 3.1, 3.3; multi-tab-editor Req 18.1 (`SWAP n`) | 🔲 Manual: type `1` + F9 -> swap to workspace 1; `2` + F9 -> workspace 2; F9 with empty field -> previously active workspace; the field content is passed as the argument | Reported via prompt: "Typing 1 and pressing F9 should swap to workspace 1 ... pressing F9 on its own should swap the previously active workspace ... Currently typing 1 and pressing F9 is behaving like a swap list command". NOTE: the criteria already exist (command-framework Req 9 from CR-NR-054, which is PENDING GATE); this fix lands the F-key portion of Req 9.8 (the general field-content-as-argument merge at the dispatch boundary). FIXED: new `WorkbenchShell::dispatch_key_command(cmd)` (target_dispatch.rs) merges the trimmed `command_text` as the argument (`format!("{cmd} {field}")`) when the field is non-empty, else runs the bare command; does NOT force-clear the field (Req 9.9). Both key seams route through it -- the physical F-key path (`shell/update.rs`) and the Key_Label_Bar click (`shell/render.rs`). Tests: `key_command_merges_command_field_as_argument` (`1`+SWAP->tab1, `2`+SWAP->tab2, no picker), `key_command_with_empty_field_runs_bare_command`, `key_command_does_not_force_clear_command_field`. verify.ps1 CLEAN FULL nextest; ffwb.exe rebuilt. |
| B065 | FIXED | Medium | `ff-desktop` | Menu option description text does not hang-indent when it wraps. When the window (or menu dialog) is narrow enough that a Menu_Option's description overflows to a second line, the wrapped continuation aligns to the LEFT EDGE of the row (under the key column) instead of hanging-indenting under the START of the description column. Root cause: the option row is rendered as a SINGLE monospace `LayoutJob` (`menu_workspace::render::option_row_job`) containing all three columns (key | command | description) drawn as ONE wrapping label in `render_menu_workspace` (`ui.horizontal_top` -> `ui.vertical` -> per-row label). egui wraps the whole label back to the label's own left origin, so the description's overflow lands under the key column, not under the description column. Column alignment on the FIRST line is correct (space-padded within the job); only the WRAPPED continuation is mis-aligned. Investigate whether the fix is a layout change (render the three columns as separate widgets / a grid / an indented sub-block so the description column wraps within its own x-origin) or is better captured as a NEW REQUIREMENT for description hang-indent-on-wrap; decide bug-vs-requirement during triage of the fix. | menu-workspace Req 2.1a (three-column option layout) -- TBD whether a new wrap-alignment criterion is needed | 🔲 Manual: open any menu (POM/Settings), shrink the window until a long description wraps; the wrapped line should start under the description's first character, not under the key column | Reported via prompt: "when a window is made small ... Menu description flow over to the next line ... the left alignment of the overflow to a new line should be directly underneath the left alignment of the Description start". FIXED (treated as a bug against menu-workspace Req 2.1a, no new criterion needed): `menu_workspace::render::wrap_description_with_indent(desc, indent, max_line_chars)` word-wraps the description on the monospace character grid and prefixes each continuation line with `indent` spaces (indent = 4 + 2 + cmd_width + 2 = the key+command columns) so wrapped lines hang under the description column. `option_row_job` takes a `max_desc_chars` budget (derived at render time from `ui.available_width()` / glyph advance; 0 = no-wrap) and disables egui's own re-wrap (`job.wrap.max_width = INFINITY`) so it honours our breaks. Tests: `wrap_description_{short_is_single_line_no_indent, long_hangs_continuation_under_description, zero_budget_is_single_line, overlong_word_is_kept}`, `option_row_job_wraps_with_hanging_indent`, `option_row_job_preserves_column_alignment` (no-wrap path unchanged). verify.ps1 CLEAN; ffwb.exe rebuilt. REOPENED (owner: "still happening"): the first-attempt fix DID NOT WORK at runtime. Real root cause: the option row is rendered as ONE `egui::Button::new(job)` inside a `ScrollArea` with `ui.set_max_width(...)`, so egui RE-WRAPS the button's galley to the actual pixel width, wrapping back to x=0 and IGNORING both the manually inserted `\n`+indent spaces AND the `job.wrap.max_width = INFINITY` (a constrained ui forces re-layout). A single combined galley cannot hang-indent -- egui always wraps a galley back to its own x-origin. The character-budget approach (`available_width / glyph_width`) also never matches the true wrap point (button padding / scroll area / proportional vs monospace). Correct fix: render each row as ONE interactive rect painting TWO galleys -- a fixed non-wrapping key+command prefix at x0, and the description as a SEPARATE galley positioned at the description-column x-offset and wrapped to the REMAINING width, so its continuation lines naturally start at the description column (true hanging indent). FIXED (2nd attempt): the row is now a `ui.horizontal` pair -- the key+command PREFIX is a real `egui::Button` (the focusable/clickable row focus stop, preserving the CR-CH-023 Boundary_Policy; disabled rows use a non-interactive Label) built by `option_prefix_job` (non-wrapping, `wrap.max_width = INFINITY`), FOLLOWED by the description as a SEPARATE `egui::Label::new(...).wrap()` that occupies the remaining width. egui wraps the description within its OWN rect, so continuation lines start at the description column (hanging indent) instead of the row's left edge. Removed the failed char-budget/`wrap_description_with_indent`/`option_row_job`+INFINITY approach. Tests: `option_prefix_job_holds_fixed_columns_and_does_not_wrap`, `option_prefix_jobs_share_width_across_rows`; interior Tab-order tests still pass (focus stop preserved); the pixel-exact wrap alignment is a rendered-widget property (test-plan 2.2c, manual/visual). verify.ps1 CLEAN (FULL, nextest); ffwb.exe rebuilt. |
| B063 | FIXED | High | `ff-theme` / `ff-desktop` | A user theme SAVED from Default Legacy loses the Legacy per-column menu colours: after Save As + Set Active, the whole POM/menu renders WHITE (options should be white, commands turquoise, descriptions green). Root cause: the theme `mode` does not round-trip through save/load. The serialiser (`ff-theme/src/serialiser.rs`) writes the mode only as a COMMENT (`# Mode: legacy`), not a parseable field; and `load_from_toml(toml, mode)` sets `palette.mode = <passed arg>` and never reads any mode field, while `theme_defaults::load_theme_by_name` calls it with a HARDCODED `VisualMode::Dark`. So every user theme loads as `mode = Dark` regardless of what it was copied from. `menu_colours()` gates the ISPF white/turquoise/green scheme on `palette.mode == VisualMode::Legacy`, so a Legacy-derived saved theme falls back to inherited white. | theme-and-appearance Req 5 (visual modes), Req 9 (serialisation round-trip), Req 13 (Legacy colour semantics) | 🔲 Manual: Theme Editor -> select Default Legacy -> Save As "MyLegacy" -> Set Active; POM options must stay white/turquoise/green (not all white) | Diagnosed: `menu_colours_are_legacy_scheme_when_legacy_palette_active` confirms the render path is correct when `mode == Legacy`, so the fault is mode persistence. Fix: serialise `mode` as a real TOML field (`mode = "legacy"`) and have `load_from_toml` read it back (falling back to the passed default when absent), so a copied Legacy theme round-trips `mode = legacy`. Existing criteria (Req 5/9/13) -- no gate needed. FIXED: `ff-theme/src/serialiser.rs` writes `mode = "<section_name>"` after `name`; `ff-theme/src/loader.rs::load_from_toml` reads the `mode` field (via `VisualMode::from_str_loose`) and uses that EFFECTIVE mode for the default palette, style slots, and the final `palette.mode`, falling back to the passed arg when the field is absent. Tests `serialiser::tests::{serialise_round_trip_preserves_legacy_mode, load_without_mode_field_uses_passed_default}` (RED before, GREEN after) + `ff-desktop shell::tests::menu_colours_are_legacy_scheme_when_legacy_palette_active` (render-path guard). verify.ps1 CLEAN (FULL, nextest); ffwb.exe rebuilt. |
| B055 | FIXED | Medium | `ff-desktop` | Wrong Tab focus order at launch: pressing Tab from the command field lands on the six Status_Bar segments first (phase "RUNNING", session start, "Ln x, Col y", encoding, line count, right-aligned "FileForge Workbench v0.1.0") before reaching the Menu bar / POM. Root cause: `render_status_bar` builds each segment with `egui::SelectableLabel` (`Sense::click()`), making them egui-native focus stops. The shell Tab ring (`FocusStop`, Req 16) never includes them, so egui native traversal walks them before the ring gains control. Violates Requirement 16 (Tab-Order Focus Cycle), whose enumerated stops exclude the status bar. | Req 16 menu-and-statusbar | 🔲 Manual: at launch press Tab repeatedly; focus must go Command field -> POM options/menu bar, never the status bar | Fixed: `render_status_bar` now renders each segment with `ui.label(...)` instead of `egui::SelectableLabel` (which uses `Sense::click()` and is focusable). Segments are no longer egui-native Tab stops. Regression guard `status_bar_segments_are_not_tab_focus_stops` (egui_kittest harness). 924 ff-desktop tests pass under nextest. Reported via prompt: "I have to press tab 7 times before i see something get focus and that is the Running status" |
| B054 | FIXED | High | `ff-desktop` | REOPENED -- the first fix (bind TextEdits to the mutable working model) did NOT resolve it. Key/group became typable but Command/Description still cannot be typed, AND (after a further Grid/vertical-layout attempt) the main command line is ALSO blocked while the Menus editor is open -- indicating a GLOBAL focus/input steal, not a layout issue. Three layout-based attempts failed. See docs/status/RESUME-menus-editor-typing-bug.md for the full handoff, the in-place diagnostic, and the strong hypothesis (MENUS may not be landing on TabKind::MenusEditor -- diag lines were absent from the run log). Original: In the Menus Editor (CR-NR-075) the option-row text fields (key / command / description / group) cannot be typed into -- typing does nothing, including on a newly-added option. Root cause: the pure render bound each `TextEdit` to a per-frame local (`let mut key = option.key.clone()`) that was re-cloned from the unchanged working model every frame, so keystrokes were discarded before the next frame (egui needs the edited buffer to persist across frames). The `EditOption` action only fired on `lost_focus`, which never happened because the buffer reset each frame. | menu-workspace Req 13.4 (edit option fields) | 🔲 Manual: open the Menus Editor, add or edit an option -- key/command/description/group accept typed input | Reported via prompt: "in the menu editor i cannot change the Values in options, even if i insert a new option i cannot type any values in the key, command or description fields". Fixed: the render iterates `menu.options` MUTABLY and binds each TextEdit directly to the working field (`&mut option.command` etc.), so edits persist immediately; the Option_Key is uppercased on `lost_focus`. Removed the now-unused EditOption/OptionField action plumbing. verify.ps1 CLEAN. FIXED (Phase bug-sweep, Wave 0): re-verified at runtime -- the fix (fields bound to `&mut option.command`/`&mut option.description` with stable ids `menus_opt_command`/`menus_opt_description` via `menu.options.iter_mut()`) IS present and working; the OPEN/REOPENED status was STALE. Added the missing behaviour regression test `menus_editor_command_and_description_fields_accept_typed_input` (focuses each field, injects an `egui::Event::Text`, runs a frame, asserts the typed char persisted into `state.working.options[i].command`/`.description`) -- confirmed GREEN. Also proven Tab-reachable by the pre-existing `menus_editor_every_stable_control_is_tab_reachable`. verify.ps1 CLEAN (FULL, nextest). |
| B068 | FIXED | Medium | `ff-desktop` | Function keys do not work in a Detached_Workspace window. In the primary window F3 (bound to END) works, but in a detached window F3 (and other F-keys) do nothing; only the TYPED `END` command works there. Root cause: physical F-key detection runs once per frame in the main `update()` reading the PRIMARY window's `ctx.input(...)` and dispatches against the primary window BEFORE the floating-viewport loop; nothing inside a detached viewport's frame (`vctx`) reads F-keys, and even if it did the dispatch would target the primary active tab, not the detached one. So F-keys are effectively primary-window-only. | menu-and-statusbar Req 18.2 (detached window has full functionality incl. keyboard shortcuts); relates to CR-CH-036 (per-window command context) | 🔲 Manual: detach a workspace, press F3 in the detached window -> it performs END on that window (as the typed END does) | Reported via prompt: "I notice that functions keys don't seem to work in the detached window. END command does work but F3 does nothing." Fix (CR-CH-037 bundle): inside each detached window's frame, detect F-key presses from the child `vctx` and dispatch them through `dispatch_key_command` WITHIN the `with_workspace_context` swap so they act on the detached tab (F3 = END there). FIXED (Phase bug-sweep Wave A cont., menu-and-statusbar Req 18.11): extracted the primary F-key resolution into a shared `resolve_function_key_command(ctx)` and added `dispatch_detached_function_key(vctx)`, called inside the detached viewport's `with_workspace_context` swap so a key pressed while the detached window has OS focus resolves via the same key map and dispatches against the detached tab (identical to the primary window). Test `detached_function_key_return_acts_on_its_tab` (F4/RETURN in the detached context returns ITS tab to the POM and leaves the primary tab untouched). Delivered with the detached-window bundle (CR-CH-037 close=RETURN, CR-CH-038 RETURN semantics, CR-NR-088 DOCK+SF2, CR-NR-089 detached menu bar). verify.ps1 CLEAN (FULL nextest); ffwb.exe rebuilt. | (TextEdit bound to a per-frame local, keystrokes discarded); fixed by binding TextEdits directly to the mutable working MenuFile (edits persist immediately; key uppercased on lost_focus). verify.ps1 CLEAN |

| Phase (nav-stack) | B054 REOPENED -- Menus editor Command/Description still un-typable and the command line is also blocked while the editor is open (global focus/input steal, not layout). 3 layout fixes failed. Handoff in docs/status/RESUME-menus-editor-typing-bug.md; diagnostic in shell/render.rs MenusEditor arm (uncommitted); next step is one clean diagnostic run. Also spotted: theme.active validation warning storm every frame (separate B### to log). |
| Phase (statusbar-focus) | B055 added -- Status_Bar segments (SelectableLabel) are egui focus stops, so Tab at launch cycles through the six status segments before the Menu/POM, violating Req 16 tab-order cycle. Fix direction: render segments as non-interactive labels so they are not tab stops. |
| Phase (statusbar-focus) | B055 FIXED -- Status_Bar segments changed from `SelectableLabel` to non-interactive `ui.label`; no longer Tab focus stops (Req 16 conformance). Regression test `status_bar_segments_are_not_tab_focus_stops` added; 924 ff-desktop tests pass under nextest. |
| Phase (tab-order-fix) | B056 added + FIXED -- POM Tab: calendar week-rows were focusable SelectableLabels (spurious "invisible" stops), and the interior focus id could not be requested from a stale frame; fixed by making calendar rows/cells plain labels (Req 15.10), a one-shot fresh-id first-interior focus latch, and a focus outline on the focused option (Req 16.10). First menu reached is now Settings (Enter opens Settings). Full-shell egui_kittest tests added: full_shell_first_tab_focuses_reported_first_interior, full_shell_tab_reaches_settings_as_first_menu_item; calendar test tightened to EXACTLY two focusable widgets. verify.ps1 CLEAN. |
| Phase (theme-editor-tab) | B057 added -- Theme Editor Context never wired into the CR-CH-023 tab-order model (no first/last interior ids, no focus latch), producing a phantom invisible focus stop before the Theme selector combo. Fix: report interior ids from `theme_editor_panel::render` and wire the ThemeEditor arm like MenusEditor; full-shell egui_kittest regression test. Existing criterion (Req 16) -- no gate. |
| Phase (theme-editor-tab) | B057 FIXED -- `ThemeEditorState.first_interior_id` captured from the combo's `response.id` each frame; hex TextEdits given stable ids `("theme_editor_hex", i)`; ThemeEditor render arm sets first/last interior ids and calls `honour_interior_focus_latch` (mirrors MenusEditor). Regression test `full_shell_theme_editor_first_tab_focuses_theme_selector` (confirmed RED without the wiring, GREEN with it). verify.ps1 CLEAN (FULL, nextest); ffwb.exe rebuilt. |
| Phase (theme-editor-tab) | B057 VERIFIED -- owner confirmed Tab order on the Theme Editor workspace now works ("the tabbing on the themes workspace is now working"). |
| Phase (chained-nav) | B061 added -- chained fastpath `=0.K` shows "command not yet implemented"; the spec'd `resolve_chained_path` (menu-workspace Req 5) was never wired into `handle_command` (dead code). Fix: wire chained-path dispatch into the command line (pop to POM origin, resolve each segment as an option key on the target menu, honour `.` collapse / `;` push), reusing the existing menu-open + option-forward path. Existing requirement -- no gate. |
| Phase (chained-nav) | B061 FIXED -- `WorkbenchShell::try_chained_fastpath` wired into `handle_command`; splits `=0.K`/`3.1`/`=0;E.T` on `.`/`;` (<=4 segments), `=` pops to POM origin, each segment dispatched as an Option_Key on the real navigation path; removed superseded dead `resolve_chained_path` + unused `lookup_option`. Tests `chained_fastpath_*` RED->GREEN. verify.ps1 CLEAN; ffwb.exe rebuilt. |
| Phase (menu-bar) | B062 added (LOGGED + DEFERRED) -- `handle_command` uppercases the whole input, conflating case-insensitive verb with case-sensitive arguments; will corrupt FIND/CHANGE search/replace case. Fix (deferred to Find/Replace work): parse `(verb, rest)`, uppercase only the verb, pass `rest` verbatim; audit every handler. |
| Phase (arg-case-audit) | B062 VERIFIED (no live corruption) -- audit found every arg-bearing `handle_command` arm already slices from the original `cmd` (not `upper`); `upper` is verb-matching only, and the CommandEngine parser preserves arg tokens. Feared FIND/CHANGE/LOCATE/THEME case corruption does not occur. Locked in with guard tests `command_arguments_preserve_case_in_history` / `parse_two_args_preserves_argument_case` / `mixed_case_theme_verb_and_argument_resolve`; declined the risky whole-line (verb,rest) refactor (multi-token verbs + X/=X + fastpath hazards, zero behaviour benefit). Severity High->Low. verify.ps1 CLEAN. |
| Phase (theme-mode-roundtrip) | B063 added + FIXED -- a user theme saved from Default Legacy rendered the whole menu white because the visual `mode` did not round-trip (serialiser wrote it as a comment; loader hardcoded the passed default = Dark). Fix: serialise `mode` as a real field and read it back in `load_from_toml` (effective mode = file's mode else passed default). Tests: `serialise_round_trip_preserves_legacy_mode`, `load_without_mode_field_uses_passed_default`, `menu_colours_are_legacy_scheme_when_legacy_palette_active`. verify.ps1 CLEAN; ffwb.exe rebuilt. |
| Phase (reset-bare-targets) | B067 added + FIXED -- F12 RETRIEVE broken when the command field is non-empty (regression from B066): the merged `RETRIEVE <field>` failed the exact-match guards, so recall was skipped, history polluted, and the pointer reset. Fixed by matching the RETRIEVE verb by prefix in both the history-exclusion guard and the dispatch branch; handler still reads the field for the LIST/empty trigger. Tests `retrieve_via_key_with_nonempty_field_recalls_previous_command`, `retrieve_list_via_key_opens_history_overlay`. verify.ps1 CLEAN; ffwb.exe rebuilt. |
| Phase (reset-bare-targets) | B066 added + FIXED -- F-keys now pass the Command ===> field content as the argument (implements command-framework Req 9.8): new `dispatch_key_command` merges the field as the arg at the shared key-dispatch boundary; both the physical F-key path and the Key_Label_Bar click route through it; the field is not force-cleared (Req 9.9). `1`+F9 -> SWAP 1. verify.ps1 CLEAN; ffwb.exe rebuilt. |
| Phase (reset-bare-targets) | B065 FIXED (1st attempt) -- menu option description hang-indent via `wrap_description_with_indent` + `option_row_job` char budget. DID NOT WORK at runtime (see below). |
| Phase (workspace-unify) | B065 REOPENED + FIXED (2nd attempt) -- root cause: the single-`Button` galley was re-wrapped by egui within `set_max_width` back to x0, ignoring the manual `\n`+indent and INFINITY. Real fix: `ui.horizontal` pair -- a focusable `Button` key+command prefix (`option_prefix_job`, non-wrapping) + a separate wrapping `Label` for the description in the remaining width, so continuation lines hang-indent under the description column. Removed the failed char-budget helpers. Focus contract preserved (interior Tab-order tests pass). verify.ps1 CLEAN; ffwb.exe rebuilt. |
| Phase (reset-bare-targets) | B065 added (LOGGED, not fixed) -- menu option description does not hang-indent when it wraps; the wrapped continuation aligns under the key column instead of under the description column, because the whole three-column row is one wrapping monospace `LayoutJob`. To investigate; bug-vs-requirement (a description hang-indent-on-wrap criterion) TBD at fix time. |
| Phase (profile-config) | B064 added + FIXED (step 1) -- config was not profile-aware and RESET BARE did not reset the active profile's theme. Fix: (A) `ff_config::paths` profile-aware `user_config_path`/`user_config_dir` (profiles/<slug>/config.toml when active; set from main.rs step-0 alongside ff-session; env override + default unchanged); (B) `reset_in_memory_to_baseline` clears the theme keys and applies Default Legacy for the ACTIVE profile only. Tests `user_config_path_is_profile_aware`, `execute_reset_bare_resets_theme_to_default_legacy`. verify.ps1 CLEAN; ffwb.exe rebuilt. Step 2 (`RESET BARE <profile>`/`ALL`) is a separate gated requirement. |

| B069 | FIXED | Medium | `ff-desktop` | Arrow keys do not navigate the Configuration workspace. In the File Navigator (Catalogs panel `files_panel.rs` via `focused_catalog` + the modern File Explorer `explorer_view.rs` via `ExplorerKey` gestures) the Up/Down arrows move a keyboard selection between nodes and Right/Enter expand/select a node. In the Configuration workspace (the CONFIG flat key-browser, `config_panel.rs`, `TabKind::ConfigPanel`, opened by `CONFIG` / `CONFIG <ns>` or Settings option `A`) the arrow keys do NOTHING: `config_panel.rs` renders a Filter field + `egui::CollapsingHeader` namespace groups + per-key widgets and relies ENTIRELY on egui-native focus/Tab + mouse clicks -- there is no `key_pressed(ArrowUp/ArrowDown/ArrowRight)` handling, no selected/focused-row state, and no keyboard way to expand/collapse a namespace group. Distinct from B058 (which fixed the phantom Tab stop before the Filter field); this is the ABSENCE of arrow-key navigation, i.e. keyboard behaviour inconsistent with the file-tree workspaces. | configuration-system Req 15 / Req 20 (CONFIG view); relates to file-tree-panel Req 20 (Explorer keyboard nav) -- NO existing CONFIG keyboard-navigation criterion (gap) | 🔲 Manual: open CONFIG, press Up/Down -> a row/group selection moves; Right/Enter expands a collapsed namespace group; the behaviour is consistent with the File Explorer tree | Reported via prompt: "In the File navigator the arrow keys Allow navigations up and down and expansion of the nodes. In the configuration workspace these keys do not work." Diagnosed READ-ONLY: `config_panel.rs` has no arrow-key handling at all (grep for `ArrowDown`/`ArrowUp` in ff-desktop returns command_palette, editor_panel, explorer_view, files_panel, search_results_panel -- NOT config_panel). This reveals a MISSING acceptance criterion (keyboard navigation of the Config view was never specified), so per the workflow a CHANGE REQUEST (CR-CH-039) is logged and the requirements gate runs BEFORE the code fix. FIXED (Phase config-keynav, CR-CH-039, configuration-system Req 21): the CONFIG view is now a keyboard-navigable Config_Tree matching the File Navigator. `config_panel/tree.rs` holds a PURE model + reducer (`visible_rows`, `reduce_config_key` mirroring `explorer_view::reduce_key`, `reconcile_cursor`). `config_panel/render.rs` runs a per-frame `config_keyboard_effects` driver ONLY when the tree owns the keyboard (`tree_has_keyboard` -- not the Filter field or a key widget): Up/Down move a `cursor` (clamped, no wrap), Right expands a collapsed group or steps to its first child, Left collapses an expanded group or steps to the parent, Enter toggles a group / focuses a key's value widget, Home/End jump to first/last; the cursor row is highlighted (`paint_cursor_highlight`) and mouse + keyboard share one `collapsed` map. Tests: 8 reducer unit tests (`config_panel::tree::tests::*`) + full-shell `shell::tests::full_shell_config_tree_arrows_navigate_and_expand`. (config_panel.rs split into config_panel/{mod,render,tree}.rs for the 400-line rule; pre-existing non-ASCII replaced with ASCII per documentation.md.) verify.ps1 CLEAN (FULL, nextest); ffwb.exe rebuilt. |

| Phase (config-keynav) | B069 FIXED -- CONFIG view is a keyboard-navigable node tree (Config_Tree) matching the File Navigator: pure reducer in config_panel/tree.rs mirroring explorer_view::reduce_key + config_keyboard_effects driver gated on tree focus + cursor-row highlight; 8 reducer tests + full-shell egui_kittest. CR-CH-039 / configuration-system Req 21. verify.ps1 CLEAN (FULL nextest); ffwb.exe rebuilt. |

| Phase (config-keynav) | B069 added -- arrow keys do not navigate the Configuration workspace (CONFIG flat key-browser has no keyboard Up/Down/expand, unlike the File Navigator tree). Read-only diagnosis: `config_panel.rs` has zero arrow-key handling and no selection state. Reveals a missing CONFIG keyboard-navigation criterion -> CHANGE REQUEST CR-CH-039 logged; requirements gate runs before the fix. |

| B070 | FIXED | Low | `ff-encoding` | Property test `bom_detection_always_correct` (crates/ff-encoding/tests/property_tests.rs) is INCORRECT for the UTF-16LE case: it prepends the UTF-16LE BOM (`FF FE`) then appends unconstrained random `content` and asserts detection is always `Utf16Le`. When the fuzzer picks `content` starting with `00 00`, the concatenated bytes become `FF FE 00 00`, which is a valid UTF-32LE BOM -- so `detect_bom` CORRECTLY returns `Utf32Le` (per Requirement 2.3 and the passing unit test `detect_bom_disambiguates_utf32le_from_utf16le_with_nul`), but the test asserts `Utf16Le` and fails. Minimal counterexample: `content=[0,0]`, `bom_type=1`. The detector (`bom.rs`) is CORRECT and unchanged; the test's invariant is false for the LE case. Surfaced by a proptest counterexample; unrelated to CR-NR-093 (fails on `cargo nextest run -p ff-encoding` in isolation). | encoding-and-characters Req 2.1, 2.2, 2.3 | -- | Reported during CR-NR-093 Slice 2c.1 verify.ps1 (blocked the FULL gate). Root cause is a test-authoring defect, not a detector defect. Fix (test-only): for the UTF-16LE case constrain the generated content so it cannot extend the short BOM into a longer one (skip content beginning `00 00` via `prop_assume!`), OR derive `expected` from the actual concatenated bytes. No production code change. |

| Phase (window-split-2c) | B070 added -- ff-encoding proptest `bom_detection_always_correct` incorrectly asserts UTF-16LE for `FF FE 00 00` (a valid UTF-32LE BOM); detector is correct, test is wrong. Test-only fix: constrain UTF-16LE content to not begin `00 00`. Surfaced by CR-NR-093 Slice 2c.1 verify. |
| Phase (window-split-2c) | B070 FIXED -- added `prop_assume!` excluding UTF-16LE content beginning `00 00` in property_tests.rs; removed the stale `[0,0]/bom_type=1` proptest-regression seed (it encoded the test defect, not a detector defect). No production change. `cargo nextest run -p ff-encoding` = 181/181 pass. |

| B071 | FIXED | Medium | `ff-desktop` | When the Workspace is split, switching a region's Context to one that renders via a ctx-level panel (the File Explorer, `TabKind::FileExplorerPanel`, rendered by `render_nav_explorer` as a full-window panel) collapses the visible split back to full screen. Root cause: `render_central_panel` computes `is_file_explorer = self.tabs.active_tab().kind == FileExplorerPanel` (resolved through the FOCUSED leaf) and, when true, renders the explorer full-window and SKIPS the `if !is_file_explorer { ... render_split_central ... }` branch -- so the split is bypassed even though the TabGroupTree is still split. The split tree/model is intact; only the RENDER goes full-screen. (Related: the FileExplorer body arm inside `render_active_tab_body` is a no-op -- CR-NR-092/2b accepted edge -- so a FileExplorer in a region would otherwise draw blank; this bug is the more visible full-screen takeover.) | layout-and-docking Req 13.4, 14.4 (both Tab_Groups render simultaneously) | ?? Manual: SPLIT, then in one region open the File Explorer (=2 / FILES) -> the split must REMAIN, explorer confined to its region | Reported via prompt: "when i split a window it opens a new workspace to the side but when we switch the context it takes the whole screen up again, it should remain in the split window". Diagnosed READ-ONLY: render.rs render_central_panel is_file_explorer short-circuit precedes/​bypasses the is_split split render. Fix direction: when is_split(), keep the split render authoritative and render the File Explorer INSIDE its region (via render_active_tab_body) rather than as a ctx-level full-window panel. |

| Phase (window-split-2c) | B071 added -- splitting then switching a region to the File Explorer collapses the split to full screen; render_central_panel's is_file_explorer ctx-level-panel branch bypasses the split render. Fix: when split, the split render is authoritative and the explorer renders inside its region. |
| Phase (window-split-2d) | B071 FIXED -- render_central_panel gates is_file_explorer on `&& !self.tabs.is_split()`; render_nav_explorer split into seed/body/apply so the FileExplorerPanel arm in render_active_tab_body renders the explorer INTO its region while split. Full-shell test full_shell_file_explorer_in_split_region_keeps_split. verify pending (committed with the fix). |
