[package]
name = "ff-dsalloc"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Dataset Allocator for FileForgeWorkbench — JCL DD parsing, DSN resolution, symbolic substitution, allocation simulation, GDG/referback handling, RESOLVE command"

[dependencies]
thiserror = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
chrono = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
pretty_assertions = { workspace = true }
tempfile = { workspace = true }
