[package]
name = "ff-vsam-services"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "VSAM Services for FileForgeWorkbench — KSDS, ESDS, RRDS, LDS behaviour, alternate indexes, record-level operations, and the VsamService trait interface."

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
pretty_assertions = { workspace = true }
