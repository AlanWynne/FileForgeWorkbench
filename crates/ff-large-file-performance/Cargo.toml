[package]
name = "ff-large-file-performance"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "GUI-independent rendering optimisation infrastructure for FileForgeWorkbench — PositionCache, LineLayoutCache, chunked measurement, viewport-aware lazy computation"

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
pretty_assertions = { workspace = true }
