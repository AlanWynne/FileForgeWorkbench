//! `Tab` — a single tab holding a DocumentHandle and independent per-tab state.
//!
//! Each Tab is identified by a unique `TabId` and maintains its own viewport,
//! cursor, selections, language, fold state, and bookmarks.
